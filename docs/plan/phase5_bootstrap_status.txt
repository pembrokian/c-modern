Bootstrap Phase 5 Status

This repository has now completed the narrower repository-specific bootstrap Phase 5 milestone, and it should still not claim that the full Phase 5 from plan.txt is finished.

Short answer on completion status:

- Yes, the narrower repository-specific Phase 5 milestone is now complete.
- No, this is not a claim that the full language-wide Phase 5 in `plan.txt` is complete.
- The repository now has the real hosted executable path that the narrower bootstrap Phase 5 plan was intended to establish, along with the executable evidence and explicit exclusions needed to describe that milestone responsibly.

The ordered implementation sequence for this status note now lives in `docs/plan/phase5_bootstrap_execution_plan.txt`.
The Stage 0 freeze note for that sequence now lives in `docs/plan/phase5_bootstrap_backend_slice.txt`.

What is in place now:
- typed MIR exists as a real compiler boundary after bootstrap semantic checking and before backend lowering
- `mc check` can parse, semantically check, lower to MIR, validate MIR, and emit deterministic MIR dumps for the currently supported bootstrap subset
- deterministic semantic and MIR fixture coverage exists under `tests/sema/` and `tests/mir/`, giving Phase 5 a stable frontend contract to lower from
- the tree now includes a real backend scaffold under `compiler/codegen_llvm/` plus a dedicated `mc_codegen_llvm` CMake target and focused backend unit coverage
- the current driver remains intentionally thin while now being able to emit LLVM IR, invoke the host toolchain, and produce one hosted executable through bootstrap `mc build`
- the current build stays green under the canonical local workflow, so later backend work can start from a known-good Stage 1 baseline

What Stage 0 has now fixed explicitly:
- the first backend choice is LLVM
- the first hosted bootstrap target is the current Darwin arm64 development host, recorded in repository terms as hosted `arm64-apple-darwin`
- the initial runnable backend slice is intentionally smaller than the full MIR surface and starts with direct-call scalar control flow rather than globals, checked faults, aggregate-heavy lowering, low-level memory operations, or atomics
- unsupported MIR outside that initial slice is now expected to fail with narrow backend diagnostics instead of being treated as implied future support
- the repository-specific meaning of Phase 5 bootstrap completion is now written down separately from the full `plan.txt` milestone

What Stage 1 has now fixed explicitly:
- `compiler/codegen_llvm/` is no longer a placeholder; it exposes `BootstrapTargetConfig()` and `LowerModule(...)` as the first backend entrypoints
- CMake now builds the dedicated `mc_codegen_llvm` library and links it into `mc` without turning `mc check` into a codegen command
- the backend scaffold rejects unsupported bootstrap inputs directly, including unsupported targets, globals, extern functions, non-function symbol refs, non-direct calls, and out-of-scope MIR instructions
- focused backend unit tests pin the frozen target contract and the Stage 1 rejection behavior

What Stage 2 has now fixed explicitly:
- the first inspectability surface is deterministic backend IR text, exposed through `DumpModule(...)` in the backend library and the optional `mc check --dump-backend` path
- backend dumps now use deterministic build-dir-scoped paths under `build/*/dumps/backend/`, and `mc dump-paths` reports them alongside AST and MIR dump locations
- backend naming is reviewable and stable for this slice: functions use `@name`, locals use `%local.name`, blocks use `bb<function>_<block>.<source-label>`, temporaries use `%t<function>_<block>_<instruction>`, and reserved future globals use `@g.name`
- `tests/codegen/` now contains focused backend snapshot coverage, and smoke coverage proves one or more checked modules can lower to backend output deterministically without linking

What Stage 5 has now fixed explicitly:
- the backend can render real LLVM IR for the runnable scalar control-flow slice and write deterministic `.ll` artifacts under `build/*/dumps/backend/`
- the backend now emits object files and links hosted executables through the ordinary host `xcrun clang` path on Darwin arm64, with deterministic object and executable paths under `build/*/obj/` and `build/*/bin/`
- the thin driver now exposes `mc build` while still remaining bootstrap-scoped; it reuses the existing parse -> sema -> MIR pipeline and only adds the backend, object, and link path required for one-module executable bring-up
- the hosted entry boundary is now explicit in codegen: an emitted wrapper bridges the C-runtime `main(argc, argv)` entry into bootstrap Modern C `main` procedures with either zero parameters or the canonical `Slice<cstr>`-shaped argument surface
- end-to-end executable coverage now exists in `tests/codegen/codegen_executable_tests.cpp`, and the canonical local workflow proves hello-style and direct-call programs build and run on the first hosted target

What a narrower bootstrap Phase 5 will need to satisfy:
- lower the supported typed MIR subset to backend IR without letting backend defaults redefine Modern C language semantics
- support enough code generation for ordinary procedures, globals, aggregates, calls, branches, and returns to produce runnable hosted executables
- carry the sema-owned layout and MIR-owned checked-operation semantics through code generation for the supported bootstrap subset
- provide a thin bootstrap path for object emission and linking without prematurely claiming the broader Phase 7 tool workflow
- add executable-focused tests under `tests/codegen/` that prove the supported bootstrap subset runs end to end on the first hosted target

What still blocks a fuller Phase 5 completion claim:
- checked runtime fault support is still narrow: the runnable Stage 5 path now realizes the admitted integer-width divide, shift, and bounds traps for the current bootstrap slice, but it does not yet claim broader checked-operation coverage across the full MIR type surface
- globals and aggregate-sensitive lowering are now admitted for the current narrow runnable slice, but broader representation-sensitive cases are still outside the executable surface
- the repository-specific finish note is intentionally narrower than the full `plan.txt` Phase 5 and leaves broader backend and workflow surface for later work

Current structural debt worth tracking explicitly:
- the current driver surface is inspection-oriented around `mc check`; Phase 5 should extend it only as far as backend bring-up requires and should avoid collapsing Phase 7 workflow concerns into early backend work
- the repository currently has a MIR boundary but no backend-specific dump or validation layer, so Phase 5 will need to decide where backend-facing inspectability lives without weakening MIR as the semantic contract
- runtime startup and platform-linking concerns remain outside the current bootstrap compiler path, so executable bring-up must define a narrow ownership boundary between compiler output and hosted runtime support

Practical bootstrap interpretation:
- the repository is entering Phase 5 from a green Phase 4 bootstrap baseline rather than from a parser-only or sema-only bring-up state
- the immediate goal is not a full toolchain or build-system surface; it is a narrow, inspectable backend path that can turn the already-supported MIR subset into working executables on one hosted target
- Phase 5 should continue to distinguish a narrower repository bootstrap milestone from the broader Phase 5 target in plan.txt

Repository finish-note status:
1. the narrower milestone evidence is now recorded in `phase5_bootstrap_finish.txt`
2. this status note should now be read as a summary of what the narrower milestone includes and what still remains outside it
3. later work should treat broader checked-operation coverage, wider representation-sensitive lowering, volatile or atomic lowering, and broader workflow surface as post-Phase-5 expansion rather than as missing bootstrap bring-up

Historical bring-up map and deferred follow-up:

The detailed themes below record the bootstrap bring-up work and the larger follow-up surface around it.
Items still marked pending should be read as explicitly deferred beyond the finished narrower repository-specific Phase 5 milestone, not as blockers to that milestone.

Theme: Backend scope and milestone framing
- completed: restate the narrower bootstrap Phase 5 objective in repository terms so future work tracks the original plan.txt Phase 5 goal without overstating readiness
- completed: choose the first backend implementation path and first hosted target triple or platform combination for bootstrap executable bring-up
- completed: define the initial MIR instructions and semantic-boundary operations that count as the admitted bootstrap backend slice, with unsupported forms required to fail crisply
- pending: document which tool workflow pieces are intentionally deferred to Phase 7 so Phase 5 stays focused on backend bring-up rather than general build orchestration

Theme: Backend scaffolding and ownership boundaries
- completed: add a real backend library under `compiler/codegen_llvm/` and wire it into CMake without bypassing the existing sema -> MIR boundary
- completed: define the backend entry points, module/function lowering API, and fixed hosted Darwin arm64 target configuration for the Stage 1 scaffold
- completed: narrow backend diagnostics now reject globals, extern lowering, unsupported MIR instructions, non-direct calls, and non-function symbol refs directly inside the backend scaffold
- pending: add backend-facing dump or debug output where it helps inspect lowering decisions without making backend IR the language definition
- pending: keep driver responsibilities limited to pipeline orchestration so backend policy does not accumulate inside `compiler/driver/`

Theme: MIR-to-backend instruction lowering
- pending: lower the supported bootstrap MIR control-flow forms, including blocks, branches, conditional branches, switches if needed, and returns
- pending: lower the admitted initial bootstrap value and effect instructions for locals, constants, `store_local`, direct calls, and direct symbol references before widening the slice further
- pending: add checked MIR operations such as bounds checks, divide checks, and shift checks only after the initial executable path is stable and only with explicit executable proof on the first hosted target
- pending: add pointer/integer boundaries, aggregate-heavy lowering, and the current volatile/atomic subset only after the narrower initial slice is already working end to end

Theme: Type, layout, and data representation
- pending: map MIR scalar, pointer, aggregate, array, slice, and buffer representations onto the chosen backend type system for the supported bootstrap subset
- pending: preserve sema-computed struct layout, alignment, padding, and field-offset facts through global and stack allocation lowering
- pending: define how distinct semantic types lower when they share representation with base types so backend representation does not erase sema-owned meaning in the wrong layer
- pending: add layout-sensitive tests that prove backend data placement matches the current bootstrap semantic contract on the first hosted target

Theme: Calls, globals, and hosted entry bring-up
- completed: lower the admitted direct procedure call, parameter passing, and return surface needed by the current runnable executable slice
- completed: define the minimal hosted entry path needed to turn a checked Modern C root module into a runnable program on the first target
- completed: integrate the narrow hosted startup boundary through an emitted `main(argc, argv)` wrapper without turning runtime glue into ad hoc compiler semantics
- completed: extend executable proof beyond hello-style entry and direct calls into scalar globals plus aggregate-return and field-sensitive cases in the current admitted slice

Theme: Object emission, linking, and bootstrap driver surface
- completed: add the minimum driver and backend path needed to emit LLVM IR, produce an object file, and link one hosted executable from one checked source module
- completed: add the narrow link step through ordinary host platform tools without claiming the broader Phase 7 workflow
- completed: define deterministic locations for backend dumps, objects, and linked executables under `build/`
- completed: keep command-line growth bootstrap-thin by adding only `mc build` on top of the existing inspection-oriented surface

Theme: Checked behavior and runtime fault realization
- completed: realize the current admitted checked runtime faults through module-driven private executable helpers for divide, shift, and bounds failure on the first hosted target
- pending: ensure backend lowering preserves wraparound integer behavior where MIR already records it explicitly instead of inheriting accidental backend defaults
- pending: validate that the supported volatile and atomic MIR instructions lower to correct backend operations for the chosen target subset
- completed: add executable tests that distinguish supported checked fault behavior from silent host-default execution for the current admitted integer-width divide, shift, and bounds slice

Theme: Executable test coverage and milestone evidence
- completed: populate `tests/codegen/` with narrow end-to-end executable cases that exercise the first supported backend surface before scaling up to broader examples
- completed: add tests for ordinary procedures, globals, returns, aggregates, checked divide or shift behavior, bounds-checked slice or index behavior, and the minimal hosted entry path within the current narrow slice
- completed: add representative negative or diagnostic-oriented coverage where backend bring-up should fail crisply because a MIR form or target behavior is not yet supported
- completed: define the evidence required for the narrower repository-specific `phase5_bootstrap_finish.txt`, including green build-and-run coverage for the chosen hosted target and clear alignment with the narrower repository milestone

Overall status update:
- The narrower repository-specific bootstrap Phase 5 milestone is complete enough to support a real hosted executable path on Darwin arm64, and that narrower completion is now recorded explicitly in `phase5_bootstrap_finish.txt`.
- In practical terms, the repository is no longer blocked on backend bring-up; it already has a working LLVM path, object emission, linking, hosted entry wiring, globals and aggregate-sensitive runnable cases in the admitted slice, executable checked-fault proof for the currently admitted integer-width divide, shift, and bounds cases, and crisp-failure coverage for representative unsupported forms.
- The remaining work is no longer Phase 5 bootstrap closure. The remaining work is later-surface expansion and broader workflow or semantics work that remains outside the narrower repository milestone.
- The first widening note now lives in `docs/plan/post_phase5_checked_operation_widening.txt`.

Current ordered follow-up sequence:
1. keep the narrower admitted executable slice green under the canonical local workflow
2. widen the admitted slice only when the repository is ready to prove the new behavior with targeted executable coverage
3. keep broader backend surface, volatile or atomic lowering, wider checked-operation coverage, and broader workflow concerns explicitly outside this finished narrower milestone until they are implemented and evidenced separately

How the remaining next steps fit into the stages:
- They are post-Phase-5 widening tasks, not missing Stage 0 through Stage 6 implementation steps. The bootstrap sequence established the initial backend, inspectability, runnable executable path, executable checked/runtime proof, and the narrower finish note that now closes the repository milestone.
- Any broader backend expansion beyond that point should be treated as new admitted-slice work. In other words, more checked-operation surface, more memory-model work, and volatile or atomic lowering are valuable, but they should be understood as later widening work rather than as unfinished bootstrap closure.