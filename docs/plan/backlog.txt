Modern C - Backlog

Purpose

This file is for implementation work that is known, wanted, or repeatedly rediscovered, but is not yet committed to a specific phase doc.

Use this file for:
- themes that need follow-up work
- concrete tasks that were deferred during implementation
- recurring cleanup or refinement items
- issues that keep surfacing across compiler layers

Do not use this file for:
- unresolved language or architecture questions that belong in docs/arch/open_decisions.txt
- settled rationale that belongs in docs/arch/key_decisions.txt
- phase-specific milestone tracking that belongs in the relevant phase doc

Suggested entry shape

- status: active | deferred | parked
- area: parser | sema | mir | codegen | runtime | stdlib | tooling | docs
- task: short description of the work
- why: what limiter or pressure exposed the item
- next step: the smallest useful follow-up
- reference: phase doc, issue, commit, or decision log entry if available

Current themes

- status: active | area: docs | task: keep the Phase 119 namespace-pressure audit explicit as one fixed init-owned service-directory answer rather than as permission to add dynamic discovery, a general namespace framework, a service manager, or compiler work | why: Phase 119 now proves that the landed running kernel slice can answer the current namespace pressure with one fixed three-entry init-owned service directory over the landed log, echo, and transfer services, but later planning could still blur that result into broader service-management or compiler-surface claims if the boundary is not kept visible | next step: keep `/Users/ro/dev/c_modern/docs/plan/phase119_namespace_pressure_audit.txt`, `/Users/ro/dev/c_modern/docs/plan/admin/canopus_post_phase109_speculative_roadmap.txt`, `/Users/ro/dev/c_modern/docs/plan/admin/modern_c_canopus_readiness_position.txt`, this backlog, and later active kernel phase notes aligned so the next work stays focused on a running-system support statement, image or target truth, or other real owners rather than speculative namespace growth | reference: `/Users/ro/dev/c_modern/docs/plan/phase119_namespace_pressure_audit.txt`

- status: active | area: docs | task: keep the Phase 118 delegated request-reply follow-through explicit as one bounded retained-handle plus invalidated-source contract rather than as permission to add namespace policy, multi-child orchestration, a general service framework, or compiler work | why: Phase 118 now proves that the landed running kernel slice can publish one delegated request-derived reply plus one explicit invalidated-source refusal over the landed transfer-service owner, but later planning could still blur that result into broader service-management or compiler-surface claims if the boundary is not kept visible | next step: keep `/Users/ro/dev/c_modern/docs/plan/phase118_request_reply_and_delegation_follow_through.txt`, `/Users/ro/dev/c_modern/docs/plan/admin/canopus_post_phase109_speculative_roadmap.txt`, `/Users/ro/dev/c_modern/docs/plan/admin/modern_c_canopus_readiness_position.txt`, this backlog, and later active kernel phase notes aligned so the next work stays focused on namespace pressure, running-system support truth, image or target truth, or other real owners rather than speculative framework growth | reference: `/Users/ro/dev/c_modern/docs/plan/phase118_request_reply_and_delegation_follow_through.txt`

- status: active | area: docs | task: keep the Phase 117 init-orchestrated multi-service bring-up explicit as one bounded init-owned aggregate service set rather than as permission to add dynamic loading, namespace policy, a general service framework, or compiler work | why: Phase 117 now proves that the landed running kernel slice can publish the existing log, echo, and transfer service owners as one bounded init-orchestrated multi-service bring-up through explicit program-cap and endpoint authority, but later planning could still blur that result into broader service-management or compiler-surface claims if the boundary is not kept visible | next step: keep `/Users/ro/dev/c_modern/docs/plan/phase117_init_orchestrated_multi_service_bring_up.txt`, `/Users/ro/dev/c_modern/docs/plan/admin/canopus_post_phase109_speculative_roadmap.txt`, `/Users/ro/dev/c_modern/docs/plan/admin/modern_c_canopus_readiness_position.txt`, this backlog, and later active kernel phase notes aligned so the next work stays focused on bounded delegation, namespace pressure, image or target truth, or other real owners rather than speculative framework growth | reference: `/Users/ro/dev/c_modern/docs/plan/phase117_init_orchestrated_multi_service_bring_up.txt`

- status: active | area: docs | task: keep the Phase 116 MMU activation barrier follow-through explicit as one `hal` plus MMU publication boundary rather than as permission to widen barrier taxonomy, target-entry scope, interrupt policy, or compiler work | why: Phase 116 now proves that the landed running kernel slice can publish translation-root activation through one explicit `hal.memory_barrier()` hook owned by `kernel/src/mmu.mc`, but later planning could still blur that result into broader low-level or compiler-surface claims if the boundary is not kept visible | next step: keep `/Users/ro/dev/c_modern/docs/plan/phase116_mmu_activation_barrier_follow_through.txt`, `/Users/ro/dev/c_modern/docs/plan/admin/canopus_post_phase109_speculative_roadmap.txt`, `/Users/ro/dev/c_modern/docs/plan/admin/modern_c_canopus_readiness_position.txt`, this backlog, and later active kernel phase notes aligned so the next work stays focused on running-system growth, target truth, or other real owners rather than speculative barrier-surface expansion | reference: `/Users/ro/dev/c_modern/docs/plan/phase116_mmu_activation_barrier_follow_through.txt`

- status: active | area: docs | task: keep the Phase 115 timer ownership hardening explicit as one kernel-owned wake-delivery clarification rather than as permission to widen device policy, scheduler scope, barrier work, or compiler work | why: Phase 115 now proves that the landed running kernel slice can keep interrupt ownership at entry plus dispatch classification while routing tick advancement, wake observation, and single-delivery wake consumption through one narrow timer owner, but later planning could still blur that result into broader timer-policy or compiler-surface claims if the boundary is not kept visible | next step: keep `/Users/ro/dev/c_modern/docs/plan/phase115_timer_ownership_hardening.txt`, `/Users/ro/dev/c_modern/docs/plan/admin/canopus_post_phase109_speculative_roadmap.txt`, `/Users/ro/dev/c_modern/docs/plan/admin/modern_c_canopus_readiness_position.txt`, this backlog, and later active kernel phase notes aligned so the next work stays focused on barrier, target, or other low-level owners rather than speculative language widening | reference: `/Users/ro/dev/c_modern/docs/plan/phase115_timer_ownership_hardening.txt`

- status: active | area: docs | task: keep the Phase 114 address-space and MMU ownership split explicit as one kernel-owned translation-root clarification rather than as permission to widen VM policy, loader scope, timer hardening, or compiler work | why: Phase 114 now proves that the landed running kernel slice can separate generic mapping layout from translation-root mechanics through one narrow MMU owner without widening the admitted slice, but later planning could still blur that result into broader VM-policy or compiler-surface claims if the boundary is not kept visible | next step: keep `/Users/ro/dev/c_modern/docs/plan/phase114_address_space_and_mmu_ownership_split.txt`, `/Users/ro/dev/c_modern/docs/plan/admin/canopus_post_phase109_speculative_roadmap.txt`, `/Users/ro/dev/c_modern/docs/plan/admin/modern_c_canopus_readiness_position.txt`, this backlog, and later active kernel phase notes aligned so the next work stays focused on timer, barrier, target, or other low-level owners rather than speculative language widening | reference: `/Users/ro/dev/c_modern/docs/plan/phase114_address_space_and_mmu_ownership_split.txt`

- status: active | area: docs | task: keep the Phase 113 interrupt entry and generic dispatch boundary explicit as one kernel-owned hardware-boundary clarification rather than as permission to widen timer policy, interrupt-framework scope, MMU work, or compiler work | why: Phase 113 now proves that one landed timer-backed wake path can arrive through explicit interrupt entry and generic dispatch without widening the admitted running slice, but later planning could still blur that result into broader interrupt-policy or compiler-surface claims if the boundary is not kept visible | next step: keep `/Users/ro/dev/c_modern/docs/plan/phase113_interrupt_entry_and_generic_dispatch_boundary.txt`, `/Users/ro/dev/c_modern/docs/plan/admin/canopus_post_phase109_speculative_roadmap.txt`, `/Users/ro/dev/c_modern/docs/plan/admin/modern_c_canopus_readiness_position.txt`, this backlog, and later active kernel phase notes aligned so the next work stays focused on address-space, MMU, timer, or target owners rather than speculative language widening | reference: `/Users/ro/dev/c_modern/docs/plan/phase113_interrupt_entry_and_generic_dispatch_boundary.txt`

- status: active | area: docs | task: keep the Phase 112 syscall boundary thinness audit explicit as one kernel-owned ownership-hardening step rather than as permission to widen syscall families or reopen compiler work | why: Phase 112 now proves that the landed syscall edge can stay at decode, gate, and observation shaping while capability, endpoint, address-space, and lifecycle semantics remain with their owning modules, but later planning could still blur that result into broader syscall-surface growth or speculative compiler reopening if the boundary is not kept visible | next step: keep `/Users/ro/dev/c_modern/docs/plan/phase112_syscall_boundary_thinness_audit.txt`, `/Users/ro/dev/c_modern/docs/plan/admin/canopus_post_phase109_speculative_roadmap.txt`, `/Users/ro/dev/c_modern/docs/plan/admin/modern_c_canopus_readiness_position.txt`, this backlog, and later active kernel phase notes aligned so the next work stays focused on the next real interrupt, MMU, timer, or hardware-boundary owner rather than on speculative language widening | reference: `/Users/ro/dev/c_modern/docs/plan/phase112_syscall_boundary_thinness_audit.txt`

- status: active | area: docs | task: keep the first explicit init bootstrap-capability handoff bounded to one init-visible endpoint handle, one launchable program capability, and one no-ambient-root-authority proof instead of treating Phase 103 as real service launch, namespace policy, or broader init orchestration | why: Phase 103 now proves that one real kernel-owned artifact can boot, reach first user entry, own explicit endpoint and handle state, complete byte-only IPC, transfer one attached endpoint handle, consume one launchable program capability to spawn one child, block and wake that child once on one deterministic timer tick, reap it through one explicit wait handle, and hand `init` one explicit bounded bootstrap capability set with root authority still kernel-only, but later Canopus planning could still blur that result into a larger service-support claim if the concrete boundary is not kept visible | next step: keep `/Users/ro/dev/c_modern/docs/plan/phase103_init_bootstrap_capability_handoff.txt`, `/Users/ro/dev/c_modern/docs/plan/admin/canopus_kernel_bringup_speculative_roadmap.txt`, this backlog, and later active Canopus phase notes aligned so the next work stays focused on Phase 107 user-to-user transfer pressure rather than overclaiming init-owned service truth | reference: `/Users/ro/dev/c_modern/docs/plan/phase103_init_bootstrap_capability_handoff.txt`
- status: active | area: docs | task: keep the post-`v0.2` next-step framing explicit around a Canopus-driven `v0.3` direction rather than vague further core-language widening | why: the admitted `v0.2` core is already at a repository-bounded semantic plateau, and the first concrete upcoming consumer that changes the deferral logic for freestanding startup and `hal` is now the planned Canopus Lite bring-up rather than another hosted utility wave | next step: keep `/Users/ro/dev/c_modern/docs/plan/admin/v0_3_direction_canopus.txt`, `/Users/ro/dev/c_modern/docs/plan/admin/v0_3_speculative_roadmap.txt`, `/Users/ro/dev/c_modern/docs/plan/admin/canopus_v0_consumer_matrix.txt`, this backlog, and any later active phase note aligned so the next planning wave stays grounded in freestanding, target, and hardware-boundary proof rather than drifting into abstract feature accumulation | reference: `/Users/ro/dev/c_modern/docs/plan/admin/v0_3_direction_canopus.txt`
- status: active | area: docs | task: keep the first active Canopus-driven execution wave explicit as freestanding startup truth plus narrow `hal` admission rather than letting broader cross-compilation, `os`, or IPC ambitions outrun the first proof pair | why: the new v0.3 direction, consumer matrix, and proof ladder now identify one bounded next phase, but that phase can still blur unless the repository keeps the Phase 81 execution note, implementation checklist, and minimal target model aligned | next step: keep `/Users/ro/dev/c_modern/docs/plan/phase81_freestanding_and_hal_bootstrap_execution.txt`, `/Users/ro/dev/c_modern/docs/plan/admin/phase81_freestanding_hal_checklist.txt`, `/Users/ro/dev/c_modern/docs/plan/admin/canopus_minimal_target_model.txt`, and this backlog aligned as implementation begins | reference: `/Users/ro/dev/c_modern/docs/plan/phase81_freestanding_and_hal_bootstrap_execution.txt`
- status: active | area: tooling | task: keep the new freestanding manifest hardening slice bounded to explicit target identity plus explicit extra prebuilt link inputs while linker scripts, arbitrary linker flags, broader cross-compilation, and image packaging remain deferred | why: Phase 82 makes the first non-hosted artifact path more honest by requiring explicit freestanding `target` identity and manifest-owned `link.inputs`, but the repository still does not have a proof requiring generalized linker control or non-bootstrap target admission | next step: preserve the landed `target` plus `link.inputs` contract, reopen only when the next Canopus proof requires linker scripts, broader target-toolchain settings, or packaging behavior beyond explicit prebuilt object or archive inputs | reference: `/Users/ro/dev/c_modern/docs/plan/phase82_freestanding_artifact_and_entry_hardening.txt`
- status: active | area: docs | task: keep the freestanding support statement, the hosted-versus-freestanding split, README wording, and the focused freestanding support audit aligned with the admitted Phase 81 through Phase 93 proof slice | why: Phase 84 turned the first freestanding proofs into a published repository-bounded support statement and one repo-owned audit, and Phase 85 through Phase 93 materially widened that proof set with bounded kernel endpoint-queue, task-lifecycle, static-data follow-through, external build-integration, first user-space handshake, explicit capability-transfer checks, the first ordinary user-space helper-boundary audit, the first explicit user-space launch-and-observation proof, and the first deterministic timer-wake event-source proof, so the slice could drift back into silence or overclaim without the same maintenance hook | next step: update `/Users/ro/dev/c_modern/docs/plan/freestanding_support_statement.txt`, `/Users/ro/dev/c_modern/docs/plan/release_hardening_hosted_slice.txt`, `README.md`, and `mc_freestanding_support_audit` whenever the admitted freestanding proof slice changes materially | reference: `/Users/ro/dev/c_modern/docs/plan/phase93_timer_wake_proof.txt`
- status: active | area: docs | task: keep the first kernel build integration audit explicit as a bounded emitted-object and runtime-object relink proof instead of treating Phase 88 as general linker-script, image-packaging, or external build-system admission | why: Phase 88 now proves that one emitted freestanding entry object and generated runtime object compose honestly with one minimal external relink step, but later Canopus planning could still blur that result into a broader kernel build or packaging claim if the concrete proof is not kept visible | next step: keep `/Users/ro/dev/c_modern/docs/plan/phase88_kernel_build_integration_audit.txt`, `/Users/ro/dev/c_modern/docs/plan/admin/v0_3_speculative_roadmap.txt`, this backlog, and later active Canopus phase notes aligned so the next work stays focused on the next real user-space or build blocker rather than overclaiming a general external build workflow | reference: `/Users/ro/dev/c_modern/docs/plan/phase88_kernel_build_integration_audit.txt`
- status: active | area: docs | task: keep the first user-space policy proof explicit as one bounded init-to-log-service handshake instead of treating Phase 89 as general service management, capability-runtime admission, or namespace policy | why: Phase 89 now proves one explicit kernel-issued endpoint capability and one request-and-acknowledgment flow, but later Canopus planning could still blur that result into a broader init or service framework if the concrete proof is not kept visible | next step: keep `/Users/ro/dev/c_modern/docs/plan/phase89_init_to_log_service_handshake.txt`, `/Users/ro/dev/c_modern/docs/plan/admin/v0_3_speculative_roadmap.txt`, this backlog, and later active Canopus phase notes aligned so the next work stays focused on the next explicit user-space policy blocker rather than overclaiming a general service model | reference: `/Users/ro/dev/c_modern/docs/plan/phase89_init_to_log_service_handshake.txt`
- status: active | area: docs | task: keep the first explicit capability-transfer proof bounded to one transferred handle and one receiver-issued action instead of treating Phase 90 as general capability-table admission, ambient authority, or broader lifecycle policy | why: Phase 90 now proves one user-space component can transfer one endpoint capability to another and that the receiver can use it for one bounded acknowledged request, but later Canopus planning could still blur that result into a broader handle-runtime or service framework if the concrete proof is not kept visible | next step: keep `/Users/ro/dev/c_modern/docs/plan/phase90_capability_handle_transfer_proof.txt`, `/Users/ro/dev/c_modern/docs/plan/admin/v0_3_speculative_roadmap.txt`, this backlog, and later active Canopus phase notes aligned so the next work stays focused on the next explicit user-space policy blocker rather than overclaiming general capability management | reference: `/Users/ro/dev/c_modern/docs/plan/phase90_capability_handle_transfer_proof.txt`
- status: active | area: docs | task: keep the first ordinary user-space helper boundary bounded to one imported helper module instead of treating Phase 91 as public `sys` wrapper admission, capability-runtime growth, or general helper-framework policy | why: Phase 91 now proves that the repeated transfer and request scaffolding from the first user-space proofs can live inside one ordinary helper module, but later Canopus planning could still blur that result into a broader runtime or service framework if the concrete proof is not kept visible | next step: keep `/Users/ro/dev/c_modern/docs/plan/phase91_early_user_space_helper_boundary_audit.txt`, `/Users/ro/dev/c_modern/docs/plan/admin/v0_3_speculative_roadmap.txt`, this backlog, and later active Canopus phase notes aligned so the next work stays focused on the next explicit user-space policy blocker rather than overclaiming a public wrapper layer | reference: `/Users/ro/dev/c_modern/docs/plan/phase91_early_user_space_helper_boundary_audit.txt`
- status: active | area: docs | task: keep the first explicit user-space launch-and-observation proof bounded to one child launch, one ready path, and one wait-handle observation instead of treating Phase 92 as public spawn admission, loader-framework growth, or full process-management support | why: Phase 92 now proves that init can launch one child from one explicit program capability and observe one child exit through one explicit wait handle, but later Canopus planning could still blur that result into a broader process framework if the concrete proof is not kept visible | next step: keep `/Users/ro/dev/c_modern/docs/plan/phase92_user_space_lifecycle_follow_through.txt`, `/Users/ro/dev/c_modern/docs/plan/admin/v0_3_speculative_roadmap.txt`, this backlog, and later active Canopus phase notes aligned so the next work stays focused on the next explicit user-space policy blocker rather than overclaiming a public spawn or loader layer | reference: `/Users/ro/dev/c_modern/docs/plan/phase92_user_space_lifecycle_follow_through.txt`
- status: active | area: docs | task: keep the first event-source proof bounded to one deterministic timer-backed wake path instead of treating Phase 93 as public timer admission, general interrupt handling, or scheduler-framework growth | why: Phase 93 now proves that one explicit timer-backed wait can fire at one deterministic tick, wake one blocked worker, and publish one wake observation, but later Canopus planning could still blur that result into a broader interrupt or scheduler framework if the concrete proof is not kept visible | next step: keep `/Users/ro/dev/c_modern/docs/plan/phase93_timer_wake_proof.txt`, `/Users/ro/dev/c_modern/docs/plan/admin/v0_3_speculative_roadmap.txt`, this backlog, and later active Canopus phase notes aligned so the next work stays focused on the next first-system integration blocker rather than overclaiming a general timer or interrupt surface | reference: `/Users/ro/dev/c_modern/docs/plan/phase93_timer_wake_proof.txt`
- status: active | area: stdlib | task: decide whether the phase-1 scalar encoded `Error` surface should later widen to messages, wrapped causes, or a heap-backed object | why: the new kind/code encoding removes anonymous sentinel pressure in real programs, but it intentionally stops short of message ownership, source context, and causal chains | next step: wait for another admitted real program that still feels too opaque after kind/code classification, then design the narrowest explicit follow-up without reopening hidden propagation | reference: `/Users/ro/dev/c_modern/docs/arch/key_decisions.txt`
- Phase 8: keep the admission matrix, findings log, and blocked canonical entries explicit so deferred pressure never disappears into prose
- Phase 8: keep remaining condvar, atomic, and broader threaded canonical entries named as blocked until their owning sync surface is honest enough
- Phase 12/24/34/44/66: keep `hal` deferred until a real consumer matrix justifies it; the completed Phase 35 admission plus the completed Phase 66 matrix now leave broader `time` deferred beyond the already admitted narrow monotonic slice, the completed Phase 44 audit plus Phase 45/46 follow-through now admit one narrow hosted `os` subprocess slice for explicit argv spawn plus wait plus bounded stdin/stdout pipe composition while leaving environment APIs, cwd mutation, stderr policy, signals, and broader process control deferred, and Phase 66 now records that `testing` helper pressure is real but still secondary to filesystem and error-contract follow-through
- Post-Phase-24: keep the next-series choice explicit between sema or compiler closure, build-surface widening, release-hardening, and post-gate maintenance plateaus so repository motion stays evidence-first rather than drifting into abstract completeness work
- Phase 13: finish `mc test` timeout reporting, manifest ergonomics, and deterministic rebuild correctness without collapsing C++ fixture suites too early
- Phase 14: refresh canonical-program admission and closure work so each deferred entry has a named owner gap, proof path, or backlog item
- tighten the path from limited bootstrap solutions to fuller spec-aligned implementations
- collect repeated compiler-layer limitations before they become accidental design
- promote stable patterns from ad hoc fixes into deliberate architecture once pressure is proven

Ranked post-Phase-14 priorities

- status: active | area: codegen | task: keep executable structural aggregate const global emission bounded after the completed Phase 58 closure | why: Phase 58 now carries sema-owned structural `ConstValue` facts through MIR and the LLVM executable path for the admitted `issue_rollup` consumer, so the remaining question is no longer whether top-level aggregate const globals can emit honestly but where to stop before broader linker policy or unproved constant families are implied | next step: reopen only when another admitted executable-build consumer needs global constant support beyond the landed scalar-plus-structural slice or when broader linker behavior becomes the real owner | reference: `/Users/ro/dev/c_modern/docs/plan/phase58_structural_aggregate_const_backend_emission.txt`
- status: active | area: docs | task: keep the repository-wide next-step order explicit after the completed Phase 65 plateau decision | why: Phase 60 through Phase 65 now close the known admitted core-language ownership gaps and explicitly route the repository toward release hardening, support clarity, and adjacent-surface follow-through, so the planning surface should stop treating vague core-language closure as the default next step | next step: keep `/Users/ro/dev/c_modern/docs/plan/admin/phase57_to_phase80_speculative_roadmap.txt`, this backlog, `README.md`, and the active phase note aligned around the post-Phase-65 ordering | reference: `/Users/ro/dev/c_modern/docs/plan/phase65_language_surface_plateau_decision.txt`
- status: done | area: stdlib | task: close the filesystem and cross-module error-contract follow-through authorized by the completed Phase 66 matrix | why: Phase 67 added `fs.read_all_err(...)` and `fs.list_dir_err(...)`, preserved the coarse compatibility wrappers, and moved the admitted `file_walker` proof onto the widened listing path | next step: keep source-breaking replacement of the coarse wrappers deferred unless a later wave proves that compatibility no longer outweighs churn | reference: `/Users/ro/dev/c_modern/docs/plan/phase67_filesystem_and_error_contract_follow_through.txt`
- status: done | area: stdlib | task: close the next honest argv-shaped subprocess follow-through after the completed Phase 67 filesystem work | why: Phase 68 added `os.spawn_argv(...)`, preserved the fixed `spawn(...)` helper as a compatibility wrapper, and moved the admitted `line_filter_relay` proof onto the explicit argv path while broader `time` work still lacked a new consumer | next step: keep environment APIs, cwd mutation, stderr policy, signals, close-policy reshaping, and broader time helpers deferred until a later admitted consumer proves one of those fronts honest | reference: `/Users/ro/dev/c_modern/docs/plan/phase68_process_or_deferred_time_follow_through.txt`
- status: active | area: mir | task: keep broader generic-function executable support explicit after the completed erased-ABI imported generic closure | why: Phase 49 now proves the admitted direct-call family needed by `sync.thread_spawn<T>`, but the repository still does not claim general generic-function monomorphization or executable support for signatures whose ABI depends on unresolved type parameters | next step: reopen only when a second admitted imported generic procedure-typed consumer or another executable proof needs generic support beyond the erased-ABI family landed in Phase 49 | reference: `/Users/ro/dev/c_modern/docs/plan/phase49_imported_generic_call_closure.txt`
- status: active | area: tooling | task: keep shared libraries, external system-library links, install policy, and broader cross-target coordination explicit after the landed Phase 30 and Phase 74 hardening proofs | why: Phase 30 proved deterministic selected-target reuse and non-selected-target non-churn on the admitted richer project graph, and Phase 74 moved that guarantee onto the shipped `issue_rollup` workflow itself, so the next workflow pressure is no longer build-surface trust inside one manifest but where to stop before runtime-loaded artifacts, broader linker policy, or distribution claims | next step: keep `sharedlib`, external library link semantics, install naming, and broader cross-target coordination deferred until a real admitted project needs more than the current in-project static-library plus selected-target hardening proof | reference: `/Users/ro/dev/c_modern/docs/plan/phase74_deterministic_rebuild_and_cross_target_reuse.txt`
- status: active | area: docs | task: keep the hosted support statement, installation path, helper-slice boundary, first-use smoke path, public-cut smoke path, release-readiness audit path, final `v0.2` gate outcome, release-snapshot preparation path, and Phase 65 plateau statement aligned with the admitted repository slice | why: Phase 76 through Phase 80 showed that supported-slice wording, smoke commands, release-readiness conclusions, the final tag-or-defer statement, and the maintainer release-snapshot path can drift independently from the implemented slice, so future capability changes need documentation, support-audit, smoke-path, release-audit, final-gate, and release-snapshot updates at the same boundary instead of lingering as stale prose or hidden proof assumptions | next step: update `/Users/ro/dev/c_modern/docs/plan/release_hardening_hosted_slice.txt`, `/Users/ro/dev/c_modern/docs/plan/phase65_language_surface_plateau_decision.txt`, `README.md`, the `mc_first_use_smoke` path, the `mc_public_cut_smoke` path, the `mc_release_readiness_audit` path, the `mc_v0_2_gate` path, the `mc_release_snapshot_prep` path, and the supported-slice doc audit whenever the admitted hosted slice changes materially | reference: `/Users/ro/dev/c_modern/docs/plan/phase80_release_snapshot_preparation.txt`
- status: done | area: tooling | task: turn the maintainer-owned release-execution plateau into one concrete repo-owned release-snapshot preparation path after the completed Phase 79 gate | why: Phase 79 left the remaining blocker as operational rather than technical, but the repository still lacked one repeatable helper that reran the final gate from committed state, captured the exact reviewed commit, and printed the exact tag command without creating the tag automatically | next step: keep automatic tag creation deferred unless the repository explicitly decides to let automation mutate git refs later; otherwise keep the helper, docs, and audit aligned as the supported maintainer path | reference: `/Users/ro/dev/c_modern/docs/plan/phase80_release_snapshot_preparation.txt`
- status: done | area: stdlib | task: close the narrow pipe-oriented subprocess helper follow-through after the completed Phase 70 gate | why: the admitted real `line_filter_relay` proof and the focused stdlib subprocess smoke were both repeating the same one-input-pipe plus one-output-pipe `spawn_argv(...)` close-slot pairing, so Phase 71 added `os.spawn_pipe_argv(...)` as the owner for that admitted relay shape without widening broader process policy | next step: keep environment APIs, cwd mutation, stderr policy, signals, multi-pipe subprocess plumbing, and broader child-lifecycle redesign deferred until a later admitted consumer proves one of those fronts honest | reference: `/Users/ro/dev/c_modern/docs/plan/phase71_process_pipe_helper_follow_through.txt`
- status: done | area: stdlib | task: close the bounded merged child-diagnostics subprocess follow-through after the completed Phase 71 relay-helper wave | why: the admitted subprocess slice could relay stdin and stdout honestly, but a failing child still exposed only status unless callers reopened broader stderr policy, so Phase 72 added `os.spawn_pipe_argv_merged_output(...)` as the narrow owner for merged diagnostics on the admitted pipe-relay shape and proved it through one focused stdlib smoke plus one ordinary test in `line_filter_relay` | next step: keep separate stderr routing, multi-pipe child plumbing, environment APIs, cwd mutation, signals, and broader process lifecycle redesign deferred until a later admitted consumer proves one of those fronts honest | reference: `/Users/ro/dev/c_modern/docs/plan/phase72_process_diagnostics_capture_follow_through.txt`
- status: done | area: stdlib | task: close the bounded split stdout and stderr subprocess follow-through after the completed merged-diagnostics wave | why: the admitted subprocess slice could already expose merged diagnostics, but some consumers still need to distinguish normal stdout from child stderr without reopening broader process policy, so Phase 73 added `os.spawn_pipe_argv_split_output(...)` for the admitted relay family and proved it through one focused stdlib smoke plus one ordinary test in `line_filter_relay` | next step: keep arbitrary file-descriptor routing, multi-pipe process topologies, environment APIs, cwd mutation, signals, and broader lifecycle redesign deferred until a later admitted consumer proves one of those fronts honest | reference: `/Users/ro/dev/c_modern/docs/plan/phase73_process_split_stderr_follow_through.txt`
- status: done | area: stdlib | task: close the next ordinary-test helper hardening wave after the completed Phase 67 and Phase 68 adjacent follow-through | why: Phase 69 added additive `testing.expect_non_nil<T>(...)`, `testing.expect_buffer_non_nil<T>(...)`, `testing.expect_nil<T>(...)`, `testing.expect_ok(...)`, and `testing.expect_err(...)` helpers, then moved representative admitted real project tests onto the widened nil-pointer and classified-error paths | next step: keep fixture frameworks, output capture helpers, timeout helpers, and broader test-context machinery deferred unless repeated admitted project pressure proves one of those fronts honest later | reference: `/Users/ro/dev/c_modern/docs/plan/phase69_testing_surface_hardening.txt`
- status: active | area: codegen | task: keep the admitted narrow sync slice bounded to typed thread spawn or join, mutex, condvar wait or signal or broadcast, and atomic load or store publication while remaining atomic breadth stays explicit follow-on work | why: Phase 70 re-audited the admitted real sync consumers and found no current public pressure for compare-exchange, exchange, or fetch-add beyond the already landed hosted slice, even though condvar broadcast is already implemented | next step: reopen only when an admitted real project needs public atomic read-modify-write or another broader coordination boundary rather than mutex or condvar or load or store publication | reference: `/Users/ro/dev/c_modern/docs/plan/phase70_sync_and_mem_follow_on_gate.txt`
- status: active | area: stdlib | task: keep broader mem API widening explicit after the completed Phase 70 gate | why: the admitted real mem consumers still stop at explicit `Buffer<u8>` ownership, `slice_from_buffer<u8>(...)`, and arena init or new or deinit, so Phase 70 found no honest current pressure for generic buffer metadata helpers, allocator-adapter APIs, or richer arena policy | next step: reopen only when an admitted real project needs public mem behavior beyond the landed `u8`-buffer and arena slice | reference: `/Users/ro/dev/c_modern/docs/plan/phase70_sync_and_mem_follow_on_gate.txt`
- status: active | area: stdlib | task: keep the admitted `io.pipe()` slice narrow while non-blocking pipe policy, subprocess piping, and richer IPC abstractions remain deferred | why: Phase 43 now admits a direct handle-first blocking pipe proof through `examples/real/pipe_handoff/`, but the repository still does not have a second consumer justifying poller-coupled pipe semantics, half-close rules, subprocess stdio wiring, or channel-like policy surfaces | next step: preserve the current blocking `io.Pipe` plus `io.pipe()` proof and reopen only when an admitted project needs behavior beyond the landed handoff slice | reference: `/Users/ro/dev/c_modern/docs/plan/phase43_io_pipe_and_handle_first_sync_reframing.txt`
- status: active | area: stdlib | task: keep the admitted `io.pipe()` plus `io.poller_*` composition slice narrow while richer non-blocking policy, half-close semantics, subprocess piping, and IPC abstractions remain deferred | why: the repository now has both a blocking `pipe_handoff` proof and a second `pipe_ready` readiness-based proof, so the next remaining pipe follow-on pressure is no longer whether readiness composes but where to stop before broader policy claims | next step: preserve the current blocking and readiness-based pipe proof pair and reopen only when an admitted project needs behavior beyond those bounded slices | reference: `/Users/ro/dev/c_modern/docs/plan/phase43_io_pipe_and_handle_first_sync_reframing.txt`
