Modern C - Backlog

Purpose

This file is for implementation work that is known, wanted, or repeatedly rediscovered, but is not yet committed to a specific phase doc.

Use this file for:
- themes that need follow-up work
- concrete tasks that were deferred during implementation
- recurring cleanup or refinement items
- issues that keep surfacing across compiler layers

Do not use this file for:
- unresolved language or architecture questions that belong in docs/arch/open_decisions.txt
- settled rationale that belongs in docs/arch/key_decisions.txt
- phase-specific milestone tracking that belongs in the relevant phase doc

Suggested entry shape

- status: active | deferred | parked
- area: parser | sema | mir | codegen | runtime | stdlib | tooling | docs
- task: short description of the work
- why: what limiter or pressure exposed the item
- next step: the smallest useful follow-up
- reference: phase doc, issue, commit, or decision log entry if available

Current themes

- Phase 8: keep the admission matrix, findings log, and blocked canonical entries explicit so deferred pressure never disappears into prose
- Phase 8: keep remaining condvar, atomic, and broader threaded canonical entries named as blocked until their owning sync surface is honest enough
- Phase 12: admit `time`, `os`, `sync`, `hal`, and `testing` only when a real consumer matrix and stated API surface justify them
- Phase 13: finish `mc test` timeout reporting, manifest ergonomics, and deterministic rebuild correctness without collapsing C++ fixture suites too early
- Phase 14: refresh canonical-program admission and closure work so each deferred entry has a named owner gap, proof path, or backlog item
- tighten the path from limited bootstrap solutions to fuller spec-aligned implementations
- collect repeated compiler-layer limitations before they become accidental design
- promote stable patterns from ad hoc fixes into deliberate architecture once pressure is proven

Ranked post-Phase-14 priorities

- status: active | area: docs | task: keep the repository-wide next-step order explicit after the landed networking and first sync waves | why: the old ranked plan still described networking as next and sync as queued even after both repository-bounded admissions landed, which would hide the real follow-up work | next step: keep `/Users/ro/dev/c_modern/docs/plan/admin/post_phase14_execution_plan.txt`, this backlog, and the active Phase 12 note aligned around compiler debt closeout before more capability breadth | reference: `/Users/ro/dev/c_modern/docs/plan/admin/post_phase14_execution_plan.txt`
- status: active | area: mir | task: support direct local/global field-address lowering so canonical proofs no longer need global-shaped rewrites for `&value.field` | why: the shared-counter sync admission exposed that MIR could store to fields but could not take the address of a direct aggregate field, forcing the proof to flatten state awkwardly | next step: keep executable coverage for direct `&(local.field)` and extend only when a real consumer needs broader nested pointer-field addressability | reference: `/Users/ro/dev/c_modern/compiler/mir/mir.cpp`
- status: active | area: mir | task: keep the restored typed imported generic `thread_spawn<T>(entry: func(*T), ctx: *T)` surface honest while broader imported generic procedure-call lowering remains deferred | why: the current compiler-owned rewrite now preserves the public typed sync API, but it is still a bounded bootstrap solution rather than full general imported generic call support | next step: preserve focused sema/MIR coverage for the typed rewrite and reopen broader imported generic procedure-call lowering only when a second real consumer needs it | reference: `/Users/ro/dev/c_modern/docs/plan/decision_log.txt`
- status: active | area: tooling | task: continue Phase 13 trust and reporting hardening on the richer networking project fixtures | why: Phase 18 chose workflow maturity as the next plateau because the admitted evented-echo and evented-partial-write projects now expose manifest ergonomics, deterministic rebuild correctness, and `mc test` sharpness more honestly than a third capability family would | next step: keep adding narrow run-test-rerun and reporting regressions on the existing networking fixtures before reopening `sync`, `hal`, or release-hardening breadth | reference: `/Users/ro/dev/c_modern/docs/plan/phase18_workflow_hardening_and_plateau_decision.txt`
- status: active | area: tooling | task: keep the admitted evented-echo and evented-partial-write `mc run` and `mc test` workflow proofs deterministic as the networking project surface grows | why: the hosted networking family now has multiple real project fixtures, so graph, rebuild, and reporting friction should be caught there before more examples accumulate | next step: preserve run-test-rerun build-directory reuse on the richer networking fixtures instead of adding ad hoc one-off harnesses when new workflow pressure appears | reference: `/Users/ro/dev/c_modern/docs/plan/phase18_workflow_hardening_and_plateau_decision.txt`
- status: active | area: docs | task: keep the hosted support statement, installation path, checked-vs-release note, and known limitations aligned with the admitted repository slice | why: the release-hardening note now exists, so future capability changes need documentation updates at the same boundary instead of drifting into stale README prose | next step: update `/Users/ro/dev/c_modern/docs/plan/release_hardening_hosted_slice.txt` and `README.md` whenever the supported hosted slice changes materially | reference: `/Users/ro/dev/c_modern/docs/plan/release_hardening_hosted_slice.txt`
- status: active | area: codegen | task: keep the admitted narrow sync slice bounded to typed thread spawn or join, mutex, condvar, and atomic load or store publication while remaining atomic breadth stays explicit follow-on work | why: the shared-counter, producer-consumer, and atomic-publication proofs are now landed, but the hosted `sync` admission should still not quietly widen into a larger concurrency grab bag | next step: keep the current publication slice narrow and route any compare-exchange, fetch-add, broadcast, or destroy-surface follow-up through named owner docs instead of expanding `sync` opportunistically | reference: `/Users/ro/dev/c_modern/docs/plan/phase12_stdlib_runtime_maturity.txt`
