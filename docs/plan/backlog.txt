Modern C - Backlog

Purpose

This file is for implementation work that is known, wanted, or repeatedly rediscovered, but is not yet committed to a specific phase doc.

Use this file for:
- themes that need follow-up work
- concrete tasks that were deferred during implementation
- recurring cleanup or refinement items
- issues that keep surfacing across compiler layers

Do not use this file for:
- unresolved language or architecture questions that belong in docs/arch/open_decisions.txt
- settled rationale that belongs in docs/arch/key_decisions.txt
- phase-specific milestone tracking that belongs in the relevant phase doc

Suggested entry shape

- status: active | deferred | parked
- area: parser | sema | mir | codegen | runtime | stdlib | tooling | docs
- task: short description of the work
- why: what limiter or pressure exposed the item
- next step: the smallest useful follow-up
- reference: phase doc, issue, commit, or decision log entry if available

Current themes

- status: active | area: kernel + examples | task: keep the first rendering answer bounded to one fixed four-cell visible contract plus app-local render policy rather than widening display into text layout, launcher into presentation policy, or hosted output into a second rendering family | why: Phase 278 closes the first readable rendering path by reusing the existing four-byte visible cell payload already carried by `display_surface.mc` and `issue_rollup_app.mc`, and by aligning the hosted `issue_rollup` output path to the same `EMTY`/`STDY`/`BUSY`/`ATTN` contract; current evidence still does not justify fonts, layout engines, widget libraries, composition, or a separate hosted presentation system | next step: reopen only when a second real app or second rendering need cannot stay honest on the current fixed four-cell contract and the narrowest repair is more than app-local render-policy follow-through | reference: docs/plan/active/phase278_fixed_font_text_rendering_or_cell_rendering_path.txt, kernel/src/services/display_surface.mc, kernel/src/issue_rollup_app.mc, examples/real/issue_rollup/src/render/rollup_render.mc, examples/real/issue_rollup/src/render/rollup_render_table.mc

- status: active | area: kernel | task: keep the post-Phase-273 app path bounded to one admitted `issue_rollup` consumer plus launcher-owned manifest and status queries rather than widening launcher or update-store into a package API, UI shell, status registry, or second orchestrator | why: Phase 271 admitted one bounded `issue_rollup`-only manifest family carried on installed bytes and exposed through `launcher_service`, Phase 272 spent that path on one explicit installed-workflow demo with one compact launcher-visible status surface `(installed program, installed version, foreground program, visible state)`, and Phase 273 then re-audited Band C and confirmed that the current evidence still does not justify a filesystem, second-app install policy, broader package metadata, launcher-owned durable state, shell dashboards, or real process-execution semantics | next step: reopen only when a second real repository-owned installed program appears, when callers need richer app-facing status than the current compact launcher query can carry honestly, when a materially different asset or manifest family cannot stay on the current `issue_rollup` path, or when broader app execution semantics become a stronger blocker than the first UI band | reference: docs/plan/active/phase271_app_asset_and_manifest_follow_through.txt, docs/plan/active/phase272_first_user_facing_installed_workflow_demo.txt, docs/plan/active/phase273_app_substrate_pressure_audit.txt, kernel/src/services/launcher_service.mc, kernel/src/services/update_store_service.mc, examples/real/issue_rollup/src/render/rollup_manifest.mc, tests/system/kernel_reset_lane_phase272_installed_workflow_demo/src/main.mc

- status: active | area: kernel | task: keep the first display answer bounded to one fixed `display_surface` owner plus one launch-time foreground handoff rather than widening launcher into a compositor, shell into a dashboard, or the kernel into an app-render policy owner | why: Phase 275 lands the first honest visible target as one four-cell surface with explicit present/query semantics, uses `program_catalog` for tiny foreground display tokens, and keeps `launcher_service` on single-foreground truth only; current evidence still does not justify repaint scheduling, real app render payloads, multi-surface regions, shell-visible dashboards, or composition frameworks | next step: reopen only when a real admitted consumer needs repaint or app-driven present payloads that cannot stay honest on the current fixed foreground-token path, or when multiple visible regions/surfaces become a stronger blocker than the Phase 276 foreground input-routing follow-through | reference: docs/plan/active/phase275_display_surface_owner_first_slice.txt, kernel/src/services/display_surface.mc, kernel/src/kernel_dispatch.mc, kernel/src/identity/program_catalog.mc

- status: active | area: kernel | task: keep foreground app input routing bounded to one launcher-owned foreground decision plus one explicit routing seam rather than widening into focus stacks, background delivery, app registries, or session frameworks | why: Phase 276 closes the first UI path by keeping `input_event.mc` on admitted event-shape parsing, keeping `launcher_service.mc` on single-foreground truth, and adding one explicit `foreground_input_route.mc` seam that routes the admitted key-event family to exactly one current foreground app or one explicit no-foreground/unsupported outcome; current evidence still does not justify background app delivery, task switching, a focus stack, subscriptions, or routing persistence beyond the existing launcher foreground byte | next step: reopen only when a real admitted consumer needs a second simultaneous app target, richer focus policy than the current single-foreground launcher truth, or a routed input family that cannot stay honest on the current parsed-event plus launcher-foreground plus one-route seam split; otherwise prefer the next repaint/present follow-through over broader routing work | reference: docs/plan/active/phase276_foreground_app_input_routing.txt, kernel/src/input_event.mc, kernel/src/foreground_input_route.mc, kernel/src/services/launcher_service.mc

- status: active | area: kernel + examples | task: keep the first human-facing app proof bounded to one app-local `issue_rollup` interaction reducer plus the admitted four-cell surface rather than widening into text-entry frameworks, widgets, or a shared app toolkit | why: Phase 279 closes the first UI outcome checkpoint by moving direct interaction into `examples/real/issue_rollup/src/app/issue_rollup_interactive.mc`, where one bounded key vocabulary appends issue lines and reuses the existing parse-plus-render owners to drive `EMTY`/`STDY`/`BUSY`/`ATTN` on the admitted display surface; current evidence still does not justify richer editing controls, a control library, shared UI state owners, or a second app proving a broader abstraction | next step: reopen only when the first app needs richer interaction than the current bounded key vocabulary can carry honestly, when a second real app appears with materially different UI pressure, or when repeated app-local interaction code proves one narrower shared owner than the current app-local reducer | reference: docs/plan/active/phase279_first_human_facing_app_proof.txt, examples/real/issue_rollup/src/app/issue_rollup_interactive.mc, kernel/src/issue_rollup_app.mc

- status: active | area: kernel + examples | task: keep post-Phase-289 UI growth bounded to the frozen basic-UI contract rather than widening into a desktop shell, shared toolkit, session framework, or app platform | why: Phase 281 closed the first UI lane, Phases 282 through 285 kept composition, alternate input, focus, and control pressure bounded, Phase 286 then admitted `review_board` as the second directly usable UI proof, Phase 287 added one bounded journal-backed `review_board` view-state slice, Phase 288 documented the canonical `mc` and grouped-test workflow for the admitted UI programs, and Phase 289 then froze the resulting Band E truth: one launcher-selected foreground app at a time on one fixed four-cell surface, one optional two-region composition seam, one key-only input family, app-local render and control semantics, two directly usable app proofs (`issue_rollup` and `review_board`), and one bounded persisted view-state slice | next step: reopen only when a real admitted consumer needs more than that frozen contract and can name the narrowest stronger owner, such as a second input family, broader composition, shared controls, richer persistence, or a broader platform-direction step that still stays narrower than a desktop or toolkit framework | reference: docs/plan/active/phase281_first_ui_slice_audit.txt, docs/plan/active/phase282_surface_composition_or_region_owner_first_slice.txt, docs/plan/active/phase283_pointer_or_alternate_input_first_slice.txt, docs/plan/active/phase284_selection_and_focus_follow_through.txt, docs/plan/active/phase285_small_control_vocabulary_audit.txt, docs/plan/active/phase286_first_editor_or_installer_style_ui_proof.txt, docs/plan/active/phase287_document_or_view_state_persistence_follow_through.txt, docs/plan/active/phase288_ui_tooling_and_build_path_audit.txt, docs/plan/active/phase289_second_ui_slice_freeze.txt, kernel/src/input_event.mc, kernel/src/foreground_input_route.mc, kernel/src/services/display_surface.mc, kernel/src/services/surface_composition.mc, kernel/src/issue_rollup_app.mc, kernel/src/review_board_app.mc, examples/real/review_board/src/review_board_ui.mc

- status: active | area: compiler | task: keep error-surface follow-through owner-local and additive after Phases 222, 223, 223A, 223B, and 223C by preferring note-level, formatting, and bounded origin-truth repairs before broad diagnostic-engine expansion | why: Phase 222 landed additive target-selection notes in `compiler/driver/project.cpp`, Phase 223 landed builder-owned unsupported-bootstrap-target notes in `compiler/driver/builder.cpp`, Phase 223A landed matching early project-validation notes for missing freestanding `target` declarations in `compiler/driver/project.cpp`, Phase 223B repaired one coarse project-validation origin by reporting duplicate module-set file ownership at the later conflicting `module_sets.<name>.files` manifest line instead of `build.toml:1:1`, and Phase 223C repaired one compiler-manifest parser failure by keeping `unknown compiler regression kind` local to `compiler/driver/driver.cpp` and adding an accepted-kinds note instead of redesigning the manifest system; all five fixes improved real compiler-visible failures without changing the diagnostic model, and current evidence still does not justify categories, fix-it engines, localization, IDE-protocol redesign, provenance frameworks, manifest-schema frameworks, or reopening non-compiler error surfaces | next step: reopen only when another concrete compiler truth gap cannot be solved by owner-local additive diagnostics or bounded path/span truth repairs similar in scope to the landed project, builder, project-validation note, module-set origin, and compiler-manifest accepted-kinds repairs | reference: docs/plan/active/phase222_error_surface_pressure_audit.txt, docs/plan/active/phase223_compiler_diagnostic_truth_repair_follow_through.txt, docs/plan/active/phase223a_project_validation_diagnostic_note_follow_through.txt, docs/plan/active/phase223b_support_layer_diagnostic_origin_truth_repair.txt, docs/plan/active/phase223c_compiler_manifest_diagnostic_formatting_repair.txt, compiler/driver/project.cpp, compiler/driver/builder.cpp, compiler/driver/driver.cpp

- status: deferred | area: stdlib | task: keep source-level `errors.Error` widening deferred after Phase 222 unless a production consumer proves kind/code plus local context is insufficient | why: Phase 222 re-audited admitted real programs and found no new production blockage beyond binary `is_ok()` or `is_err()` control flow; widening now would be speculative | next step: reopen only when a production call site cannot decide user output or recovery path from kind/code plus local context, and the narrowest honest fix is bounded optional message/cause data rather than error-object frameworks | reference: docs/plan/active/phase222_error_surface_pressure_audit.txt, docs/plan/archive/phase175_error_model_widening_first_gate.txt

- status: deferred | area: kernel | task: keep compact status/classifier reporting bounded after Phase 222 until a caller-visible recovery gap is proven | why: Phase 222 re-audited compact status surfaces and found current status-plus-classifier payloads still actionable on admitted workflows | next step: reopen only when an admitted caller cannot recover or present enough truth from `SyscallStatus` plus compact payload/classifier bytes on the existing protocol path | reference: docs/plan/active/phase222_error_surface_pressure_audit.txt, kernel/src/shell/shell_service.mc

- status: active | area: kernel | task: keep kernel_dispatch.mc on the post-Phase-224 narrow-routing boundary rather than letting later lifecycle, retained-state, or service pressure drift back into it | why: Phase 224 re-audited the dispatch owner and moved shell lifecycle command decode into `kernel/src/shell/shell_service.mc` and boot-state lifecycle projection into `kernel/src/boot/boot_identity.mc`, reasserting that dispatch should stay at endpoint lookup, bounded control execution, routing, and effect wiring; future service growth can still regress this boundary if new shell vocabulary or boot-state summary logic is added directly to dispatch by convenience | next step: when later phases touch `kernel_dispatch.mc`, first ask whether the branch is routing or effect wiring; if it is shell protocol decode, move it to shell; if it is restart or retained projection, move it to boot or init; if it is service-local validation or consequence logic, move it to the service owner; reopen Phase 224 only when one concrete branch still cannot be owned honestly without a new nearby helper | reference: docs/plan/active/phase224_kernel_dispatch_ownership_reassertion.txt, kernel/src/kernel_dispatch.mc, kernel/src/shell/shell_service.mc, kernel/src/boot/boot_identity.mc

- status: active | area: kernel | task: keep source tree grouping bounded to the Phase 219 layout until the next wave of services proves a new subdirectory is needed | why: Phase 219 introduced boot/, identity/, services/, shell/, scenarios/, transport/, and debug/ to absorb the next 10–20 files without root noise; current evidence does not justify further nesting or additional groups | next step: add new service files to services/, new scenarios to scenarios/, new boot/lifecycle helpers to boot/; open a new subdirectory only when an admitted module family genuinely does not fit an existing group | reference: docs/plan/active/phase219_kernel_src_tree_restructure.txt, kernel/src/

- status: active | area: kernel | task: keep queue observability bounded to count and peek inside `queue_service` rather than widening into mailbox or iteration frameworks | why: Phase 196 closes the real user-facing visibility gap on the retained queue owner with one non-mutating count path and one non-mutating peek path, both still owned by the queue service and routed through the existing compact shell family; current evidence still does not justify subscriptions, queue dumps, blocking receive, or a general observability framework | next step: reopen only when a real admitted consumer needs queue iteration, notifications, blocking dequeue semantics, or richer retained-queue policy that cannot stay inside the current fixed-capacity FIFO owner | reference: docs/plan/active/phase196_queue_observability_and_peek_follow_through.txt, kernel/src/services/queue_service.mc

- status: active | area: kernel | task: keep restart-visible user-issued state local to `ticket_service` rather than widening into a session or capability framework | why: Phase 188 proved the narrowest honest user-facing restart boundary: one bounded reset-style owner issues epoch-tagged tickets, explicit restart invalidates them while the public endpoint stays stable, and callers must handle stale state directly; current evidence still does not justify sessions, authentication, transferred tickets, or supervisor policy | next step: reopen only when a real admitted consumer needs ticket transfer, richer per-ticket metadata, multi-service session continuity, or another restart-visible user-state boundary that cannot stay local to one owner | reference: docs/plan/active/phase188_restart_visible_ticket_service_follow_through.txt, kernel/src/ticket_service.mc

- status: active | area: kernel | task: keep retained-state reload explicit and init-owned as retained workflows begin to span more than one owner | why: Phase 179 proved the explicit save-and-reload shape on `log_service`, Phase 181 extended it to `kv_service`, Phase 187 added `queue_service`, and Phase 197 showed one bounded coordinated retained workset (`kv_service` plus `queue_service`) can still stay explicit on the existing shell lifecycle and init-owned restart path; current evidence still does not justify a generic persistence framework, snapshot registry, or durability layer | next step: reopen only when a second coordinated retained bundle is needed, when restart coordination requires dynamic dependency policy rather than one flat init-owned helper, when a fourth admitted retained owner makes snapshot plumbing more distortive than honest, or when a real consumer needs snapshot lifetime beyond the immediate restart call | reference: docs/plan/active/phase179_bounded_retained_state_reload_probe.txt, docs/plan/active/phase181_key_value_retained_state_reload_follow_through.txt, docs/plan/active/phase187_retained_queue_service_follow_through.txt, docs/plan/active/phase197_bounded_cross_service_retained_restart_coordination_probe.txt
- status: active | area: kernel | task: keep coordinated retained identity bounded to one explicit workset marker rather than widening into bundle frameworks | why: Phase 200 adds the narrowest caller-visible identity for the Phase 197 retained workset: one boot-owned `workset_generation` counter queried through `XIW!`, advanced only by `XRW!`, and left unchanged by individual owner restarts; current evidence still does not justify synthetic bundle discovery, richer identity payload bundles, or a registry of retained relationships | next step: reopen only when a second coordinated retained bundle appears, when callers need richer coordinated identity than one generation counter, or when workset identity needs to compose dynamically rather than through one boot-owned explicit counter | reference: docs/plan/active/phase200_bounded_coordinated_retained_workset_identity_query_follow_through.txt, kernel/src/boot.mc, kernel/src/kernel_dispatch.mc
- status: active | area: kernel | task: keep second-bundle retained coordination explicit and flat rather than widening into a retained-bundle framework | why: Phase 201 adds a second explicit coordinated retained pair on the existing shell lifecycle path, `kv_service` plus `log_service`, because the repository already had a real cross-owner audit relationship through kv-write advisory log markers; the implementation still fits one additive shell target plus one init-owned helper, so current evidence still does not justify a retained-bundle registry, dynamic dependency graph, or generic coordination policy | next step: reopen only when a third coordinated retained bundle appears, when two explicit helper paths in `init.mc` become more distortive than honest, or when clients need coordinated retained identity or discovery across more than one bundle rather than one fixed target at a time | reference: docs/plan/active/phase201_bounded_second_coordinated_retained_audit_lane_probe.txt, kernel/src/init.mc, kernel/src/kernel_dispatch.mc, kernel/src/shell_service.mc
- status: active | area: kernel | task: keep retained summary bounded to one compact shell-visible query rather than widening into metadata frameworks or dashboards | why: Phase 206 adds the narrowest honest inspection surface over the already-admitted retained model: one `XS<target>!` summary on the existing lifecycle control path, one boot-owned generation marker per retained lane, and one init-owned last-restart outcome classification for restartable owners; current evidence still does not justify bundle discovery, richer metadata payloads, persistence descriptors, or operator dashboards | next step: reopen only when a real admitted consumer needs more retained metadata than the current participation plus outcome plus generation marker contract can carry, when another coordinated retained lane makes fixed-target summary routing distortive, or when the compact generation marker stops being an honest summary surface | reference: docs/plan/active/phase206_retained_service_summary_and_restart_outcome_inspection.txt, kernel/src/kernel_dispatch.mc, kernel/src/boot_identity.mc
- status: active | area: kernel | task: keep coordinated retained policy inspection bounded to one compact fixed-target query rather than widening into discovery, dashboards, or a retained-bundle framework | why: Phase 208 makes the pre-restart coordination truth explicit through one `XP<target>!` shell-visible query that reports the current fixed target, up to two participating owners, and one keep-versus-clear policy byte; the two admitted coordinated retained lanes and the current single-owner restartable services still fit that flat owner-local contract honestly, so current evidence still does not justify registries, variable-length metadata payloads, or dynamic dependency policy | next step: reopen only when a third coordinated retained lane appears, when a target needs more than two participating owners, when callers need richer retained policy metadata than the current four-byte contract can carry, or when fixed-target routing stops being the honest owner boundary | reference: docs/plan/active/phase208_explicit_retained_coordination_comparison_and_policy_probe.txt, kernel/src/init.mc, kernel/src/kernel_dispatch.mc, kernel/src/shell_service.mc
- status: active | area: kernel | task: keep authority inspection bounded to one compact fixed-target shell query rather than widening into capability objects, discovery, or dashboards | why: Phase 212 makes the Phase 209 authority vocabulary executable through one `XA<target>!` shell-visible query that reports the current fixed target, authority class, transfer-bearing flag, and retained-versus-coordinated scope; the admitted authority surfaces still fit that owner-local compact contract honestly, so current evidence still does not justify registries, dynamic policy metadata, ACLs, or a second system model | next step: reopen only when a real admitted consumer needs richer authority metadata than the current class plus transfer plus scope contract can carry, when a target no longer fits the current fixed target set, or when authority inspection can no longer stay on the existing shell control family honestly | reference: docs/plan/active/phase212_authority_inspection_follow_through.txt, kernel/src/service_topology.mc, kernel/src/kernel_dispatch.mc
- status: active | area: kernel | task: keep restartable-service identity taxonomy shared and bounded to the current admitted identity classes rather than widening into registries, durable naming, or compiler-owned identity machinery | why: Phase 213 gathered the current stable public name, per-service generation, coordinated lane generation, restart-outcome, and retained-summary identity truths into one reusable taxonomy and one runtime-owned target-classification helper; current evidence still does not justify dynamic object ids, discovery registries, persistence naming, or compiler-known identity features | next step: reopen only when a real admitted target or workflow no longer fits the current taxonomy classes, when audit-lane identity needs a direct public generation query rather than the current derived inspection surfaces, or when a future phase intentionally changes the identity model rather than reusing the current taxonomy | reference: docs/plan/active/phase213_unified_identity_taxonomy_for_restartable_services.txt, docs/arch/veya/veya_restartable_service_identity_taxonomy_v0.txt, kernel/src/identity_taxonomy.mc
- status: active | area: kernel | task: keep compact service-state inspection bounded to one fixed-target shell query rather than widening into discovery, dashboards, or a second system model | why: Phase 214 composes lifecycle class, restart mode, retained participation, retained policy, bounded generation marker, and already-admitted local count metadata into one compact `XC<target>!` query over the existing shell control family; the truthful owners still fit that fixed-target compact contract honestly, so current evidence still does not justify a registry, dynamic metadata store, richer operator dashboard, or reopened compiler work | next step: reopen only when a real admitted consumer needs more state metadata than the current four-byte contract can carry, when fixed targets stop being the honest owner boundary, or when the bounded generation marker stops being sufficient for the compact state summary | reference: docs/plan/active/phase214_system_introspection_surface_for_service_state.txt, kernel/src/service_state.mc, kernel/src/kernel_dispatch.mc

- status: active | area: kernel | task: keep retained-versus-durable state classification explicit on the fixed-target shell lifecycle path rather than widening into storage frameworks | why: Phase 217 adds one compact `XD<target>!` boundary query over existing shell control ownership so retained state, durable non-admission, and durable requirements remain explicit and executable; current evidence still does not justify filesystem mechanisms, durable logs, mountable namespaces, naming frameworks, or compiler/runtime expansion into persistence | next step: reopen only when a real admitted consumer needs durable behavior beyond explicit retained reload and can prove the narrowest next owner is durable persistence (with explicit naming and recovery semantics) rather than another retained-state clarification | reference: docs/plan/active/phase217_retained_versus_durable_state_boundary.txt, kernel/src/service_state.mc, kernel/src/kernel_dispatch.mc

- status: active | area: kernel | task: keep durable named-state, staged-update truth, and replay bounded to the explicit `journal_service`, `object_store_service`, and `update_store_service` owners plus explicit boot/init reload rather than widening into a filesystem, persistence registry, or storage framework | why: Phase 230 admitted one durable replay owner with fixed lanes and explicit reload, Phase 234 admitted one second durable owner with fixed-capacity named slots plus explicit create/read/replace and owner-local artifact reload, and Phase 242 admitted one product-shaped staged-update owner with one compact manifest plus one staged artifact slot and explicit durable reload; the repository now proves durable replay, generic durable named data, and bounded staged-update truth on the live reset-lane kernel without needing directories, delete/truncate/seek semantics, general naming graphs, or durable-owner coordination frameworks | next step: reopen only when a fourth admitted durable owner appears, when durable naming or recovery can no longer stay owner-local across the current three owners, or when a real admitted consumer needs filesystem-scale behavior or richer update storage semantics that fixed-lane journal replay, fixed-capacity named slots, and the single-slot staged update owner cannot carry honestly | reference: docs/plan/active/phase230_durable_journal_service_first_pressure_step.txt, docs/plan/active/phase234_durable_named_object_store_first_slice.txt, docs/plan/active/phase242_update_manifest_and_staged_artifact_store_first_slice.txt, kernel/src/services/journal_service.mc, kernel/src/services/object_store_service.mc, kernel/src/services/update_store_service.mc

- status: active | area: kernel | task: keep restart-safe update application bounded to one workflow-owned apply lane plus one update-store-owned applied target rather than widening into an installer, rollback engine, dependency graph, or generic activation framework | why: Phase 243 closed the first honest product-shaped apply path, Phase 244 added one bounded delegated installer-authority gate, and Phase 245 proved that the same retained update lane remains honest under mailbox-full deferred reporting and workflow restart; current evidence still does not justify package queues, broader installer policy, rollback, richer activation machinery, or a second update execution framework | next step: reopen only when a real admitted apply workload needs a second materially different apply lane, richer manifest truth than the current version/length contract, reporting metadata that no longer fits the current workflow-query plus completion contract, or activation behavior that cannot stay owner-local to `update_store_service` plus the existing workflow substrate | reference: docs/plan/active/phase243_restart_safe_update_apply_workflow.txt, docs/plan/active/phase244_delegated_installer_authority_probe.txt, docs/plan/active/phase245_update_recovery_and_completion_reporting_pressure.txt, kernel/src/services/update_store_service.mc, kernel/src/services/workflow/service.mc

- status: active | area: kernel | task: keep completion delivery bounded to one retained completion mailbox owner plus workflow-local retry state rather than widening into a generic mailbox, queue framework, broker, or scheduler policy | why: Phase 232 made workflow completion pressure operational with one retained `completion_mailbox_service` owner and one workflow-local `WORKFLOW_STATE_DELIVERING` retry path, Phase 240 extended that same split to external ingress by keeping request occupancy owner-local to `connection_service` until mailbox delivery succeeds, and Phase 245 proved the same split on a delegated update-apply outcome under mailbox backlog plus workflow restart; the admitted slices now prove internal, external, and product-shaped update consumers without justifying subscriptions, fairness frameworks, generic completion registries, queue generalization, or transport widening | next step: reopen only when a second materially different completion consumer cannot stay honest on the current mailbox owner, when delivery fairness or retry policy can no longer stay split between mailbox backlog truth, workflow-local retry, and connection-local or update-local ownership, or when the compact completion and workflow-query command families can no longer carry the admitted caller-visible contract | reference: docs/plan/active/phase232_completion_mailbox_pressure_probe.txt, docs/plan/active/phase240_external_ingress_completion_and_backpressure.txt, docs/plan/active/phase245_update_recovery_and_completion_reporting_pressure.txt, kernel/src/services/completion_mailbox_service.mc, kernel/src/services/workflow/service.mc, kernel/src/services/connection_service.mc

- status: active | area: kernel | task: keep temporary delegation bounded to one completion-bound lease owner rather than widening into sessions, authentication, generic capability objects, or a delegation framework | why: Phase 233 proves the current authority model under real workload by adding exactly one retained `lease_service` owner that issues temporary workflow-completion leases, binds validity to the completion-mailbox generation, and allows one explicit cross-pid consume path into `completion_mailbox_take(...)`; the admitted slice now has one real temporary delegation path, but current evidence still does not justify broader rights algebra, delegation registries, richer transport, or compiler expansion | next step: reopen only when a second materially different delegated workload cannot stay honest on the current completion-bound lease owner, when temporary authority needs metadata beyond `(lease_id, workflow_id, completion_generation)`, or when the fixed four-byte shell frame can no longer carry the admitted caller-visible delegation contract | reference: docs/plan/active/phase233_bounded_delegation_lease_service_probe.txt, kernel/src/services/lease_service.mc, kernel/src/services/completion_mailbox_service.mc

- status: active | area: kernel | task: keep delegated durable mutation bounded to one workflow-scheduled named-object update lease kind plus one object-local version byte and explicit conflict outcome rather than widening into transactions, MVCC, or generic versioned storage | why: Phase 254 closes the Phase 236 deferral by making `object_store_service` persist one bounded per-object version marker, having `lease_service` capture that version at delegated object-update lease issue time, and carrying the expected version through the existing object-update workflow lane to reject direct object changes deterministically at commit time; the landing resolves the real ambiguity without adding a second workflow lane, retry engine, merge policy, or cross-owner version framework | next step: reopen only when a real admitted workload needs richer conflict metadata than the current explicit conflict outcome, when another durable owner needs the same version-aware contract, or when multi-object durable coordination becomes the narrowest honest next owner rather than a broader transaction wave | reference: docs/plan/active/phase254_object_version_truth_for_delegated_durable_mutation.txt, kernel/src/services/object_store_service.mc, kernel/src/services/lease_service.mc, kernel/src/services/workflow/core.mc, kernel/src/services/workflow/advance.mc

- status: active | area: kernel | task: keep file-like service growth bounded to one explicit owner-local probe rather than widening into filesystem, durable storage, or namespace frameworks | why: Phase 218 intentionally admits one compact file-service-shaped pressure step to test naming, identity, operation, and failure contracts under the existing retained-versus-durable boundary; current evidence still does not justify durable media semantics, mountable path graphs, recovery frameworks, or compiler/runtime persistence expansion | next step: reopen only when a real admitted consumer needs behavior that the bounded file-like owner cannot express honestly without durable persistence semantics, richer naming policy, or explicit recovery machinery, and prove the narrowest next owner before widening | reference: docs/plan/active/phase218_narrow_file_service_shape_probe.txt

- status: active | area: kernel | task: keep timer-service growth bounded to one compact owner-local timeout and cancellation surface rather than widening into scheduler, interrupt, or mailbox frameworks | why: Phase 220 landed `timer_service` as the narrowest next pressure step after the file-like owner: one bounded id-based timer state machine with create/cancel/query/expired behavior, retained-reload restart policy, and deterministic scenario-plus-reset-lane coverage; current evidence still does not justify dynamic discovery, blocking receive frameworks, wall-clock APIs, watchdog policy, or supervisor growth | next step: reopen only when a real admitted consumer needs richer temporal semantics than the current compact create or cancel or query or expired-window contract can carry honestly | reference: docs/plan/active/phase220_timer_and_task_service_narrow_scope_probe.txt, kernel/src/services/timer_service.mc

- status: active | area: kernel | task: keep task-service follow-through bounded to explicit owner-local work-state consequences after Phase 225 rather than widening into process, session, worker-pool, or scheduler frameworks | why: Phase 225 chose `task_service` as the strongest current running-system pressure and landed one bounded successful-completion path (`JD<id>!`) so active work can transition to `DONE` without opening broader orchestration policy; the owner now covers submit/query/complete/cancel/list-active plus failed classification and explicit reset-on-restart truth, and current evidence still does not justify process models, sessions, worker pools, quotas, or supervisor policy | next step: reopen only when a real admitted consumer needs another task-visible consequence or inspection surface that cannot stay inside the current owner-local submit/query/complete/cancel/list-active contract, such as bounded result payloads or history beyond direct query | reference: docs/plan/active/phase225_bounded_next_service_pressure_audit.txt, kernel/src/services/task_service.mc
- status: active | area: kernel | task: keep connection-style ingress bounded to the connection owner's fixed-capacity slots and owner-local idle-budget timeout rather than widening into timer, poller, workflow, or network frameworks | why: Phase 238 adds one persistent external-style ingress owner through `connection_service`, proves explicit open/receive/send/close plus disconnected and timed-out outcomes, and keeps timeout truth owner-local so the slice stays honest on the current boot-wired shell path; current evidence still does not justify timer-backed deadlines, `timer_service` coupling, workflow-routed ingress, sessions, sockets, or a broader network stack | next step: reopen only when a real admitted consumer needs wall-clock or poller-backed timeout semantics, ingress must route into workflow/completion owners instead of staying service-local, or another external-style ingress shape cannot stay honest inside the current fixed-capacity connection owner | reference: docs/plan/active/phase238_connection_service_narrow_ingress_admission.txt, kernel/src/services/connection_service.mc

- status: active | area: kernel | task: keep file-service payload semantics bounded to append-until-full plus retained prefix read after Phase 226 rather than widening into durability, namespace, or filesystem frameworks | why: Phase 226 closed the real caller-visible deficit by letting `file_service` append up to four retained bytes per file and return the retained prefix on read, using the already-landed explicit `FileSlot` shape without reopening compiler work; current evidence still does not justify delete, truncate, seek, paths, directories, durable media, or a storage framework | next step: reopen only when a real admitted consumer needs file behavior that cannot stay honest inside the current fixed-name, fixed-slot, four-byte retained append/read contract | reference: docs/plan/active/phase226_bounded_file_service_payload_growth.txt, kernel/src/services/file_service.mc

- status: active | area: kernel | task: keep stall-consequence classification owner-local to queue_service and keep WouldBlock bounded to the push-rejected-while-stalled case rather than widening into blocking semantics, cross-service stall propagation, or abandon policies | why: Phase 216 makes the first-time overflow (Exhausted) distinct from a persistent stall (WouldBlock) and makes the stall count queryable, but current evidence still does not justify automatic retry, supervisor notification, watchdog restart, cross-service stall coordination, or blocked-task records; stall classification is deterministic and local to the queue owner | next step: reopen only when a real admitted consumer needs to act on a stall consequence stronger than WouldBlock plus stall count query, such as bounded abandon semantics with caller-visible retry budget, cross-owner stall propagation, or an execution-level blocked-task record that cannot stay inside the current queue-owned path | reference: docs/plan/active/phase216_timeout_and_stalled_service_consequence_probe.txt, kernel/src/queue_service.mc, kernel/src/scenario_stall.mc
- status: active | area: kernel | task: keep queue-pressure policy owner-local to queue_service with explicit reject-on-full semantics rather than widening into scheduler, mailbox, or generic backpressure frameworks | why: Phase 215 makes queue scarcity explicit on the admitted queue owner: enqueue at capacity returns `Exhausted`, queue state is unchanged on rejection, and callers can observe the consequence directly on the existing queue command path; current evidence still does not justify blocking queues, retry frameworks, telemetry surfaces, or cross-owner pressure coordination | next step: reopen only when a real admitted consumer needs pressure behavior stronger than explicit reject (for example bounded drop observation, blocking semantics, or cross-owner coordination) that cannot stay local to `queue_service` | reference: docs/plan/active/phase215_queue_pressure_and_backpressure_first_probe.txt, kernel/src/queue_service.mc, kernel/src/scenario_queue.mc

- status: active | area: tooling | task: keep the workflow suite split by behavior family and grow reset-lane coverage through the shared scenario table instead of returning to a catalog-sized monolith or inventing a large test DSL | why: Phase 184B showed that the real maintenance pressure was file ownership and repeated build-plus-run scaffolding, not missing abstraction; the reset-lane family in particular was dominated by near-identical fixture install, build, and run code that now lives behind one `ResetLaneScenario` table and shared runner, and Phase 202 added a workflow guard that fails if a checked-in reset-lane fixture with `build.toml` is missing from that table | next step: keep new workflow checks in the smallest behavior-owned suite that directly matches the asserted UX contract; extend `tool_kernel_reset_lane_suite.cpp` by adding one scenario entry when a new reset-lane fixture only needs the standard build-plus-run success shape; reopen a narrower registration table only if filtering or progress reporting pressure appears without sacrificing explicit assertions | reference: docs/plan/active/phase184b_tool_workflow_suite_domain_split.txt, docs/plan/active/phase202_legacy_archive_retirement_and_reset_lane_maintenance_refresh.txt, tests/tool/tool_workflow_orchestrator.cpp, tests/tool/tool_kernel_reset_lane_suite.cpp

- status: active | area: tooling | task: keep discovery-driven test registration bounded to descriptor-driven grouped case identity rather than widening into hidden dependency inference or a test framework | why: Phase 203 lands one real filesystem-and-descriptor source of truth for grouped workflow and real-project cases by making `tests/tool/workflow/<case-name>/case.toml` and `tests/tool/real_projects/<case-name>/case.toml` drive CMake registration, grouped-runner dispatch, and local selector case identity; the real maintenance pressure was duplicate case-name registries, not a missing graph engine or magical ownership inference | next step: extend the descriptor-driven model only to other homogeneous families that have the same repeated case-registration pressure; keep semantic ownership selection explicit in `tools/select_tests.py` for shared compiler, support, and harness layers; do not widen into hidden dependency discovery or a generic test DSL | reference: docs/plan/active/phase203_discovery_driven_test_registration.txt, docs/arch/c-lang/tools/discovery_driven_test_registration_spec.txt, tests/tool/workflow/, tests/tool/real_projects/

- status: active | area: tooling | task: keep grouped tool descriptor semantics unified through one repository-owned implementation path and keep real-project ownership case-local by default | why: Phases 204 and 205 removed the remaining parallel descriptor parsers, kept `tools/tool_case_manifest.py` as the shared descriptor path for CMake registration and local selector identity, and completed the migration from central real-project suite owners to adjacent `tests/tool/real_projects/<case-name>/test.cpp` files behind one thin dispatcher; current evidence still does not justify hidden dependency inference, generated ownership rules, a generic assertion layer, or a return to catalog-sized suite owners | next step: extend the shared descriptor path only when another homogeneous family has the same repeated registration pressure; keep `tests/tool/tool_real_project_suite.cpp` thin, add new real-project cases beside their descriptor directories by default, preserve explicit shared-layer ownership rules in `tools/select_tests.py`, and reopen declaration generation only if `tests/tool/tool_real_project_suite_internal.h` becomes the honest next pressure point | reference: docs/plan/active/phase204_tool_test_system_simplicity_and_reference_quality_cleanup.txt, docs/plan/active/phase205_real_project_case_local_migration_completion.txt, tools/tool_case_manifest.py, cmake/ToolTestRegistration.cmake, tests/tool/tool_real_project_suite.cpp

- status: active | area: docs | task: keep support statements aligned with the admitted slice as later phases land | why: Phase 168 refreshed both the Veya running-system support statement and the Modern C position note to reflect the post-Phase-166 landing, and Phase 202 refreshed the active workflow/support wording again when the retired freestanding archive and stale maintenance paths were finally removed; if a later phase changes active claims without refreshing those documents, the gap will recur | next step: reopen veya_running_system_support_statement.txt and modern_c_veya_readiness_position.txt whenever a phase admits new behavior on either the hosted or the Veya running-system slice, or whenever an active support/workflow path is retired or renamed; use Phase 202 as the current maintenance baseline | reference: docs/plan/active/phase168_running_system_and_compiler_support_statement_refresh.txt, docs/plan/active/phase202_legacy_archive_retirement_and_reset_lane_maintenance_refresh.txt
- status: active | area: kernel | task: keep restart identity as a split contract rather than widening into a framework | why: Phase 182 made the current truth explicit in code: ServiceRef keeps routing stable, the boot-wired pid also stays stable, but each explicit restart replaces the service instance and bumps a generation marker; retained-state continuity remains explicit per owner rather than universal | next step: reopen only when a real phase needs stale-handle invalidation, shell-visible restart markers beyond the additive generation getter, dynamic rebinding, or another identity boundary that cannot be expressed as stable refs plus explicit generation counters | reference: docs/plan/active/phase182_restart_identity_continuity_probe.txt
- status: active | area: kernel | task: keep endpoint ids as stable public names and routing keys until a real phase justifies a capability gate at the dispatch gateway | why: Phase 183 explicitly classified the endpoint address model: boot-wired endpoint ids are public names, knowing one is sufficient to address a service, and no capability check is performed; the authority surface is the reserved handle-slot mechanism in syscall.ReceiveObservation; this is the intentional design for the current flat boot-wired simulation kernel | next step: reopen only when knowing a public name alone should no longer be sufficient to address a service, when handle-based authority becomes a real routing gate rather than a reserved field, or when dynamic endpoint assignment replaces the current static boot-wired constants | reference: docs/plan/active/phase183_endpoint_ownership_and_authority_audit.txt, kernel/src/service_topology.mc, kernel/src/kernel_dispatch.mc
- status: active | area: kernel | task: keep received-handle authority explicit at the dispatch gateway while preserving Phase 185's one explicit transfer exception | why: Phase 184 found the leak in the current model and Phase 185 admitted the narrowest honest repair: handle-bearing observations are still invalid on ordinary named traffic, but one explicit transfer endpoint now consumes a monotonic one-shot grant and performs one bounded follow-through send | next step: reopen only when a real admitted phase needs ordinary handle-bearing receive traffic outside the transfer endpoint, more than one handle per message, receiver-visible rights algebra, or another authority-routing rule broader than the current default rejection plus one transfer exception | reference: docs/plan/active/phase185_ipc_shape_reaudit_under_restart_and_retained_state.txt, kernel/src/transfer_grant.mc, kernel/src/kernel_dispatch.mc
- status: active | area: kernel | task: keep the transferred-handle runtime path bounded to one monotonic grant table and one transfer-only service even after the Phase 198 multi-handle step | why: Phase 185 admitted one explicit one-handle path and Phase 198 proved that exactly one additive two-handle `(log, kv)` pair can still stay honest inside the same transfer owner; current evidence still does not justify ordinary handle-bearing traffic, receiver-visible rights algebra, generic pair routing, or a capability framework | next step: reopen only when a real admitted consumer needs more than two transferred handles, a second real retained pair shape beyond the current `(log, kv)` contract, ordinary handle-bearing traffic to non-transfer services, or another authority-context feature that cannot stay inside the current monotonic grant table plus transfer_service path | reference: docs/plan/active/phase198_multi_handle_transfer_follow_through_under_explicit_authority.txt, kernel/src/transfer_service.mc
- status: active | area: kernel | task: keep future authority changes framed through the Phase 209 ownership vocabulary rather than through new ad hoc exception language | why: Phase 209 gathered the admitted authority surfaces into one reusable repository-owned table keyed by owner, public name, reference, mutation, transfer, observation, and destruction rights; later endpoint, transfer, retained-lane, retained-state, and shell-control work can now ask whether a proposal changes one of those rights instead of relitigating authority from scratch | next step: reopen only when a real admitted object kind or authority rule no longer fits the current table, or when the public-name endpoint model, transfer-only exception, or fixed-target retained-control model stops being the honest current boundary | reference: docs/plan/active/phase209_authority_vocabulary_and_ownership_table_audit.txt, docs/arch/veya/veya_authority_vocabulary_and_ownership_table_v0.txt
- status: active | area: compiler | task: keep type inference bounded to local `:=` initializer inference rather than widening into cross-statement or public-surface inference | why: Phase 210 lands the narrowest honest ergonomic gain: one-line right-hand-side-only inference for local mutable bindings; the implementation and docs now explicitly reject top-level inference, function-signature inference, struct-field inference, and unresolved `nil` or generic-only bindings, so future widening needs stronger evidence than local boilerplate relief alone | next step: reopen only when a real admitted consumer cannot be served by local `:=` plus explicit annotations and can prove that the narrowest next step is stronger than one-line local inference but still weaker than a general implicit-typing system | reference: docs/plan/active/phase210_narrow_local_initializer_type_inference.txt, compiler/sema/check.cpp
- status: active | area: compiler | task: retire the older bootstrap `name = expr` binding-or-assignment compatibility surface now that `:=` is the intended inferred local binding spelling | why: Phase 210 made `:=` the explicit local inferred-binding form but intentionally left the older parser-plus-sema compatibility path in place; that leaves two local binding stories in the language surface and keeps parser, sema, MIR, and tests carrying a bootstrap disambiguation path whose preferred replacement now exists | next step: execute the scoped retirement plan in Phase 211 by migrating repository-owned bare-name implicit bindings to `:=` or explicit declarations, then tightening parser and sema so bare `=` is assignment only | reference: docs/plan/active/phase211_bootstrap_binding_or_assignment_retirement_scope.txt, compiler/parse/parser_statements.cpp, compiler/sema/check.cpp
- status: active | area: kernel | task: keep boot-state cross-service pressure on named typed `ServiceCell<T>` fields plus explicit restart-outcome fields until a later boot wave proves a stronger typed boot-local metadata owner is narrower than the current explicit switches | why: Phase 275A re-audited the remaining repetition after later boot-service growth and found that the dominant cross-service work is still heterogeneous: `boot_update.mc` performs typed state replacement and generation bumps, `init.mc` performs materially different per-service reload or init logic, and `boot_identity.mc` projects marks, restart outcomes, and target-specific metadata with different per-service meanings; a stronger uniform table would remove some endpoint-to-field mapping but would currently introduce a second metadata framework or erased-state pressure rather than a real simplification | next step: preserve the current typed named-field shape, continue moving static classification facts to `service_topology.mc` when they are topology truth, and reopen only when future boot growth produces both repeated endpoint-to-field mapping and repeated action shape across multiple boot owners strongly enough that one explicit typed boot-local metadata owner can absorb that work honestly | reference: docs/plan/active/phase275a_boot_service_cell_uniformity_design_audit.txt, docs/arch/key_decisions.txt, kernel/src/boot.mc, kernel/src/boot/boot_update.mc, kernel/src/boot/boot_identity.mc, kernel/src/boot/init.mc
- status: active | area: runtime | task: keep kernel observability effect-backed and prohibit replay-based trace paths | why: reset-lane serial tracing previously replayed service logic with synthetic state and debug inference, which created a parallel truth path that could diverge from real execution | next step: require new kernel tracing surfaces to append event codes from real effects and reject new replay-style trace helpers in `kernel/src/` | reference: /Users/ro/dev/c_modern/docs/plan/active/phase153_service_composition_shape_audit.txt
- status: active | area: kernel | task: keep reset-lane entry split between default scripted scenarios and one explicit live receive mode rather than widening into an event framework | why: Phase 253 closes the old backlog item by admitting one bounded hosted live input owner, but it does so without replacing `scenarios.run(...)` as the default repo-owned integration lane; `kernel_entry.mc` now owns the explicit mode split, `live_receive.mc` owns stdin-backed four-byte frame assembly, and current evidence still does not justify schedulers, interrupt delivery, brokers, generic poller frameworks, or making live ingress the ambient default | next step: reopen only when a real admitted consumer needs more than the current explicit `live` stdin-backed mode or when a second live source, transport shape, or mailbox-style owner proves the current scripted-default plus explicit-live split is no longer the narrowest honest boundary | reference: /Users/ro/dev/c_modern/docs/plan/active/phase253_bounded_live_receive_owner_probe.txt, /Users/ro/dev/c_modern/kernel/src/kernel_entry.mc, /Users/ro/dev/c_modern/kernel/src/live_receive.mc
- status: active | area: kernel | task: enforce the main.mc growth boundary through kernel_entry delegation rather than reopening entry-point orchestration | why: `main.mc` is now reduced to boot-state init plus one call into `kernel_entry.run(...)`, and Phase 253 preserved that boundary while adding live ingress; future phases can still regress by pushing protocol bytes, argument policy, or runtime orchestration back into the root entry instead of the nearest owner | next step: keep `main.mc` limited to boot init and one call into `kernel_entry`; keep mode selection in `kernel_entry`, scripted integration in `scenarios`, and live input assembly in `live_receive`; reopen only when a real admitted consumer proves that one of those three owners cannot carry the next bounded entry-path change honestly | reference: /Users/ro/dev/c_modern/kernel/src/main.mc, /Users/ro/dev/c_modern/kernel/src/kernel_entry.mc, /Users/ro/dev/c_modern/docs/plan/active/phase253_bounded_live_receive_owner_probe.txt

- status: active | area: kernel | task: keep retained/restart comparison bounded to one compact fixed-target shell query rather than widening into discovery or a dashboard | why: Phase 227 adds one additive `XM<target>!` comparison path that reuses the already-admitted retained summary truth for participation, generation marker, and last restart outcome while keeping ownership in `boot_identity.mc` and `init.mc`; current evidence still does not justify a registry, persistence framework, or any richer operator surface | next step: reopen only when a real admitted consumer needs comparison data beyond the current compact retained summary projection, when fixed targets stop being the honest owner boundary, or when the current shell lifecycle family can no longer carry the comparison contract honestly | reference: docs/plan/active/phase227_retained_restart_identity_follow_through.txt, kernel/src/kernel_dispatch.mc, kernel/src/boot/boot_identity.mc

Longer-horizon compiler and tooling items

- status: deferred | area: tooling | task: add per-part semantic diagnostic ownership for multi-file modules without reopening the module-system design | why: Phase 190 completed the core repair: diagnostics now resolve synthetic merged-module spans back to the exact contributing part file and original line number via `CheckOptions.module_part_paths`; column recovery within a part is the one remaining gap (column numbers are not shifted by the line-offset scheme, so they are already correct) | next step: reopen only if a real consumer needs per-part column recovery or if direct-source multi-file mode is admitted and needs the same treatment | reference: docs/plan/active/phase190_multifile_diagnostic_ownership_follow_through.txt, compiler/sema/check.h, compiler/sema/check.cpp, compiler/driver/builder.cpp
- status: deferred | area: stdlib | task: decide whether the scalar encoded `Error` surface should widen to messages, wrapped causes, or a heap-backed object | why: Phase 175 consumer audit confirmed that all admitted real programs use is_err() for binary flow control and return fixed OS exit codes; no program inspects kind() or code() in production; no program renders a human-readable error description to the user; the kind/code model is not a current ceiling on any admitted program | next step: reopen only when BOTH hold: (1) a production call site cannot determine what to tell the user or which recovery path to take even with kind and code values; AND (2) the narrowest resolution is a bounded optional message slice or explicit cause field — not a heap-backed object, cause chain, implicit try-operator, or hidden propagation; binary is_err() flow control alone and test-only kind/code assertions alone are not triggers | reference: /Users/ro/dev/c_modern/docs/plan/archive/phase175_error_model_widening_first_gate.txt
- status: deferred | area: mir | task: keep broader generic-function monomorphization explicit after the Phase 171 deferral audit and the completed Phase 255/256 helper follow-through | why: Phase 171 kept broad monomorphization deferred, and Phases 255 and 256 then closed the only current real value-shaped generic-body pressure through the same-module and direct imported `ServiceCell<T>` helper path; the repository still lacks a stronger admitted consumer for whole-program or broader imported generic-body completion | next step: reopen only when a real admitted consumer is blocked beyond the landed Phase 255/256 boundary, such as linked static-library imported helper bodies, indirect-only callable shapes, or another ABI-shaped generic body that cannot stay on the erased-ABI or direct-helper path; Phase 261 now classifies Band B as stronger than this deferred frontier until that happens | reference: `/Users/ro/dev/c_modern/docs/plan/active/phase171_generic_monomorphization_first_slice.txt`, `/Users/ro/dev/c_modern/docs/plan/active/phase255_complete_value_shaped_user_body_generic_helpers.txt`, `/Users/ro/dev/c_modern/docs/plan/active/phase256_imported_generic_helper_follow_through.txt`, `/Users/ro/dev/c_modern/docs/plan/active/phase261_compiler_pressure_audit_after_the_generic_and_table_wave.txt`
- status: deferred | area: compiler | task: keep broader array-extent const-eval expansion explicit after the completed Phase 248 follow-through | why: Phase 248 now normalizes module-local compile-time integer expressions in array dimension positions across sema and MIR local annotation reconstruction, and it admits nested fixed arrays through the real compiler path; imported qualified names in array dimensions, const functions in type positions, and arbitrary compile-time execution remain outside the admitted slice | next step: reopen only when a real admitted consumer is materially blocked by imported qualified array extents or another still-deferred type-position constant shape, and keep the solution narrow to array-extent evaluation rather than widening into dependent typing or general compile-time execution | reference: `/Users/ro/dev/c_modern/docs/plan/active/phase248_array_type_follow_through_for_named_extents_and_2d_arrays.txt`
- status: deferred | area: compiler | task: keep broader hosted executable global-constant widening explicit after the Phase 251 top-level string-constant repair | why: Phase 251 closed the concrete hosted executable defect for top-level `const str` by adding one narrow string-global lowering seam and spending it in `journal_service`; no broader current admitted consumer justifies widening the executable backend into a general global-constant framework, and Phase 261 classifies Band B as stronger than this remaining compiler frontier | next step: reopen only when a real admitted executable consumer is materially blocked by another specific top-level constant family, and keep the next repair scoped to that family instead of widening global constant lowering wholesale | reference: docs/plan/active/phase251_hosted_executable_top_level_string_constant_lowering.txt, docs/plan/active/phase261_compiler_pressure_audit_after_the_generic_and_table_wave.txt, compiler/codegen_llvm/backend.cpp, compiler/codegen_llvm/backend_executable_support.cpp, kernel/src/services/journal_service.mc

- status: active | area: compiler | task: keep post-Band-A compiler reopening gated behind concrete Band B blockers after Phase 261 | why: Phases 255 through 260 closed the real same-module/imported helper, descriptor-table, procedure-table, imported runtime-table, and bounded record-construction pressures that justified Band A; the remaining compiler fronts are now explicit and narrower than the next app-facing substrate wave: broader generic-body execution beyond the landed helper path, imported descriptor transport and descriptor-plus-procedure composition, and broader type-position constant/global-constant execution | next step: prefer Band B work first; reopen compiler only when a real launcher, app, or other repository-owned consumer is blocked specifically by one of those named fronts and the narrowest honest repair stays owner-local instead of expanding into another diffuse compiler wave | reference: docs/plan/active/phase261_compiler_pressure_audit_after_the_generic_and_table_wave.txt, docs/plan/admin/speculative_roadmap_post_phase254.txt
- status: deferred | area: compiler | task: keep broader callable-feature widening deferred after Phase 230b now that imported struct procedure tables lower honestly across module boundaries | why: Phase 230b now admits the real pressure slice completely: explicit typed fixed arrays and struct constants containing exact-signature named non-generic procedures, including imported struct constants projected through fields and called after MIR lowering and executable emission; current evidence still does not justify closures, lambda syntax, callback registries, erased callbacks, procedure equality or ordering, or generic procedure constants | next step: reopen only when a real admitted owner is materially blocked by one of those still-closed callable surfaces and the narrowest honest repair is that specific feature rather than a broader callback framework | reference: docs/plan/active/phase230b_explicit_procedure_value_dispatch_tables_probe.txt, docs/arch/c-lang/language/procedure_value_dispatch_tables_note.txt, compiler/mir/mir_lower.cpp
- status: deferred | area: compiler | task: keep imported runtime-table follow-through bounded to fixed procedure-valued tables after Phase 259 | why: Phase 259 now proves one real imported runtime-table owner in `examples/real/issue_rollup/src/render/rollup_render_table.mc` and one maintained imported consumer in `rollup_render.mc` through sema, `.mci`, MIR, executable lowering, and real-project validation; current evidence still does not justify imported descriptor transport, descriptor-plus-procedure table composition in a real owner, or table-driven kernel dispatch, shell decode, or boot topology | next step: reopen only when a real admitted consumer needs imported descriptor data, procedure-valued descriptor fields, or a materially different imported runtime-table owner, and route that pressure to the narrowest blocked surface rather than a broader callback or metadata framework | reference: docs/plan/active/phase259_imported_constant_surface_follow_through_for_runtime_tables.txt, examples/real/issue_rollup/src/render/rollup_render_table.mc, tests/compiler/codegen/codegen_executable_tests.cpp
- status: deferred | area: compiler | task: keep descriptor-table follow-through bounded to same-module explicit typed owners after Phase 257 until a real imported or procedure-valued descriptor consumer appears | why: Phase 257 proves one maintained descriptor family in `kernel/src/identity/service_topology.mc` combining labels, nested records, and enum metadata through sema, MIR, and executable lowering; current evidence still does not justify imported descriptor transport, procedure-valued descriptor fields in a real descriptor owner, or any broader metadata framework | next step: reopen only when a real admitted consumer needs descriptor data across an imported boundary or needs exact-signature procedure fields inside a maintained descriptor owner, and route that work to the narrowest blocked surface rather than a launcher/app framework wave | reference: docs/plan/active/phase257_const_evaluable_table_and_descriptor_follow_through.txt, kernel/src/identity/service_topology.mc, tests/compiler/codegen/const_descriptor_table.mc
- status: active | area: tooling | task: keep the retired freestanding proof lane deleted and rebuild any future non-hosted validation from live owners rather than resurrecting a frozen harness | why: Phase 202 harvested the only durable current-owner facts from the retired freestanding archive into active docs, deleted the archive tree, and added a workflow guard that keeps checked-in reset-lane fixtures anchored to the live scenario table; the repository no longer benefits from carrying a second frozen proof corpus or stale maintenance scripts outside the live build and selector contract | next step: reopen only when a new admitted non-hosted validation lane has a current owner and repeatable live workflow, and create that lane directly under live `kernel/`, `tests/system/`, or the grouped tool suites instead of recreating an archive | reference: docs/plan/active/phase202_legacy_archive_retirement_and_reset_lane_maintenance_refresh.txt, tests/tool/tool_kernel_reset_lane_suite.cpp
- status: active | area: tooling | task: keep shared libraries, external system-library links, install policy, and broader cross-target coordination explicit after the landed Phase 30 and Phase 74 hardening proofs | why: Phase 30 proved deterministic selected-target reuse and non-selected-target non-churn on the admitted richer project graph, and Phase 74 moved that guarantee onto the shipped `issue_rollup` workflow itself, so the next workflow pressure is no longer build-surface trust inside one manifest but where to stop before runtime-loaded artifacts, broader linker policy, or distribution claims | next step: keep `sharedlib`, external library link semantics, install naming, and broader cross-target coordination deferred until a real admitted project needs more than the current in-project static-library plus selected-target hardening proof | reference: `/Users/ro/dev/c_modern/docs/plan/phase74_deterministic_rebuild_and_cross_target_reuse.txt`
- status: active | area: stdlib | task: keep the admitted `io.pipe()` and `io.poller_*` composition slice narrow while richer non-blocking policy, half-close semantics, subprocess piping, and IPC abstractions remain deferred | why: the repository now has both the blocking `pipe_handoff` proof and the readiness-based `pipe_ready` proof, so the next remaining question is where to stop before broader policy claims rather than whether the basic composition works | next step: preserve the current blocking and readiness-based pipe proof pair and reopen only when an admitted project needs behavior beyond those bounded slices | reference: `/Users/ro/dev/c_modern/docs/plan/phase43_io_pipe_and_handle_first_sync_reframing.txt`
- status: active | area: codegen | task: keep the admitted narrow sync slice bounded to typed thread spawn or join, mutex, condvar wait or signal or broadcast, and atomic load or store publication while remaining atomic breadth stays explicit follow-on work | why: Phase 70 re-audited the admitted real sync consumers and found no current public pressure for compare-exchange, exchange, or fetch-add beyond the already landed hosted slice, even though condvar broadcast is already implemented | next step: reopen only when an admitted real project needs public atomic read-modify-write or another broader coordination boundary rather than mutex or condvar or load or store publication | reference: `/Users/ro/dev/c_modern/docs/plan/phase70_sync_and_mem_follow_on_gate.txt`
- status: active | area: stdlib | task: keep broader mem API widening explicit after the completed Phase 266 allocator-selection audit | why: the admitted real mem consumers now come with explicit selection guidance as maintained truth: ordinary owning contiguous storage stays on `Buffer<T>`, bounded fixed-size reuse stays on `mem.Pool`, transient scratch stays on `Arena`, and larger-granularity staging stays on `mem.Run`; current evidence still does not justify slab caches, allocator-adapter APIs, `arena_allocator(...)`, run sub-allocation or mapping helpers, owning arena string helpers, or a broader region framework | next step: reopen only when an admitted real project exposes a repeated object family or larger-granularity pressure that cannot stay honest on the landed buffer, pool, arena, or run rules and the narrowest repair is one explicit new memory owner rather than consumer-local cleanup | reference: `/Users/ro/dev/c_modern/docs/plan/active/phase266_allocator_selection_guidelines_and_audit.txt`
- status: active | area: kernel | task: keep ServiceSlot topology naming bounded to the current flat static record; reopen if growth pressure materialises | why: Phase 170 added ServiceSlot (endpoint, pid) and four slot constants to service_topology.mc, unifying the wiring that was previously split between endpoint ID constants and boot.mc owner-pid constants; no routing framework, iteration API, or supervisor access was admitted | next step: reopen topology structure when the service count grows past eight, when a supervising layer needs to iterate the static slot table at runtime, or when a phase requires dynamic endpoint assignment that cannot be expressed as a static constant; until then the slot constants scale linearly and require no mechanism change | reference: `docs/plan/active/phase170_static_service_topology_first_acknowledgment.txt`, `kernel/src/service_topology.mc`
- status: active | area: kernel | task: keep supervisor framework deferred after the Phase 172 explicit deferral decision | why: Phase 172 audited restart pressure at the current 4-service point and Phase 176 re-tested that decision at 5 services; init-owned explicit restart in `kernel/src/init.mc` remains sufficient; `restart_log()`, `restart_kv()`, and `restart_echo()` are the complete flat policy surface needed right now; no health-check callback, retry counter, watchdog, or autonomic restart was warranted | next step: reopen supervisor when service count exceeds 8, when restart-frequency pressure requires per-service retry policies that init.mc cannot express cleanly as flat functions, or when a real phase proves that caller-driven explicit restart is the narrowest blocked path | reference: `docs/plan/active/phase172_supervisor_and_restart_shape_probe.txt`, `docs/plan/active/phase176_adversarial_service_growth_under_explicit_restart.txt`, `kernel/src/init.mc`
- status: active | area: kernel | task: keep compiler and framework surfaces closed after Phase 176 fifth-service growth | why: Phase 176 added a real fifth boot-wired service and explicit restart follow-through without needing compiler, sema, MIR, backend, ABI, target, runtime, hal, routing-table, or supervisor expansion; the current Message or Effect plus flat ServiceSlot model still carries the admitted pressure honestly | next step: reopen only when a later admitted service cannot fit the current flat shell routing, leaf dispatch, and init-owned restart model without distortion, or when a concrete pressure point proves the narrowest next owner is outside `kernel/src/` | reference: `docs/plan/active/phase176_adversarial_service_growth_under_explicit_restart.txt`
- status: active | area: kernel | task: keep hostile-user shell diagnostics bounded to compact classifier replies until a real consumer needs more | why: Phase 177 closed the shell-owned truth gap with two-byte invalid replies (`?C` for unknown command, `?S` for malformed admitted command shape) while preserving service-owned failures unchanged; no admitted consumer currently needs human-readable messages, wider payloads, extra event families, or a broader shell framework | next step: reopen only when a real shell-facing consumer cannot recover or present enough user-visible truth from status plus the compact classifier bytes, and the narrowest honest fix is still a bounded protocol extension rather than a text shell or error-object redesign | reference: `docs/plan/active/phase177_shell_as_hostile_user_boundary.txt`
- status: active | area: kernel | task: keep Phase 178 topology and restart helpers flat and explicit | why: Phase 178 re-audited the five-service reset-lane system after hostile-user and restart-sensitive growth pressure; the first honest structural follow-through was a flat topology lookup surface (`service_count()`, `service_slot_at(...)`, `service_slot_for_endpoint(...)`) plus a flat init-owned endpoint-driven restart entry (`can_restart(...)`, `restart(state, endpoint)`); this closes current readability pressure without admitting a routing table, supervisor, retry policy, or broader framework | next step: reopen only when the service count exceeds 8, when topology needs richer graph structure than slot enumeration or endpoint lookup, when restart policy needs per-service retry state rather than endpoint-driven replacement, or when a real phase needs serial or shell restart to become an admitted policy surface | reference: `docs/plan/active/phase178_topology_and_restart_growth_reaudit.txt`, `kernel/src/service_topology.mc`, `kernel/src/init.mc`
- status: active | area: kernel | task: keep lifecycle classification static and keep restart enactment explicit after Phase 186 | why: Phase 186 found that the current boot/init/runtime split was still honest, but one static lifecycle fact had drifted into the wrong owner: restartability and retained-reload class belong with the fixed boot-wired topology, not with the init-owned restart executor; the repair was to make `service_topology.mc` own `service_restart_mode(...)`, `service_can_restart(...)`, and `service_restart_reloads_state(...)` while leaving `init.restart(...)` as the explicit enactment path | next step: reopen only when restart policy needs runtime health state, retry counters, or another dynamic lifecycle rule that a static topology classification cannot express honestly, or when a real phase needs serial or shell to become restartable | reference: `docs/plan/active/phase186_boot_decomposition_and_lifecycle_audit.txt`, `kernel/src/service_topology.mc`, `kernel/src/init.mc`
- status: active | area: kernel | task: keep shell-visible lifecycle control bounded to fixed target ids and explicit restart requests | why: Phase 189 proved that one user-facing lifecycle path can stay honest without a supervisor, shell runtime, or effect-model widening by using one compact `XQ`/`XR` shell family over fixed public targets, topology-owned lifecycle class, and init-owned restart enactment | next step: reopen only when a real admitted lifecycle workflow needs richer target discovery, payloads beyond the current two-byte `<target, class>` reply, or a second control family strong enough that reusing the shell endpoint as the bounded control path becomes more distortive than explicit | reference: `docs/plan/active/phase189_bounded_shell_lifecycle_control_follow_through.txt`, `kernel/src/shell_service.mc`, `kernel/src/kernel_dispatch.mc`
- status: active | area: kernel | task: keep launcher execution bounded to catalog, selection, single-foreground launch, install consumption, restart classification, manifest query, and installed-workflow status on the admitted app path rather than widening into storage, workflow, session, or process-management policy | why: Phases 268 through 272 grew `launcher_service` to the current Band C boundary: it now owns fixed-catalog selection, identify, launch-one-foreground consequences, fresh-versus-resumed-versus-invalidated classification, one bounded `issue_rollup` manifest query, and one explicit installed-workflow status surface, while `update_store_service` still owns durable bytes and launch-record truth; Phase 273 then confirmed that widening launcher beyond that current app-facing policy seam would create a false center of gravity | next step: reopen only when a second real installed-program consumer appears, when callers need richer app-facing status than the current compact launcher query can carry honestly, when a materially different asset family cannot stay on the current `issue_rollup` path, or when broader app execution semantics can no longer stay outside the current launcher-plus-update-store split | reference: `docs/plan/active/phase268_bounded_launcher_service_first_slice.txt`, `docs/plan/active/phase272_first_user_facing_installed_workflow_demo.txt`, `docs/plan/active/phase273_app_substrate_pressure_audit.txt`, `kernel/src/identity/program_catalog.mc`, `kernel/src/services/launcher_service.mc`
- status: active | area: kernel | task: keep installed-program storage bounded to one update-store-owned fixed slot and one launcher-consumed identify surface until a second installed consumer appears | why: Phase 269 closes the first honest install-artifact gap by making one explicit installed slot live on `update_store_service`, binding it to the fixed `issue_rollup` program descriptor, and letting launcher consume that truth through one compact identify query and launch gating; current evidence still does not justify multi-slot installs, package queues, richer manifest identity, bundle directories, or widening launcher into storage ownership | next step: reopen only when a second real installed-program consumer appears, when launcher needs richer installed-artifact metadata than the current program-id plus version plus len contract, or when restart-visible app resume needs a stronger owner than the current update-store-plus-launcher split | reference: `docs/plan/active/phase269_installable_bundle_or_slot_first_slice.txt`, `kernel/src/services/update_store_service.mc`, `kernel/src/services/launcher_service.mc`
- status: active | area: kernel | task: keep app-visible restart truth bounded to launcher classification over one update-store-owned persisted launch record rather than widening into sessions, save-state frameworks, or launcher-owned durable state | why: Phase 270 closes the first honest restart-visible app-path gap by letting `launcher_service` classify launch replies as `fresh`, `resumed`, or `invalidated` while `update_store_service` persists only the minimal durable comparison record needed to tell whether the installed slot still matches the last launched installed artifact across launcher restart; current evidence still does not justify process resume, document restore, multi-app background policy, richer invalidation taxonomy, or moving durable bytes or install policy into the launcher | next step: reopen only when a later admitted app path needs richer invalidation causes, a second installed-program consumer, real resumed execution semantics, or another app-facing restart shape that cannot stay honest on the current launcher-classification plus update-store launch-record split | reference: `docs/plan/active/phase270_app_visible_restart_and_resume_truth.txt`, `kernel/src/services/launcher_service.mc`, `kernel/src/services/update_store_service.mc`
- status: active | area: kernel | task: keep shell-visible restart identity bounded to one fixed-target generation query rather than widening into discovery or a control framework | why: Phase 193 proved that the Phase 182 split contract can stay user-visible through one compact `XI<target>!` query on the existing shell control path: endpoint and pid continuity remain boot-owned and stable, while restart-visible service-instance replacement is exposed as one fixed four-byte generation payload for restartable boot-wired targets | next step: reopen only when a real admitted workflow needs richer identity payloads than the current generation bytes, dynamic target discovery, broader lifecycle bundles, or another caller-visible identity boundary that cannot stay inside the existing shell endpoint plus boot-owned generation truth | reference: `docs/plan/active/phase193_bounded_shell_service_identity_query_follow_through.txt`, `kernel/src/shell_service.mc`, `kernel/src/kernel_dispatch.mc`, `kernel/src/service_identity.mc`
- status: active | area: kernel | task: keep retained-state reload bounded to explicit per-service snapshots rather than a persistence framework | why: Phase 179 proved one honest restart-with-state path through `log_service`, and Phase 181 extended the same shape to `kv_service`; the current two-owner evidence still does not justify a filesystem, packaging layer, generic snapshot registry, or compiler-facing persistence support | next step: reopen only when a third admitted retained-state owner needs reload, when snapshot lifetime must outlive the immediate restart call, when restart replacement needs cross-service coordination instead of one owner-local save-and-reload, or when one explicit per-service snapshot path becomes more distortion than clarity | reference: `docs/plan/active/phase179_bounded_retained_state_reload_probe.txt`, `docs/plan/active/phase181_key_value_retained_state_reload_follow_through.txt`, `kernel/src/log_service.mc`, `kernel/src/kv_service.mc`, `kernel/src/init.mc`
- status: active | area: kernel | task: keep the next frontier on service-system growth rather than second-target portability | why: Phase 180 compared the real post-Phase-174 evidence and found that Phases 176 through 179 each exposed one narrower service-system owner to extend, while Phase 174 only named portability seams and did not expose a real admitted consumer whose strongest blocker is second-target work | next step: choose the next phase from service growth, service identity, retained-state follow-through, or user-facing interaction; reopen bounded second-target work only when a real admitted consumer needs Linux arm64 or another second target for behavior Darwin arm64 cannot supply honestly, and when that target pressure is stronger than any remaining owner-local service-system question | reference: `docs/plan/active/phase180_next_frontier_plateau_decision.txt`
- status: active | area: kernel | task: keep SMP constraint A deferred — unified dispatch state pointer in kernel_dispatch.mc | why: Phase 173 surveyed the kernel source and named constraint A as the hardest SMP retrofit: kernel_dispatch_step, kernel_dispatch_leaf, kernel_dispatch_kv, and execute_effect all share one *KernelBootState pointer; the read-modify-write over a shared pointer is not atomic; the service handler functions (kv_service.handle, log_service.handle) are already pure state machines and would compose cleanly with per-service shards if state partitioning were ever adopted | next step: reopen when a phase requires concurrent interrupt-driven dispatch from multiple hardware cores, or when the scheduler ready queue needs concurrent wake from two cores over different service shards | reference: docs/plan/active/phase173_multi_core_model_boundary_identification.txt, kernel/src/kernel_dispatch.mc
- status: active | area: kernel | task: keep SMP constraint B deferred — advisory-first compound dispatch atomicity in kernel_dispatch_kv | why: Phase 173 named constraint B: kernel_dispatch_kv executes kv write then advisory log append and commits both to *state as a compound assignment; under SMP another core can observe *state between the two sub-dispatches; single-core sequential dispatch makes this invisible; medium difficulty to retrofit (per-compound lock or eliminate the compound by making advisory the caller's explicit responsibility) | next step: reopen when a phase introduces concurrent log writes from two sources sharing the same advisory commit window, or when the Phase 160 advisory-gap closure requires a stronger cross-service atomicity guarantee | reference: docs/plan/active/phase173_multi_core_model_boundary_identification.txt, kernel/src/kernel_dispatch.mc

Potential language features

- status: parked | area: compiler | task: consider one bounded byte-literal or character-literal surface for `u8` UI cells and key codes | why: Phase 291's post-UI compiler audit found the first repeated user-facing source awkwardness in the maintained app and wrapper owners: `issue_rollup` and `review_board` both spell visible cell contents and input keys as raw numeric ASCII bytes, which hides obvious intent in otherwise direct code; the pressure is parser-plus-sema-local, but it does not yet justify a broader byte-string, text, or collection-construction wave | next step: reopen only when the narrowest honest repair can stay at single-byte source literals or another equally bounded `u8`-local surface, and only if that slice still beats app-local helper cleanup and the strongest system-side options in the current wave | reference: docs/plan/active/phase291_post_ui_compiler_pressure_audit.txt, examples/real/issue_rollup/src/render/rollup_render.mc, examples/real/issue_rollup/src/app/issue_rollup_interactive.mc, examples/real/review_board/src/review_board_ui.mc, kernel/src/issue_rollup_app.mc

- status: parked | area: compiler | task: consider standalone empty collection literals without an expected type owner | why: Phase 249 lands `[]` only where an existing binding, return, call, assignment, or enclosing aggregate field already fixes the collection type; the language still deliberately rejects free-floating `[]` values so collection kind and element type do not get inferred from ambient control flow | next step: reopen only when a real admitted owner is materially distorted by the lack of standalone `[]` values and the narrowest honest solution can stay explicit about destination collection shape rather than reopening broad collection inference | reference: docs/plan/active/phase249_typed_empty_collection_literals.txt, compiler/sema/check.cpp

- status: parked | area: compiler | task: consider empty literal support for owning collection families such as Buffer<T> only under an explicit ownership contract | why: Phase 249 intentionally admits `[]` only for zero-length fixed arrays and borrowed `Slice<T>` because those empty values are honest without allocation or ownership transfer; `Buffer<T>` and other owning families remain closed because an empty literal there would smuggle construction policy behind convenient syntax | next step: reopen only when a real admitted owner is materially distorted by the lack of owning empty literals and the narrowest honest solution can state allocation and ownership policy explicitly instead of treating `[]` as hidden construction | reference: docs/plan/active/phase249_typed_empty_collection_literals.txt, compiler/sema/check.cpp

- status: parked | area: compiler | task: consider admitting discard target `_` outside local binding and assignment targets | why: Phase 246 intentionally lands `_` only for local binding targets and assignment targets so the compiler can carry explicit ignore-the-value semantics without widening into wildcard binding or pattern-system work; if future owner-local code wants `_` in parameter lists, loop headers, or switch-pattern positions, that should be reopened as a separate bounded target-position phase rather than inferred from the current landing | next step: reopen only when a real admitted owner is materially distorted by the lack of discard `_` in one of those still-closed target positions, and keep any follow-through explicit and local to that target family rather than reopening broader pattern semantics | reference: docs/plan/active/phase246_local_binding_and_discard_target_follow_through.txt, compiler/parse/parser_decls.cpp, compiler/parse/parser_statements.cpp, compiler/sema/check.cpp

- status: parked | area: compiler | task: consider widening bare target-typed explicit conversion syntax beyond builtin scalar integers | why: Phase 194 deliberately lands the direct `T(expr)` spelling only for builtin scalar integer targets because that is the narrowest real owner-local pressure the repository had; the broader parenthesized explicit-conversion surface still exists for other admitted families, but distinct, alias, pointer, slice, and other non-integer conversions do not gain the new bare syntax; while implementing Phase 194, that broader direct syntax was the main intentionally deferred language limit encountered | next step: reopen only when a real admitted owner is still materially distorted by the lack of bare direct syntax outside builtin scalar integers, and keep any design explicit rather than reopening implicit coercions or a general cast framework | reference: docs/plan/active/phase194_narrowing_int_conversion.txt, compiler/parse/parser_expr.cpp, compiler/sema/check.cpp
- status: parked | area: compiler | task: consider standalone aggregate literals without an expected type owner | why: Phase 192 closed the real owner-local boilerplate by admitting inferred brace aggregates in typed contexts, but brace literals still need a surrounding owner that already states the target aggregate type; `{ ... }` is not yet a first-class aggregate value outside a typed binding, call, return, assignment, or enclosing aggregate field | next step: reopen only when a real admitted owner is blocked specifically by the lack of standalone aggregate values and the narrowest honest solution can stay explicit about the destination aggregate shape instead of introducing hidden coercion or partial typing | reference: docs/plan/active/phase192_inferred_brace_aggregate_follow_through.txt, compiler/sema/check.cpp, kernel/src/service_topology.mc
- status: parked | area: compiler | task: consider an ordinary `sizeof(T)`-style size query for library code | why: Phase 262 landed the first fixed-size pool owner as raw byte slots because the repository still lacks an ordinary library-visible way to derive the storage size of `T`; a typed `Pool<T>` wrapper would have required compiler magic or an invented side-channel for slot sizing, which would have violated the owner-local stdlib/runtime boundary | next step: reopen only when a real admitted owner is materially distorted by explicit byte-slot sizing and the narrowest honest repair is a bounded compile-time size query rather than a broader reflection, allocator, or layout framework | reference: docs/plan/active/phase262_fixed_size_pool_allocator_first_slice.txt, stdlib/mem.mc, runtime/hosted/mc_hosted_runtime.c
- status: parked | area: compiler | task: keep broader value-shaped generic-body follow-through explicit after Phase 256 | why: Phase 256 extends the admitted helper family through one honest imported module boundary by spending the `ServiceCell<T>` helper pair in `kernel/src/boot/service_cell_helpers.mc` and by letting multi-module executable targets build one merged object before link; the repository still does not admit broader imported generic bodies across linked static-library targets, indirect-only generic callable shapes without concrete direct-call metadata, or generic call-site type inference for helper calls | next step: reopen only when a real admitted consumer is materially blocked by one of those remaining generic-body limits and the narrowest honest repair is more than the current direct-call imported helper path; keep any follow-through deterministic and do not widen parser, sema, `.mci`, or the driver unless that specific blocked consumer proves they are the real owner | reference: docs/plan/active/phase256_imported_generic_helper_follow_through.txt, compiler/driver/builder.cpp, compiler/mir/mir.cpp, kernel/src/boot/service_cell_helpers.mc
- status: parked | area: compiler | task: consider making direct scalar field-projection helper calls lower as reliably as the equivalent explicit local temporaries | why: Phase 231 validation hit a current compiler or MIR-lowering sensitivity around passing projected scalar fields from value records such as `ServiceMark` directly through helper-call shapes; the owner-local code stayed honest by extracting those projections into explicit `u32` locals first, but repeated restart-identity assertions became noisier than the underlying contract | next step: reopen only when another admitted owner is materially distorted by the same projection-call limitation and the narrowest honest repair can stay inside lowering or call typing rather than widening the language model | reference: `kernel/src/scenarios/scenario_assert.mc`, `kernel/src/scenarios/scenario_restart.mc`, `docs/plan/active/phase231_restart_safe_workflow_service_probe.txt`
- status: parked | area: compiler | task: consider overlapping nested record-update paths in one `with` block | why: Phase 199 deliberately lands dotted field-path updates but rejects overlapping subtree-plus-descendant combinations such as `state with { log: next, log.generation: g }` so the semantics stay explicit and deterministic without introducing ordering or precedence rules hidden inside the source form; that keeps MIR lowering simple and honest, but it is the remaining nested-update limitation encountered during the implementation | next step: reopen only when a real admitted owner is materially distorted by the overlapping-path rejection and the narrowest honest solution can state one deterministic precedence rule without widening into hidden mutation, implicit ordering, or a broader state framework | reference: `docs/plan/active/phase199_nested_record_update_field_path_follow_through.txt`, `compiler/sema/check.cpp`, `compiler/mir/mir_lower.cpp`
- status: parked | area: compiler | task: consider omitted-value shorthand for dotted record-update paths only if real pressure appears | why: Phase 260 lands the narrowest honest record-construction shorthand as `field:` meaning `field: field` for simple named aggregate and record-update entries; omitted values on dotted paths such as `outer.inner:` stay rejected so the shorthand remains local to existing simple field-init ownership and does not imply broader nested-update precedence or name-resolution rules | next step: reopen only when a real admitted owner is materially distorted by the lack of dotted-path omitted values and the narrowest honest solution can stay explicit without colliding with positional aggregates or broadening record-update semantics into another construction system | reference: `docs/plan/active/phase260_bounded_record_construction_ergonomics_follow_through.txt`, `compiler/parse/parser_expr.cpp`, `compiler/sema/check.cpp`
