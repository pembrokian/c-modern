Modern C - Backlog

Purpose

This file is for implementation work that is known, wanted, or repeatedly rediscovered, but is not yet committed to a specific phase doc.

Use this file for:
- themes that need follow-up work
- concrete tasks that were deferred during implementation
- recurring cleanup or refinement items
- issues that keep surfacing across compiler layers

Do not use this file for:
- unresolved language or architecture questions that belong in docs/arch/open_decisions.txt
- settled rationale that belongs in docs/arch/key_decisions.txt
- phase-specific milestone tracking that belongs in the relevant phase doc

Suggested entry shape

- status: active | deferred | parked
- area: parser | sema | mir | codegen | runtime | stdlib | tooling | docs
- task: short description of the work
- why: what limiter or pressure exposed the item
- next step: the smallest useful follow-up
- reference: phase doc, issue, commit, or decision log entry if available

Current themes

- Phase 8: keep the admission matrix, findings log, and blocked canonical entries explicit so deferred pressure never disappears into prose
- Phase 8: keep bounded threaded and evented-networking canonical entries named as blocked until runtime and sync support are honest enough
- Phase 12: admit `time`, `os`, `sync`, `hal`, and `testing` only when a real consumer matrix and stated API surface justify them
- Phase 13: finish `mc test` timeout reporting, manifest ergonomics, and deterministic rebuild correctness without collapsing C++ fixture suites too early
- Phase 14: refresh canonical-program admission and closure work so each deferred entry has a named owner gap, proof path, or backlog item
- tighten the path from limited bootstrap solutions to fuller spec-aligned implementations
- collect repeated compiler-layer limitations before they become accidental design
- promote stable patterns from ad hoc fixes into deliberate architecture once pressure is proven

Ranked post-Phase-14 priorities

- status: active | area: docs | task: keep the repository-wide next-step order explicit after the Phase 14 closeout | why: closeout notes now exist for repository-bounded waves through Phase 14, but they do not by themselves rank what should happen next | next step: use `/Users/ro/dev/c_modern/docs/plan/admin/post_phase14_execution_plan.txt` as the cross-phase execution order and keep this backlog aligned with it | reference: `/Users/ro/dev/c_modern/docs/plan/admin/post_phase14_execution_plan.txt`
- status: active | area: docs | task: keep the closed current-core pressure wave reflected consistently across planning docs while the repository moves to the next capability-family decision | why: bounded ring buffer is now admitted alongside distinct-type safety, array or slice conversion pressure, and standalone default-allocator proof, so Step 1 should no longer be described as pending | next step: treat Step 1 as closed and keep the ranked execution order focused on Step 2 | reference: `/Users/ro/dev/c_modern/docs/plan/admin/post_phase14_execution_plan.txt`
- status: active | area: stdlib | task: execute the chosen Phase 12 second-wave capability family as portable poller plus narrow net | why: the repository has now frozen Option A as the next expansion wave and must keep Option B queued rather than starting both families at once | next step: implement the bounded poller and narrow net slice named in `/Users/ro/dev/c_modern/docs/plan/phase12_stdlib_runtime_maturity.txt`, with sync held as the follow-up fallback after networking | reference: `/Users/ro/dev/c_modern/docs/plan/phase12_stdlib_runtime_maturity.txt`
- status: active | area: tooling | task: reserve second-wave Phase 13 work for the project and ordinary-test pressure exposed by the chosen new capability family | why: larger real projects will exercise `.mci`, graph validation, reporting, and rebuild correctness more honestly than another abstract tool cleanup pass | next step: expand focused project fixtures only after the next capability family is concrete | reference: `/Users/ro/dev/c_modern/docs/plan/phase13_toolchain_maturity.txt`
- status: active | area: tooling | task: keep the admitted evented-echo `mc run` and `mc test` workflow proof deterministic as the networking project surface grows | why: the hosted networking family now has a real ordinary-project proof path, so new project-graph or rebuild friction should be caught there before more examples accumulate | next step: extend the existing evented-echo project proof rather than adding ad hoc one-off networking harnesses when new workflow pressure appears | reference: `/Users/ro/dev/c_modern/docs/plan/phase13_toolchain_maturity_second_wave_closeout.txt`
- status: active | area: docs | task: keep the hosted support statement, installation path, checked-vs-release note, and known limitations aligned with the admitted repository slice | why: the release-hardening note now exists, so future capability changes need documentation updates at the same boundary instead of drifting into stale README prose | next step: update `/Users/ro/dev/c_modern/docs/plan/release_hardening_hosted_slice.txt` and `README.md` whenever the supported hosted slice changes materially | reference: `/Users/ro/dev/c_modern/docs/plan/release_hardening_hosted_slice.txt`
- status: active | area: stdlib | task: keep the admitted narrow sync slice bounded to thread spawn or join plus mutex while condvar and atomic pressure remain explicit follow-on work | why: the shared-counter mutex proof is now landed, but the first hosted `sync` admission should not quietly widen into a larger concurrency grab bag | next step: route any producer-consumer or atomic-publication follow-up through named owner docs instead of expanding `sync` opportunistically | reference: `/Users/ro/dev/c_modern/docs/plan/phase12_stdlib_runtime_maturity.txt`
