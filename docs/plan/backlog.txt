Modern C - Backlog

Purpose

This file is for implementation work that is known, wanted, or repeatedly rediscovered, but is not yet committed to a specific phase doc.

Use this file for:
- themes that need follow-up work
- concrete tasks that were deferred during implementation
- recurring cleanup or refinement items
- issues that keep surfacing across compiler layers

Do not use this file for:
- unresolved language or architecture questions that belong in docs/arch/open_decisions.txt
- settled rationale that belongs in docs/arch/key_decisions.txt
- phase-specific milestone tracking that belongs in the relevant phase doc

Suggested entry shape

- status: active | deferred | parked
- area: parser | sema | mir | codegen | runtime | stdlib | tooling | docs
- task: short description of the work
- why: what limiter or pressure exposed the item
- next step: the smallest useful follow-up
- reference: phase doc, issue, commit, or decision log entry if available

Current themes

- status: active | area: runtime | task: finish reset-lane orchestration cleanup by removing mixed state-plus-control envelopes and residual per-service dispatch wrappers | why: the service layer has largely converged on shared message-plus-effect flow, but orchestration still carries `DispatchResult`-style envelopes, route-kind trees, and synthetic service-to-service adapters that duplicate policy and keep `boot` overly impure | next step: convert the narrowest remaining orchestration owner in `kernel/src/` to message-shaped edges first, then retire one top-level mixed envelope without widening into framework or compiler work | reference: /Users/ro/dev/c_modern/docs/plan/active/phase153_service_composition_shape_audit.txt

- status: active | area: runtime | task: keep kernel observability effect-backed and prohibit replay-based trace paths | why: reset-lane serial tracing previously replayed service logic with synthetic state and debug inference, which created a parallel truth path that could diverge from real execution | next step: require new kernel tracing surfaces to append event codes from real effects and reject new replay-style trace helpers in `kernel/src/` | reference: /Users/ro/dev/c_modern/docs/plan/active/phase153_service_composition_shape_audit.txt

- status: active | area: kernel | task: promote scenarios.mc to a real event-loop dispatcher after the scenario-runner extraction | why: the current scenarios.mc still chains named scenario functions over shared state and returns a status; the next honest step is a loop that receives an observation, dispatches it, and delivers its effect so entry becomes event-driven rather than script-driven | next step: when a phase needs non-sequential or interrupt-driven dispatch, replace scenario chaining with a loop-shaped dispatcher and have `main.mc` call that loop instead of accumulating more named scenario calls | reference: /Users/ro/dev/c_modern/kernel/src/scenarios.mc

- status: active | area: kernel | task: enforce the main.mc growth boundary established by the scenario extraction | why: protocol encoding and scenario logic have already moved out of `main.mc`, but new phases can still regress by adding inline protocol bytes, inline assertions, or phase-local integration logic back into the entry point | next step: keep `main.mc` limited to init, named orchestration, and return; put new integration behavior in owner modules or the scenario or loop layer instead | reference: /Users/ro/dev/c_modern/kernel/src/main.mc

Longer-horizon compiler and tooling items

- status: active | area: tooling | task: add per-part semantic diagnostic ownership for multi-file modules without reopening the module-system design | why: the current merged semantic input keeps behavior correct, but some semantic diagnostics can still anchor to the logical module's primary source path instead of the exact contributing file | next step: reopen only if real project pressure justifies path-aware AST or diagnostics plumbing strong enough to point semantic failures back to the exact source-file part | reference: /Users/ro/dev/c_modern/docs/plan/decision_log.txt

- status: active | area: stdlib | task: decide whether the phase-1 scalar encoded `Error` surface should later widen to messages, wrapped causes, or a heap-backed object | why: the current kind or code encoding removes anonymous sentinel pressure in real programs, but it intentionally stops short of message ownership, source context, and causal chains | next step: wait for another admitted real program that still feels too opaque after kind or code classification, then design the narrowest explicit follow-up without reopening hidden propagation | reference: /Users/ro/dev/c_modern/docs/arch/key_decisions.txt

- status: active | area: mir | task: keep broader generic-function executable support explicit after the completed erased-ABI imported generic closure | why: Phase 49 now proves the admitted direct-call family needed by `sync.thread_spawn<T>`, but the repository still does not claim general generic-function monomorphization or executable support for signatures whose ABI depends on unresolved type parameters | next step: reopen only when a second admitted imported generic procedure-typed consumer or another executable proof needs generic support beyond the erased-ABI family landed in Phase 49 | reference: `/Users/ro/dev/c_modern/docs/plan/phase49_imported_generic_call_closure.txt`
- status: active | area: tooling | task: keep shared libraries, external system-library links, install policy, and broader cross-target coordination explicit after the landed Phase 30 and Phase 74 hardening proofs | why: Phase 30 proved deterministic selected-target reuse and non-selected-target non-churn on the admitted richer project graph, and Phase 74 moved that guarantee onto the shipped `issue_rollup` workflow itself, so the next workflow pressure is no longer build-surface trust inside one manifest but where to stop before runtime-loaded artifacts, broader linker policy, or distribution claims | next step: keep `sharedlib`, external library link semantics, install naming, and broader cross-target coordination deferred until a real admitted project needs more than the current in-project static-library plus selected-target hardening proof | reference: `/Users/ro/dev/c_modern/docs/plan/phase74_deterministic_rebuild_and_cross_target_reuse.txt`
- status: active | area: stdlib | task: keep the admitted `io.pipe()` and `io.poller_*` composition slice narrow while richer non-blocking policy, half-close semantics, subprocess piping, and IPC abstractions remain deferred | why: the repository now has both the blocking `pipe_handoff` proof and the readiness-based `pipe_ready` proof, so the next remaining question is where to stop before broader policy claims rather than whether the basic composition works | next step: preserve the current blocking and readiness-based pipe proof pair and reopen only when an admitted project needs behavior beyond those bounded slices | reference: `/Users/ro/dev/c_modern/docs/plan/phase43_io_pipe_and_handle_first_sync_reframing.txt`
- status: active | area: codegen | task: keep the admitted narrow sync slice bounded to typed thread spawn or join, mutex, condvar wait or signal or broadcast, and atomic load or store publication while remaining atomic breadth stays explicit follow-on work | why: Phase 70 re-audited the admitted real sync consumers and found no current public pressure for compare-exchange, exchange, or fetch-add beyond the already landed hosted slice, even though condvar broadcast is already implemented | next step: reopen only when an admitted real project needs public atomic read-modify-write or another broader coordination boundary rather than mutex or condvar or load or store publication | reference: `/Users/ro/dev/c_modern/docs/plan/phase70_sync_and_mem_follow_on_gate.txt`
- status: active | area: stdlib | task: keep broader mem API widening explicit after the completed Phase 70 gate | why: the admitted real mem consumers still stop at explicit `Buffer<u8>` ownership, `slice_from_buffer<u8>(...)`, and arena init or new or deinit, so Phase 70 found no honest current pressure for generic buffer metadata helpers, allocator-adapter APIs, or richer arena policy | next step: reopen only when an admitted real project needs public mem behavior beyond the landed `u8`-buffer and arena slice | reference: `/Users/ro/dev/c_modern/docs/plan/phase70_sync_and_mem_follow_on_gate.txt`
