Phase 127 -- Compiler Critique Response Plan
============================================

Source documents
----------------
- docs/agent/critiques/compiler/phase127/ast_phase127.txt
- docs/agent/critiques/compiler/phase127/backend_phase127.txt
- docs/agent/critiques/compiler/phase127/check_phase127.txt
- docs/agent/critiques/compiler/phase127/driver_phase127.txt
- docs/agent/critiques/compiler/phase127/mir_phase127.txt
- docs/agent/critiques/compiler/phase127/parser_phase127.txt

Purpose
-------
This note now serves as the current repository-facing summary of the Phase 127
compiler critique wave after the per-file treatment notes were written and the
first parser/backend follow-through slices landed.

Use the per-file treatment notes under `docs/plan/critique_closures/` as the
authoritative detailed record. This note exists to answer one narrower question:
what from the Phase 127 compiler critique wave is already closed, what is still
worth doing next, and what remains intentionally deferred.

Current status
--------------
Closed or already landed in code:
- the backend hardening slice recorded in
  `docs/plan/critique_closures/phase127_backend_critique_treatment.txt`
- the parser hygiene slice recorded in
  `docs/plan/critique_closures/phase127_parser_critique_treatment.txt`
- the AST dump-depth concern that originally motivated the Phase 127 AST note
- the older driver ordinary-runner O(1) summary concern
- the older MIR generic-type-matching OR-chain concern
- the older sema `kDerefField` / `ValidateGlobals` framing used by the initial
  consolidated draft of this note

Still live and worth follow-through:
- sema duplicate-name bind-vs-assign fact recording inside one pattern
- sema float-to-int constant-conversion boundary hardening
- driver duplicate non-extern definition diagnostics in shared merge paths
- driver direct-source build-result plumbing so `RunRun()` stops recomputing
  artifact paths
- driver dual-diagnostic-sink reporting in `EvaluateRunOutputCase()`

Real but intentionally deferred:
- checker lookup-map and import-resolver boundary work
- parser lexer/result-contract cleanup around `ok_`
- AST and MIR documentation/test polish that does not currently block behavior
- larger driver factoring such as process abstraction, timeout policy, and
  test-runner extraction

Cross-check summary
-------------------
The current tree and treatment notes agree on the following points:

- Parser critique work is no longer an active correctness queue. The shared
  `internal.mc` convention, parser infrastructure comment, parser lifetime
  note, separator-plus-sync helper, and top-level recovery tightening are all
  landed; only broader lexer/parser result-contract cleanup remains deferred.
- AST depth propagation is already closed. Remaining AST critique work is small
  dump-contract documentation and future-proofing, not a correctness repair.
- The sema critique's live items are the duplicate-name bind-vs-assign fact
  recording issue and the float-to-int boundary case. The earlier consolidated
  `kDerefField` and `ValidateGlobals` items are already outdated.
- The driver critique's live items are shared merge diagnostics, structured
  direct-source build results, and merged reporting from multiple diagnostic
  sinks. The generated ordinary-runner summary-growth concern is already closed.
- The MIR critique no longer identifies a live generic matching bug. Remaining
  MIR work is documentation/test hardening and later scale work.

Recommended next order
----------------------
If Phase 127 compiler-critique follow-through continues before a wider Veya
restart, the narrow honest order is:

1. `check.cpp`: fix duplicate-name bind-vs-assign fact recording in one
   binding-or-assign pattern.
2. `check.cpp`: tighten float-to-int constant-conversion bounds and add a
   focused regression test.
3. Shared driver/builder merge path: diagnose duplicate non-extern definitions
   instead of silently keeping the first encountered body.
4. `driver.cpp`: return structured direct-source build results so `RunRun()`
   consumes the authoritative executable path instead of recomputing it.
5. `driver.cpp`: merge or concatenate diagnostics in `EvaluateRunOutputCase()`.

Definition of done for this critique wave
-----------------------------------------
For current planning purposes, this critique wave should be treated as closed
enough to stop blocking adjacent work when:

- the landed backend and parser follow-through remains green
- the remaining live sema/driver items are either landed or explicitly parked
  in later phase notes/backlog entries
- no stale consolidated note continues to contradict the authoritative per-file
  treatment documents

Bottom line
-----------
Phase 127 critique work is no longer one broad compiler response queue. The
backend and parser slices are landed, the AST and MIR critiques are mostly in
documentation-or-scale territory, and the only clearly live implementation work
left from this wave sits in sema and driver. That is a small enough remainder
that Veya development does not need to stay blocked on a vague "finish the
compiler critique wave first" requirement.