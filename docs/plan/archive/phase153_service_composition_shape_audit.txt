Phase 153 -- Service Composition Shape Audit
============================================

Status
------
COMPLETE

Purpose
-------
Continue from the planned Phase 152 controlled-variation step by measuring
whether the current services compose honestly through explicit owner-local
glue or are starting to accumulate framework-like translation pressure.

This document records the landed Phase 153 slice.
It defines one bounded composition-audit phase over the admitted interactive
service slice and its chosen Phase 152 variation.
It does not claim a broker, orchestration framework, dynamic routing, or a
general service platform.

Phase result
------------
Phase 153 is complete as one bounded service-composition audit over the reset-lane kernel path.

The landed repository slice now includes:

- one real compact shell-command route in `kernel/` that reaches `log_service`
  and `kv_service` through `serial_shell_path` rather than stopping at shell-
  local echo behavior
- one explicit bounded key-value-write observation into `log_service` that
  keeps the only two-service follow-through visible without introducing a
  middleware layer
- one updated real kernel image path in `kernel/src/main.mc` that exercises
  composition through serial-shell ingress instead of bypassing it with direct
  service-only probes
- one focused reset-lane system regression proving a mixed shell workflow over
  the real build-and-run path
- one explicit conclusion: the current reset-lane composition pressure is
  still handled honestly by owner-local glue, so no framework or compiler
  expansion is justified in this phase

Relationship to companion notes
-------------------------------
This phase plan should stay aligned with:

- `docs/plan/admin/speculative_roadmap_post_phase145.txt`
- `docs/plan/active/phase152_pattern_stability_under_one_controlled_variation.txt`
- `docs/plan/active/phase154_service_identity_and_addressing_semantics.txt`
- `docs/plan/backlog.txt`
- `docs/plan/decision_log.txt`

Repository status note
----------------------
- The repository now has one extracted service shape and one planned
  stability-under-variation discriminator.
- The next likely failure mode is not raw correctness. It is glue growth:
  repeated adapters, translation helpers, or implicit conventions that begin
  to act like a hidden framework.
- If that pressure is not classified now, later work could mistake
  composition pain for permission to add orchestration machinery.

Problem being solved
--------------------
- Cross-service composition is valuable only if it remains explicit about who
  owns request interpretation, reply construction, and failure visibility.
- Without one bounded composition audit, the repository could widen into more
  service graph complexity before deciding whether explicit composition is
  still the honest model.
- The repository therefore needs one explicit Phase 153 note that measures
  whether the current system can compose without glue explosion and, if not,
  names the smallest honest composition primitive candidate.

Core rule
---------
- Audit composition pressure; do not answer it with a framework by default.
- If composition remains explicit and understandable, keep it explicit.
- If composition does not remain honest, admit at most one tightly bounded
  primitive and only after concrete glue repetition is shown.
- Keep compiler, sema, MIR, backend, ABI, target, runtime, and `hal`
  surfaces closed unless repeated composition pressure proves one narrow owner
  gap.

Chosen slice for this phase
---------------------------
Targeted boundary shape:

1. one canonical Phase 153 execution note defining the composition-audit
   boundary
2. one explicit inventory of the current cross-service glue paths over the
   admitted system and its chosen variation
3. one classification of whether each repeated adapter or translation path is
   owner-local, accidental, or evidence for one minimal primitive
4. one audit-owned proof over at least one awkward composition path
5. one explicit conclusion stating whether explicit composition remains the
   honest model

Why this is the right next step
-------------------------------
- It tests the extracted pattern under deeper structural pressure without
  adding broader platform scope.
- It forces the repository to distinguish honest explicit glue from hidden
  framework creep.
- It creates a cleaner handoff into identity and temporal work by making the
  composition boundary visible first.

Landed implementation surfaces
------------------------------
The owning implementation surfaces for the landed slice are:

1. `docs/plan/active/phase153_service_composition_shape_audit.txt`
  - records the concrete Phase 153 conclusion and keeps the no-framework rule explicit

2. `kernel/src/shell_service.mc`
  - now owns the fixed four-byte shell command classification for echo,
    log append, log tail, kv set, and kv get

3. `kernel/src/serial_shell_path.mc`
  - now owns the explicit shell-to-log and shell-to-kv handoff plus the
    bounded kv-write-to-log follow-through

4. `kernel/src/boot.mc` and `kernel/src/main.mc`
  - keep the flat endpoint dispatch and image entry explicit while the real
    mixed workflow now travels through the serial-shell path

5. `kernel/src/log_service.mc`, `kernel/src/kv_service.mc`, and
  `kernel/src/serial_shell_event_log.mc`
  - preserve service-local reply and observation ownership while exposing the
    one real two-service composition seam

6. `tests/smoke/`, `tests/system/`, and `tests/tool/tool_workflow_suite.cpp`
  - keep the reset-lane composition regression on behavior-first surfaces

Composition judgment
--------------------
The landed reset-lane inventory now has three concrete composition seams:

1. `serial_service` buffers one bounded ingress frame and hands one fixed
  shell payload to `shell_service`
2. `shell_service` classifies one fixed compact command and
  `serial_shell_path` routes it to `log_service` or `kv_service`
3. `kv_service` emits one fixed write marker that `serial_shell_path` forwards
  into `log_service`

This remains explicit owner-local glue, not hidden framework pressure.
There is one routing seam and one bounded two-service follow-through, but no
repeated adapter family, generic request algebra, callback table, or broker
layer. The honest Phase 153 conclusion is therefore to keep explicit
composition and advance to identity and temporal questions in later phases.

Overarching pattern captured
----------------------------
The post-audit runtime correction makes one stronger execution rule explicit:

1. service step inputs should be message-shaped data
2. service step outputs should be effect-shaped consequences
3. routing and reply control should come from effect application, not
  per-service dispatch or result envelopes

In this repository slice, the shell path now follows that model by emitting
`Reply` or `Send` effects rather than route-kind dispatch structs.
The same direction remains expected for remaining reset-lane services as
future phases remove transitional routing and observation shims.

Specific kv correction
----------------------
The key-value owner had briefly reintroduced the same structural mistake in a
different form by defining service-local message and effect types plus a
composite effect shape.

The corrected shape now is:

- `kv_service` consumes shared `Message` values
- `kv_service` returns shared `Effect` values
- key-value state mutation happens in service-local state
- side-effect follow-through (`Send` to log) is explicit in orchestration code

This keeps key-value semantics owner-local while preventing another hidden
service-specific runtime model from forming.

Kernel-wide recurrence check (Phase 153 follow-through)
------------------------------------------------------
Audit across `kernel/src/` found the same mistake family mostly reduced to
orchestration boundaries:

1. service-level `*Dispatch` and service-level mini-effect systems are now
  removed from shell and kv owners
2. top-level `boot.DispatchResult` still combines next-state and reply
  metadata for call-site convenience and remains a known transitional envelope
3. syscall-shaped observation adapters remain in orchestration helpers and
  should continue shrinking as message-shaped edges become dominant

Observability correction (Phase 153 follow-through)
--------------------------------------------------
Replay-based tracing was removed from the reset lane.

- event logs now consume event codes emitted by real dispatch effects
- replay helpers and debug-inference tracing are no longer the source of
  truth for serial-shell observability

This keeps observability aligned with real execution and avoids parallel
truth systems that can drift from runtime behavior.

Suggested execution order
-------------------------
1. Freeze the Phase 153 rule in docs: composition audit, no framework wave.
2. Enumerate the current cross-service glue paths over the landed and varied
   system.
3. Classify which paths are honest owner-local glue and which are repeated
   enough to count as structural pressure.
4. If necessary, test one minimal primitive candidate against the explicit
   no-hidden-generalization rule.
5. Add the audit and projected MIR proof over the chosen composition
   boundary.
6. Advance the kernel proof marker to 153 only after docs, audit, and tests
   agree on the same composition conclusion.

Preferred proof points
----------------------
1. The repository can point to concrete repeated glue rather than vague
   discomfort.
2. Explicit composition is either affirmed as the honest model or rejected
   for one narrow and stated reason.
3. No broker, orchestration framework, or hidden routing layer appears.
4. One projected MIR proof shows the composition boundary remains explicit in
   the real artifact.

Exit criteria
-------------
- the repository publishes one explicit Phase 153 note defining the
  composition-audit boundary
- the real kernel artifact builds and runs to a Phase 153 marker
- at least one awkward composition path is audited concretely
- the repository publishes one explicit conclusion about whether explicit
  composition remains sufficient or one minimal primitive is justified
- the focused regression proves build, run, docs, and MIR all expose the same
  Phase 153 boundary
- compiler and low-level toolchain surfaces remain unchanged in this phase

Outcome
-------
- all exit criteria above are now satisfied for the bounded Phase 153 slice
- the reset-lane kernel now carries a real shell-to-log and shell-to-key-value
  composition path over the compact four-byte command route
- the only two-service follow-through remains explicit as one fixed key-value-
  write observation into `log_service`
- the composition audit concludes that explicit owner-local glue remains the
  honest model in this slice, so no broker, framework, or compiler work is
  admitted here

Non-goals
---------
- adding a broker, router, orchestration framework, or service mesh
- widening the command surface just to create more composition cases
- hiding repeated glue behind implicit conventions or callback tables
- reopening sema, MIR, backend, ABI, target, runtime, or `hal`

kernel/ module admission
------------------------
Modules are added to `kernel/` only when THIS phase's audit forces them.
Do not copy legacy implementations. Follow the four-section standard: state,
init, handlers, optional debug.

If this phase's composition audit reveals lifecycle pressure (spawn/exit
needed as a composition primitive), then `lifecycle/init` is admissible:
one state struct + two handlers, not a LifecycleAudit.

If lifecycle admission requires task scheduling, `sched` is admissible in
a minimal form: ReadyQueue + TaskSlot, no LifecycleAudit prefix.

Otherwise, no modules are added in Phase 153.
