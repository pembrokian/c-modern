Phase 261 -- Compiler Pressure Audit After The Generic And Table Wave
=====================================================================

Status
------
COMPLETED

Purpose
-------
Re-audit the compiler frontier after Phases 255 through 260 so the repository
can state clearly which Band A pressures are now honestly closed, which remain
explicitly deferred, and whether the next stronger owner lies in the compiler
or in the more visible app-facing work of Band B.

This phase exists now because Band A intentionally mixes several narrow but
different compiler fronts. The repository should stop after that wave and
classify the outcome before it drifts into another unbounded compiler sequence.

Repository status note
----------------------
- Phase 252 named the same-module value-shaped generic-helper blocker and
  Phase 255 completed that direct helper slice for the real `ServiceCell<T>`
  family.
- Phase 256 extended that same helper family across one honest imported module
  boundary without reopening general imported generic-body completion.
- Phase 257 proved one same-module descriptor-table owner in
  `kernel/src/identity/service_topology.mc`.
- Phase 258 proved one same-module procedure-table consumer in the maintained
  `issue_rollup` render owner.
- Phase 259 proved one imported fixed procedure-table consumer through
  `rollup_render_table.mc` without reopening imported descriptors or registry
  semantics.
- Phase 260 closed one bounded record-construction shorthand, `field:`, at the
  parser/sema/MIR boundary and spent it in the boot helper family.
- The speculative post-254 roadmap explicitly intended this mixed wave to stop
  for an audit before another compiler phase was chosen.

Problem being solved
--------------------
- Without an explicit closeout audit, a successful Band A could easily blur
  into another sequence of loosely connected compiler wishes.
- The repository needs one honest answer to three questions: which Band A
  slices actually landed cleanly, which still-closed surfaces remain the next
  real compiler frontier, and whether the stronger next owner has now moved out
  of the compiler and into Band B's app-facing substrate work.
- This is a current planning problem, not generic roadmap hygiene. The repo is
  deliberately choosing between more compiler closure and more visible system
  outcomes.

Non-goals
---------
- no new compiler feature landing by convenience
- no opportunistic backlog grooming unrelated to the Band A compiler wave
- no broad language-version statement
- no silent reopening of parser, sema, MIR, backend, or interface-artifact
  families during the audit
- no user-facing support statement; that belongs later in the roadmap

Invariants
----------
- The audit must classify actual landed or still-blocked repository-owned
  pressures only.
- Closed surfaces must stay bounded to what the earlier phases really proved.
- Still-open surfaces must remain explicit and owner-local rather than vague
  feature families.
- The audit must not claim a broader milestone than the individual landed Band
  A phases justify.
- If the strongest next owner is no longer the compiler, the audit must say so
  plainly.

Required surface changes
------------------------
1. Re-audit Phases 255 through 260 against their actual landed or blocked
   outcomes.
2. Publish which compiler pressures are now honestly closed.
3. Publish which nearby compiler surfaces remain explicitly deferred and why.
4. Identify whether Band B's app-facing substrate work now outranks any
   remaining compiler frontier.
5. Update the backlog or decision log only where the audit finds a real
   bounded still-open pressure that should remain visible.

Forbidden changes
-----------------
- do not land a new compiler feature inside the audit note
- do not reframe a partially closed slice as broad generic or table completion
- do not broaden the audit into app, launcher, or UI phase design beyond what
  is needed to classify the next stronger owner
- do not bury the real still-open frontier under generic categories like
  "language ergonomics" or "compiler polish"

Phase role in the larger sequence
---------------------------------
- This phase closes Band A and decides whether the next honest motion remains
  compiler-owned or should move to Band B's app-shaped substrate work.
- It preserves the repository's evidence-first habit of stopping after a mixed
  follow-through wave and stating what is actually true.
- It should make the handoff into Band B easier by naming which compiler truths
  Band B may now rely on.

Relevant backlog items this phase intentionally brings under pressure
--------------------------------------------------------------------
- keep broader generic-function monomorphization explicit after the Phase 171
  deferral audit
- keep broader callable-feature widening deferred after Phase 230b
- keep broader compile-time execution and imported artifact widening explicit
  unless Band A proves one of them is still the narrowest blocker

Questions this phase must answer
--------------------------------
1. Which Band A slices landed cleanly enough that later phases can treat them
   as maintained truth rather than provisional probes?
2. Which still-closed compiler surfaces remain the next honest frontier after
   the generic and table wave?
3. Did Band A reveal that one stronger remaining pressure actually belongs to
   Band B or later app-facing work rather than to another compiler phase?
4. Which backlog and decision-log entries now need tightening, retirement, or a
   sharpened revisit trigger?

Implementation outcome
----------------------
- Band A now closes as maintained truth for five narrow compiler surfaces:
  same-module value-shaped user-body generic helpers, one imported direct
  generic-helper family, one same-module descriptor-table family, one
  maintained same-module procedure-table consumer plus one imported fixed
  procedure-table consumer, and one bounded omitted-value record-construction
  shorthand.
- Those slices remain intentionally bounded to what the landed phases actually
  proved. They do not imply broad monomorphization, imported descriptor
  transport, descriptor-plus-procedure composition, general compile-time
  execution, broad callback features, or a general object-construction system.
- The remaining compiler fronts are now clear and owner-local rather than
  diffuse:
  1. broader generic-body execution beyond the landed same-module and direct
    imported helper path
  2. imported descriptor transport and descriptor-plus-procedure table
    composition
  3. broader type-position constant evaluation and broader hosted executable
    global-constant families beyond the specifically landed repairs
- None of those remaining fronts currently outranks Band B's more visible
  program-identity, launcher, and app-shaped substrate work. The audit
  therefore closes Band A and hands priority to Band B instead of inventing a
  second compiler wave.

Questions answered
------------------
1. The maintained Band A truths are the exact landed slices from Phases 255
  through 260: direct same-module helper specialization, one imported direct
  helper family, one same-module descriptor-table family, one maintained
  same-module procedure-table consumer plus one imported fixed runtime-table
  consumer, and one bounded `field:` shorthand.
2. The still-closed compiler fronts are specific and already recorded: broader
  generic-body execution beyond the landed helper path, imported descriptors
  and descriptor-plus-procedure composition, and broader compile-time or
  global-constant execution families.
3. Band A revealed that the stronger next owner is no longer the compiler.
  Band B's app-facing substrate work now wins unless a later real consumer is
  blocked by one of the explicitly named deferred compiler fronts.
4. The backlog needed tightening where older compiler entries still read as if
  a broad generic or global-constant follow-through remained active. The
  decision log needed one explicit closeout entry recording that Band B now
  outranks the remaining compiler fronts.

Non-decisions and deferred edges
--------------------------------
- No new compiler phase is chosen inside this audit.
- No imported descriptor transport, descriptor-plus-procedure composition, or
  broader callback follow-through is reframed as partially complete.
- No broader type-position constant execution or hosted executable global-data
  framework is admitted by implication from the landed narrow repairs.
- No app, launcher, or UI design is specified here beyond the ownership claim
  that Band B now outranks the remaining compiler fronts.

Preferred implementation shape
------------------------------
- Keep the audit document-focused and owner-local.
- Reuse the repository's earlier plateau and frontier-audit style: classify
  landed truth, remaining truth gaps, and why adjacent tempting work still does
  not win.
- Prefer a short set of concrete still-open fronts over a diffuse wish list.
- If Band B now clearly wins, say so and stop rather than inventing a second
  compiler wave.

Implementation accelerators
---------------------------
Main files and navigations
- Primary controlling owners: the six preceding Band A phase docs plus
  `docs/plan/backlog.txt` and `docs/plan/decision_log.txt`.
- First-hop adjacent files: `docs/plan/admin/speculative_roadmap_post_phase254.txt`,
  `docs/agent/prompts/plan_spec.txt`, and the real source or test files named
  by Phases 255 through 260 only where the audit needs to confirm outcome.
- Nearby accelerator docs: `docs/agent/prompts/patterns_cookbook.txt`,
  `docs/agent/prompts/compiler_file_invariants.txt`, and
  `docs/agent/prompts/test_overview.txt`.
- Expected first validation target when the owner changes: none beyond build or
  test evidence already required by the earlier phases; this is a planning
  audit, not a new behavior landing.

Relevant cookbook patterns
- Pattern 6 -- Add A Compiler Phase is the nearest fit because this is a
  bounded compiler-planning slice, but the phase is explicitly an audit rather
  than a new behavior admission.
- No other cookbook pattern fits cleanly because the main owner is the planning
  surface itself.

Relevant file-local invariants
- The active Band A phase docs are the contract owners for what each slice
  meant; the audit must not silently reinterpret them.
- `docs/plan/backlog.txt` is for still-open work items, not unresolved design
  questions.
- `docs/plan/decision_log.txt` is for bounded deliberate limits, not status
  narration.
- The audit should classify compiler ownership honestly before handing off to
  Band B.

Relevant test surfaces
~~~~~~~~~~~~~~~~~~~~~~
- Primary owning validation surface: the build and test obligations already
  named by Phases 255 through 260.
- Narrowest expected first executable validation target: none new; the audit
  should cite prior validation rather than invent fresh broad-suite runs.
- Supporting validation guidance: if one Band A slice is still ambiguous, use
  the narrowest owning compiler target from that phase instead of broad full-
  suite reruns.

Test obligations
----------------
- no new executable validation is required beyond the earlier Band A phase
  evidence unless the audit finds a still-ambiguous slice
- if ambiguity remains, rerun only the narrowest owning target named by the
  ambiguous earlier phase

Validation
----------
- No new executable validation was required because the six preceding Band A
  phase documents already record their owning compiler and project validation
  surfaces and the audit found no ambiguous landed slice that changed the
  ownership classification.
- Validation for this phase is therefore documentation consistency only: the
  active Phase 261 note, `docs/plan/backlog.txt`,
  `docs/plan/decision_log.txt`,
  `docs/plan/admin/speculative_roadmap_post_phase254.txt`, and
  `docs/agent/prompts/repo_map.md` must agree that Band A is closed and that
  Band B now outranks the remaining deferred compiler fronts.

Acceptance criteria
-------------------
- the repository has one explicit closeout statement for the Band A compiler
  wave
- later phases can see clearly which compiler surfaces are now maintained truth
  and which remain deferred
- the next stronger owner after Band A is classified honestly rather than left
  implicit
- no new feature work is smuggled into the audit

Future follow-up
----------------
- If Band B clearly wins after the audit, proceed to the program-identity and
  launcher substrate work without inventing another compiler wave first.
- If one still-open compiler blocker remains stronger than Band B, record it as
  the next explicit phase rather than leaving it as narrative residue.
- Tighten or retire any stale backlog or decision-log entries exposed by the
  audit.