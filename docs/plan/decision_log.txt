Modern C - Decision Log

Purpose

This file records implementation choices where the team intentionally chose a limited or provisional solution because a fuller solution was blocked, too broad, or not yet justified.

Use this file for:
- limited solutions chosen during implementation
- cases where a deeper fix was deferred on purpose
- compiler-layer workarounds that should be revisited later
- decisions that are not language-spec questions but still matter for future cleanup

Do not use this file for:
- unresolved questions that belong in docs/arch/open_decisions.txt
- durable settled design rationale that belongs in docs/arch/key_decisions.txt
- ordinary progress notes or implementation summaries

Suggested entry shape

- date: YYYY-MM-DD
- area: parser | sema | mir | codegen | runtime | stdlib | tooling | docs
- context: what was being implemented
- limiter: what prevented the fuller solution
- decision: what limited solution was chosen
- tradeoff: what was accepted or sacrificed
- revisit trigger: what should cause this to be reopened
- reference: related phase doc, backlog item, or code location


Entries
- 2026-04-16 | area: tooling + docs | context: Phase 202 legacy archive retirement and reset-lane maintenance refresh after Phase 184B had already made the live workflow suite behavior-owned and table-driven, while the backlog still carried archive-deletion and support-doc refresh debt through Phase 201 | limiter: the repository no longer used the archived freestanding proof harness, but active docs and the local changed-path selector still pointed at dead archive paths, stale workflow filenames, and removed maintenance commands; widening this maintenance phase into a new freestanding framework, compiler change, or kernel semantic phase would have solved a larger problem than the actual maintenance drag | decision: harvest only the durable live-owner facts into active docs, add one workflow-suite guard so every checked-in reset-lane fixture with a `build.toml` must appear in the `ResetLaneScenario` table, update `tools/select_tests.py` to the split workflow-suite filenames, retire the freestanding support note to historical status, and delete `archive/legacy_freestanding/`; make no compiler, sema, MIR, backend, ABI, target, runtime, or `hal` expansion | tradeoff: the repository stops carrying a dead proof corpus and stale command claims, but older archived phase notes still refer to removed paths as historical context and this phase deliberately does not translate every old golden or maintenance script into a new live lane | revisit trigger: reopen only when a new admitted non-hosted validation lane has current owners and repeatable live workflows rather than historical proofs | reference: docs/plan/active/phase202_legacy_archive_retirement_and_reset_lane_maintenance_refresh.txt, tests/tool/tool_kernel_reset_lane_suite.cpp, tools/select_tests.py
- 2026-04-16 | area: parser + sema + mir + kernel | context: Phase 199 nested record-update field-path follow-through after Phase 195 had already admitted shallow `base with { field: value }` and active kernel `ServiceCell<T>` owners still had to rebuild one explicit inner aggregate by hand | limiter: the real pressure was the remaining nested wrapper-field boilerplate in owner-local immutable state code, not hidden mutation, array or slice updates, computed paths, object methods, or a broader state framework; the existing aggregate typing and lowering path was already sufficient if the feature stayed a sema-owned dotted field-path extension over the existing `with` form | decision: admit exactly one dotted field-path extension inside `base with { ... }`; carry the path explicitly in the AST, make sema walk each segment through record-typed fields and type-check the terminal replacement, and lower the form in MIR by recursively rebuilding only the affected aggregate chain from preserved field projections plus terminal replacements; spend the feature immediately in `kernel/src/boot_update.mc`; deliberately reject overlapping subtree-plus-descendant paths such as `cell` together with `cell.state` in the same update instead of inventing hidden ordering or precedence semantics | tradeoff: the repository now removes the remaining honest nested record-rebuild noise at the active kernel boundary with a real executable feature, but record update still deliberately stops short of array or slice element updates, computed paths, hidden mutation, and overlapping-path precedence rules; callers that truly need both whole-subtree replacement and descendant replacement in one expression must keep that logic explicit in source for now | revisit trigger: reopen only when a real admitted owner is materially distorted by the overlapping-path rejection or another bounded nested-update limit, and the narrowest next step can stay explicit and deterministic without widening into a broader state framework | reference: docs/plan/active/phase199_nested_record_update_field_path_follow_through.txt, compiler/parse/parser_expr.cpp, compiler/sema/check.cpp, compiler/mir/mir_lower.cpp, kernel/src/boot_update.mc
- 2026-04-16 | area: kernel | context: Phase 198 multi-handle transfer follow-through after Phase 185 had already admitted one explicit transferred-handle path but deliberately stopped at one attached handle per message | limiter: the real pressure was whether the current explicit authority-transfer model could carry one more bounded real shape, not whether the repository needed ordinary handle-bearing traffic, a receiver-visible rights algebra, a capability framework, or compiler/runtime widening; the existing monotonic grant table, transfer-only endpoint, and explicit restart split were already the honest owners if the new path stayed local to that same owner family | decision: keep the Phase 185 one-handle path intact and add exactly one additive two-handle shape on `TRANSFER_ENDPOINT_ID`; make `transfer_grant.grant_consume(...)` resolve either one grant or exactly two contiguous monotonic grant ids; widen `service_effect.Message` only enough to carry a second resolved transferred endpoint id; keep `Effect` unchanged; make `transfer_service` own one retained `(log, kv)` endpoint pair plus one tiny follow-through command family (`B`, `L`, `K`); keep ordinary named traffic closed to handle metadata and keep restart explicit and init-owned so restarting `transfer_service` clears the retained pair rather than reviving transferred authority ambiently | tradeoff: the repository now proves that one bounded multi-handle pressure step still fits the current explicit transfer-only authority model, but it still deliberately stops short of ordinary multi-handle traffic, generic endpoint pairs, rights algebra, capability borrowing, or any compiler, sema, MIR, backend, ABI, target, runtime, or `hal` growth; the two-handle path is intentionally specialized to the current real consumer pair (`log_service` plus `kv_service`) rather than pretending to be a general subsystem | revisit trigger: reopen only when a real admitted consumer needs more than two transferred handles, a second retained pair shape beyond the current `(log, kv)` contract, ordinary handle-bearing traffic to non-transfer services, or a receiver-visible rights model that cannot stay local to the current transfer owner | reference: docs/plan/active/phase198_multi_handle_transfer_follow_through_under_explicit_authority.txt, kernel/src/transfer_grant.mc, kernel/src/transfer_service.mc, kernel/src/kernel_dispatch.mc, kernel/src/scenario_transfer.mc, tests/system/kernel_reset_lane_phase198_multi_handle_transfer/src/main.mc
- 2026-04-16 | area: kernel | context: Phase 201 bounded second coordinated retained audit-lane probe after Phase 197 had already admitted one explicit coordinated retained workset and the kernel already contained a real kv-write advisory relationship into `log_service` | limiter: the real pressure was whether a second explicit coordinated retained bundle could still stay honest under the current shell lifecycle and init-owned restart model, not whether the repository needed a retained-bundle registry, dynamic dependency graph, richer shell discovery, or a persistence framework; the current cross-owner relationship was already real in `kernel_dispatch_kv(...)`, so the narrowest next step was one more explicit target rather than a generalized coordination layer | decision: add one additive shell lifecycle target, `TARGET_AUDIT` (`A`), keep query and restart on the existing `XQ`/`XR` family, and implement one explicit `init.restart_retained_audit_lane(...)` helper that snapshots and reloads the `kv_service` plus `log_service` pair directly; keep coordination explicit, fixed-target, and init-owned rather than inferring bundles or introducing a registry; keep identity follow-through deferred instead of broadening this phase beyond the second coordinated restart pressure | tradeoff: the repository now proves that two bounded coordinated retained pairs can still fit the current flat owner model, but it still deliberately stops short of generic retained bundles, target discovery, dynamic dependency policy, or broader persistence machinery; the repo-project kernel scenario runs this probe on a fresh boot state so the coverage stays deterministic without coupling the contract to prior scenario history | revisit trigger: reopen only when a third coordinated retained bundle appears, when two explicit coordinated helpers in `init.mc` become more distortive than honest, or when a real caller needs coordinated retained identity or discovery across multiple bundles rather than one fixed target at a time | reference: docs/plan/active/phase201_bounded_second_coordinated_retained_audit_lane_probe.txt, kernel/src/init.mc, kernel/src/kernel_dispatch.mc, kernel/src/scenario_audit_coordination.mc, tests/system/kernel_reset_lane_phase201_retained_audit_coordination/src/main.mc
- 2026-04-16 | area: kernel | context: Phase 200 bounded coordinated retained workset identity query follow-through after Phase 197 had already admitted one explicit retained workset restart target but deliberately kept identity queries per-owner only | limiter: the real pressure was one caller-visible marker for the already admitted coordinated workset, not a generic bundle registry, richer discovery surface, or broader persistence identity model; the shell control family and boot-owned generation model already carried enough structure if coordinated identity stayed one explicit boot-owned counter | decision: add one `workset_generation` field to `KernelBootState`, bump it only in `init.restart_retained_workset(...)`, expose it through `XIW!` on the existing shell control path, and keep the reply to one fixed four-byte generation payload using the shared `service_identity.generation_payload(...)` helper; keep individual service identity queries unchanged and keep individual service restarts from mutating the coordinated workset identity | tradeoff: the repository now has one bounded caller-visible coordinated identity surface, but it still deliberately stops short of bundle discovery, richer identity payloads, dynamic retained-dependency graphs, or another framework-level persistence identity layer; coordinated identity is explicit and boot-owned rather than inferred from the per-service generation pair | revisit trigger: reopen only when a second coordinated retained bundle appears, when clients need a richer coordinated identity payload than one generation counter, or when retained relationships become dynamic enough that one explicit boot-owned marker stops being honest | reference: docs/plan/active/phase200_bounded_coordinated_retained_workset_identity_query_follow_through.txt, kernel/src/boot.mc, kernel/src/init.mc, kernel/src/kernel_dispatch.mc, kernel/src/scenario_workset_identity.mc
- 2026-04-16 | area: kernel | context: Phase 197 bounded cross-service retained restart coordination after the repository had already landed explicit owner-local retained reload for `log_service`, `kv_service`, and `queue_service`, but still lacked one caller-visible workflow whose meaning depended on restart preserving a relationship across two retained owners | limiter: the real pressure was one bounded coordinated retained workflow, not a snapshot registry, persistence subsystem, supervisor, dynamic dependency graph, or compiler-facing feature; queue bytes only become caller-meaningful through retained key-value state, but the existing shell lifecycle and init-owned restart surfaces were already strong enough to carry one coordinated step if the pair stayed explicit | decision: keep `init.mc` as the explicit restart owner and add exactly one additive coordinated retained target on the shell lifecycle path, `TARGET_WORKSET` (`W`), for the retained `kv_service` plus `queue_service` pair; implement `restart_retained_workset(...)` by snapshotting both owners first and then reloading both explicitly; make `XQW!` report reload and `XRW!` enact the coordinated restart; keep service identity queries per-owner only and reject `XIW!` instead of inventing a synthetic bundle identity; keep compiler, sema, MIR, backend, ABI, target, runtime, and `hal` surfaces closed | tradeoff: the repository now proves one bounded cross-owner retained contract with real implementation and user-visible control, but it still deliberately stops short of generic snapshot bundles, dynamic coordination policy, durability, transactions, or framework-level persistence machinery; coordinated restart identity remains the composition of the individual service generation bumps rather than a new bundle identity model | revisit trigger: reopen only when a second coordinated retained bundle or richer dependency policy appears, when callers need bundle identity rather than per-service identity, or when one coordinated restart can no longer stay honest as one flat init-owned helper | reference: docs/plan/active/phase197_bounded_cross_service_retained_restart_coordination_probe.txt, kernel/src/init.mc, kernel/src/kernel_dispatch.mc, kernel/src/shell_service.mc, kernel/src/scenario_coordination.mc
- 2026-04-15 | area: kernel | context: Phase 196 queue observability and peek follow-through after Phase 187 had already admitted one retained FIFO owner with enqueue, dequeue, and explicit reload-on-restart, but callers still had no honest non-destructive visibility into queued state except by consuming it | limiter: the real pressure was one bounded user-facing observability gap, not a mailbox subsystem, iteration API, subscription model, numeric packing feature, or compiler/runtime expansion; the existing shell frame and effect model already carried enough structure if the queue owner remained the authority for both count and peek semantics | decision: keep `queue_service.mc` as the semantic owner, admit exactly two new non-mutating operations through the existing compact queue family, and encode them as in-band queue verbs sent from `shell_service.mc` to the queue endpoint: `QC!!` becomes a two-byte queue message `<C,!>` returning `Ok` with `reply_payload_len = s.len`, and `QP!!` becomes `<P,!>` returning `Ok` with one reply byte equal to the head value without mutation or `InvalidArgument` when empty; extend the live reset-lane scenario and add one focused reset-lane fixture; keep compiler, sema, MIR, backend, ABI, target, runtime, and hal surfaces closed | tradeoff: the retained queue is now honestly inspectable under repeated use and across reload-style restart, but the system still deliberately stops short of queue iteration, multi-element dumps, notifications, blocking dequeue, or any framework-level observability machinery; queue control remains compact and explicit, and count continues to reuse `reply_payload_len` instead of widening numeric packing | revisit trigger: reopen only when a real admitted consumer needs queue behavior beyond count plus next-element observation, or when a future queue-policy phase can show that blocking receive, iteration, or subscriptions are the narrowest honest next owner rather than framework drift | reference: docs/plan/active/phase196_queue_observability_and_peek_follow_through.txt, kernel/src/queue_service.mc, kernel/src/shell_service.mc, kernel/src/serial_protocol.mc, kernel/src/scenarios.mc
- 2026-04-15 | area: parser + sema + mir + kernel | context: Phase 195 narrow record-update syntax for owner-local state mutation after Phase 164 had already deferred struct embedding in favor of explicit `with_*` helpers and Phases 191 and 192 had already removed aggregate type boilerplate; active kernel owners still had to restate every untouched field when updating `KernelBootState` and adjacent state records | limiter: the real pressure was whole-record copy boilerplate, not hidden mutation, object methods, nested path editing, collection updates, or a new runtime state model; broadening the feature past shallow named-field replacement would have mixed language ergonomics with data-model work the repository still has no admitted pressure for; the current aggregate machinery already provided enough lowering power if record update stayed a sema-owned copy-with form | decision: admit exactly one explicit immutable source form, `base with { field: value, ... }`; add a dedicated AST node and parser keyword; make sema require a real record-typed base value, named fields only, duplicate-field rejection, and replacement-type checking; lower the form in MIR by projecting preserved base fields and reusing the existing aggregate-init instruction rather than introducing a new mutation or runtime instruction; spend the feature immediately in `kernel/src/boot_update.mc` and `kernel/src/serial_shell_path.mc` | tradeoff: the repository now removes the dominant owner-local whole-record noise with a real executable feature, but record update remains intentionally shallow: no nested path updates, no array or slice element updates, no hidden mutation, and no framework-style state operators; callers still rebuild one explicit inner aggregate when they need to replace a nested record field inside a wrapper like `ServiceCell<T>` | revisit trigger: reopen only when a real admitted owner is materially distorted by the remaining lack of nested field-path update and the narrowest next step can stay explicit and immutable without reopening hidden mutation or a broader state framework | reference: docs/plan/active/phase195_narrow_record_update_syntax_for_owner_local_state_mutation.txt, compiler/parse/parser_expr.cpp, compiler/sema/check.cpp, compiler/mir/mir_lower.cpp, kernel/src/boot_update.mc, kernel/src/serial_shell_path.mc
- 2026-04-15 | area: parser + sema + mir + kernel | context: Phase 194 narrow explicit integer conversion for compact protocol packing after Phase 188 and Phase 193 had each exposed the same owner-local distortion: compact byte payloads needed truth from wider integer state, and the running kernel carried a tracked four-byte generation shadow beside the real numeric generation counter to avoid reopening coercion policy | limiter: the repository already had a broader parenthesized explicit-conversion path for earlier admitted needs, but turning the new direct `T(expr)` spelling into a general bare cast surface would have widened source syntax for distinct, alias, slice, pointer, and other explicit-conversion families without real pressure; implicit mixed-width coercion remained explicitly out of scope | decision: admit bare target-typed conversion syntax only for builtin scalar integer target names (`i8`, `i16`, `i32`, `i64`, `isize`, `u8`, `u16`, `u32`, `u64`, `usize`, `uintptr`); mark that syntax distinctly in the AST so sema can require an integer source expression for the new form; reuse the existing MIR and LLVM explicit numeric conversion path; keep the older parenthesized explicit-conversion syntax as the only source spelling for the broader preexisting conversion families; spend the feature immediately in `kernel/src/service_identity.mc` and the boot-owned identity wiring by packing generation bytes directly from the numeric generation counter and deleting the tracked `generation_payload` shadow state from `ServiceMark` and `ServiceCell<T>` | tradeoff: the repository now has the direct narrow integer-packing form needed by the real kernel owner and removes the duplicated generation byte state, but bare `T(expr)` still does not apply to distinct, alias, pointer, slice, or other non-integer conversion families; those continue to use the older parenthesized explicit-conversion surface where already admitted | revisit trigger: reopen only when a real admitted owner is still materially distorted by the remaining lack of bare target-typed explicit conversion outside builtin scalar integers, and the narrowest honest next step can stay explicit without reopening implicit coercion policy or a general cast framework | reference: docs/plan/active/phase194_narrowing_int_conversion.txt, compiler/parse/parser_expr.cpp, compiler/sema/check.cpp, kernel/src/service_identity.mc, kernel/src/boot.mc
- 2026-04-15 | area: kernel + compiler | context: Phase 193 bounded shell service identity query follow-through after Phase 189 had already admitted shell-visible lifecycle class and restart control, and Phase 182 had already made restart-visible generation truth explicit in boot state | limiter: the repository needed one real shell-visible identity query, but widening into discovery, richer control bundles, a supervisor-facing surface, or compiler work would have exceeded the actual pressure; the current language also still lacks a narrow explicit integer-conversion form for packing a `u32` generation directly into a `[4]u8` shell payload, so a direct byte-pack from the numeric counter was not expressible honestly in source | decision: keep `shell_service.mc` as the parser and reply-shaping owner, keep `kernel_dispatch.mc` as the bounded shell-control executor, reuse the existing shell-endpoint control hop for `XI<target>!`, and keep compiler surfaces closed; carry a shell-visible four-byte generation payload alongside the numeric generation in the boot-owned service cells and `ServiceMark` so identity replies can stay fixed-width without introducing implicit narrowing or broader runtime framework work | tradeoff: the repository now exposes restart-visible generation directly through the existing shell path for restartable boot-wired services, but it still deliberately stops short of discovery, dynamic target lookup, richer identity bundles, or compiler-owned numeric-conversion support; generation truth is duplicated as both a numeric counter and a tracked byte payload until a narrow explicit conversion feature is admitted or stronger pressure appears | revisit trigger: reopen only when a real admitted consumer needs richer caller-visible identity than the current fixed four-byte generation payload, or when repeated owner-local byte-packing pressure justifies explicit integer-conversion syntax rather than parallel tracked payload state | reference: docs/plan/active/phase193_bounded_shell_service_identity_query_follow_through.txt, kernel/src/shell_service.mc, kernel/src/kernel_dispatch.mc, kernel/src/service_identity.mc
- 2026-04-15 | area: parser + sema + kernel | context: Phase 192 inferred brace aggregate follow-through after Phase 191 had already admitted explicit typed aggregate tables, but active owner code still had to restate obvious destination types in contexts where the target aggregate shape was already fixed by a typed binding, return type, call parameter, assignment target, or enclosing aggregate field | limiter: the repository needed to remove real owner-local boilerplate without admitting free-floating untyped aggregate values, hidden coercion, or broader compile-time execution; the existing aggregate-init representation and const/backend machinery were sufficient if sema could attach expected aggregate shape only at owners that already carry an explicit target type | decision: teach the parser to admit bare `{ ... }` aggregate-init expressions, then let sema infer the target only from existing expected-type contexts by attaching inferred aggregate shape to the aggregate expression and recursing that shape into nested child aggregate literals; spend the feature immediately in `kernel/src/service_topology.mc` by switching slot records, fallback returns, and topology tables to inferred braces | tradeoff: the language now supports the honest low-boilerplate source form in typed contexts and real owner code uses it, but standalone brace aggregates without an expected type remain unsupported and trailing commas are still rejected; this is accepted because the owner-local pressure is resolved without turning aggregate literals into free-floating partially typed values | revisit trigger: reopen only when a real admitted owner is still blocked specifically by the lack of standalone aggregate values without an expected type, or when aggregate literal list editing pressure justifies separate trailing-comma grammar work | reference: docs/plan/active/phase192_inferred_brace_aggregate_follow_through.txt, compiler/parse/parser_expr.cpp, compiler/sema/check.cpp, kernel/src/service_topology.mc
- 2026-04-15 | area: parser + sema + kernel | context: Phase 191 constant aggregate tables for flat owner data after Phase 178 had already identified `[N]ServiceSlot` tables as the honest representation for static topology but the parser still rejected explicit typed array aggregate literals like `[4]i32{ 1, 2, 3, 4 }` | limiter: the repository needed a real language admission that active kernel code could consume immediately, but inferred `{ ... }` literals, const functions, broader compile-time execution, or data-driven framework work would have widened well past the pressure point; the existing aggregate-const, `.mci`, MIR, and LLVM global-const machinery was already sufficient for the admitted slice once the front-end could represent a typed array aggregate init | decision: admit only explicit typed aggregate literals by reusing `Expr.type_target` on `AggregateInitExpr`, teach the parser to accept type-expression aggregate bases, route sema typing and constant evaluation through the existing aggregate path, and spend the feature immediately in `kernel/src/service_topology.mc` through `SERVICE_SLOTS` and `SERVICE_RESTART_MODES`; keep inferred brace literals and broader const-eval work deferred | tradeoff: the repository now has real constant aggregate tables for flat owner data with no framework growth, and the active kernel reads more honestly, but source still requires an explicit type target such as `[8]ServiceSlot{ ... }` instead of an inferred `{ ... }` form; this is accepted because the explicit type is clear and the narrower feature closes the real owner-local pressure directly | revisit trigger: reopen only when another admitted owner proves that explicit type-target aggregate literals are themselves the main source of boilerplate and inferred brace literals can be admitted without hidden coercion or broader compile-time execution semantics | reference: docs/plan/active/phase191_constant_aggregate_tables_for_flat_owner_data.txt, compiler/parse/parser_expr.cpp, compiler/sema/check.cpp, kernel/src/service_topology.mc
- 2026-04-15 | area: sema + tooling | context: Phase 190 multi-file diagnostic ownership follow-through; project-mode multi-file modules already merged logical-module parts via synthetic line offsets (`kMergedModulePartLineStride = 1000000`) before semantic checking, but the `Checker` stored only one `file_path_` and emitted all diagnostics against that primary path regardless of which part the erroring span came from | limiter: fixing this required no AST identity change, no module-system redesign, and no new import or artifact surface; the synthetic-line encoding already encoded the part index directly in every span's line number (`part_index = line / stride`, `original_line = line % stride`), so a lightweight lookup from `CheckOptions` was sufficient; the only real question was how to thread the part-path table from the builder into the checker without inventing new infrastructure | decision: add `module_part_paths` (vector of paths indexed by part) and `module_part_line_stride` to `CheckOptions`; populate them in `CompileModuleGraph` from `CanonicalizeModuleSourcePaths(node.source_paths)` and `kMergedModulePartLineStride` when `node.source_paths.size() > 1`; modify `Checker::Report()` to compute `part = span.begin.line / stride` and resolve the correct path and original line before emitting the diagnostic; keep `.mci`, build-state, and logical-module artifact identity keyed by logical module rather than by part file | tradeoff: semantic diagnostics for merged logical modules now name the exact contributing file and original line number; the fix is additive to `CheckOptions` and `Checker` only, with no AST, module, MIR, backend, or runtime change; the approach is intentionally simple: the part-path lookup is O(1) array index; the existing `TestDuplicateTopLevelAcrossModulePartsFails` regression was extended to assert the non-primary file name appears in the diagnostic output | revisit trigger: reopen only if column-number recovery within a part becomes necessary (currently the fix recovers file and line but not column, since column numbers are not affected by the line offset scheme), or if direct-source multi-file mode is admitted and needs the same treatment | reference: docs/plan/active/phase190_multifile_diagnostic_ownership_follow_through.txt, compiler/sema/check.h, compiler/sema/check.cpp, compiler/driver/builder.cpp, tests/tool/tool_multifile_module_suite.cpp
- 2026-04-15 | area: kernel | context: Phase 189 bounded shell lifecycle control follow-through after Phase 186 moved static lifecycle classification into `service_topology.mc`, Phase 178 kept restart endpoint-driven and explicit, and the reset-lane shell had already become the bounded user-facing route for compact service control | limiter: the repository needed one real user-facing lifecycle interaction, but a supervisor, admin console, dynamic service registry, new effect kind, or any compiler, sema, MIR, backend, ABI, target, runtime, or `hal` expansion would have widened far past the admitted pressure; the current effect model already supports one explicit control follow-through if the shell can route a bounded request back to a live owner | decision: add one compact shell lifecycle family `XQ<target>!` and `XR<target>!`; keep target classification and reply shaping in `shell_service.mc`, keep static lifecycle facts in `service_topology.mc`, and keep restart enactment in `init.mc`; implement restart by reusing the existing send effect path back to `SHELL_ENDPOINT_ID`, then let `kernel_dispatch.mc` perform one bounded control step that calls `init.restart(...)`; keep query replies and restart replies as the same two-byte `<target, class>` contract with `N`, `L`, and `R` lifecycle classes | tradeoff: the repository now has one honest shell-visible lifecycle control path that users can exercise directly, but it still deliberately stops short of dynamic discovery, text shells, richer lifecycle payloads, a broader control plane, or any compiler-facing surface growth; the restart control path is intentionally one fixed shell-endpoint hop, not a new effect algebra | revisit trigger: reopen only when a second admitted shell-visible control family or richer lifecycle payload proves that reusing the shell endpoint for bounded control routing has become more distortive than explicit, or when dynamic service naming or broader restart policy becomes a real admitted need | reference: docs/plan/active/phase189_bounded_shell_lifecycle_control_follow_through.txt, kernel/src/shell_service.mc, kernel/src/kernel_dispatch.mc, kernel/src/init.mc
- 2026-04-15 | area: kernel | context: Phase 188 restart-visible ticket service follow-through after Phase 182 split stable routing from replaced service-instance identity, Phase 186 kept restart enactment explicit and init-owned, and Phase 187 proved that retained-state follow-through could stay per-service without widening into framework work; the running system still lacked one user-facing service where pre-restart service-issued state becomes invalid in a way a caller must handle | limiter: the narrowest real question was one compact reset-style owner over the existing four-byte shell route; a capability framework, session registry, authentication model, supervisor, transport rewrite, or compiler, sema, MIR, backend, ABI, target, runtime, or hal expansion would have widened far past the admitted defect; the current language also still lacks a narrow explicit integer-cast surface for compact protocol packing, so mixed-width comparison between a byte payload and a wider generation marker had to stay local rather than become a compiler phase | decision: add one bounded `ticket_service` that issues two-byte `[epoch, id]` tickets, consumes them once, rejects stale tickets with `InvalidArgument` plus `S`, rejects unknown or already-consumed tickets with `InvalidArgument` plus `I`, adds one boot-wired `TICKET_ENDPOINT_ID` classified as `ServiceRestartMode.Reset`, and routes compact shell commands `TI!!` and `TU<epoch><id>` through the existing serial or shell or dispatch path; keep restart-visible truth local by reusing the existing stable endpoint plus stable pid plus bumped generation split rather than inventing a second identity subsystem; keep the compiler and runtime surfaces closed and model the compact epoch directly inside the service state instead of reopening integer-cast support in this phase | tradeoff: the running system now has one honest user-facing stale-state workflow, but it still deliberately stops short of capabilities, sessions, richer ticket metadata, supervisor policy, automatic rediscovery, or multi-ticket families larger than the bounded owner-local table; the epoch is currently byte-sized because that matches the compact shell frame and avoids widening numeric-conversion rules | revisit trigger: reopen only when a real admitted consumer needs richer caller-visible ticket payloads, ticket transfer, broader session semantics, or an explicit numeric-conversion language feature strong enough to justify compiler work beyond this owner-local service slice | reference: docs/plan/active/phase188_restart_visible_ticket_service_follow_through.txt, kernel/src/ticket_service.mc, kernel/src/scenarios.mc, tests/system/kernel_reset_lane_phase188_ticket_restart/src/main.mc
- 2026-04-15 | area: kernel | context: Phase 187 retained queue service follow-through after Phase 179 and Phase 181 proved explicit retained-state reload on append-only and key-addressed owners, and Phase 186 fixed the lifecycle-owner split so static restart class now lives in `service_topology.mc` while restart enactment stays in `init.mc`; the repository still had no evidence that the current explicit restart-with-reload shape could carry ordered state with destructive consumption without widening into mailboxes, async receive, or a broader queue framework | limiter: the narrowest real question was one user-facing FIFO owner under the existing four-byte shell route and flat dispatch path; a mailbox subsystem, scheduler work queue, service framework, persistence layer, or any compiler, sema, MIR, backend, ABI, target, runtime, or hal expansion would have widened far past the admitted defect | decision: add one bounded `queue_service` with a fixed-size FIFO, explicit `Exhausted` on enqueue when full, explicit `InvalidArgument` on dequeue when empty, and explicit `QueueSnapshot` plus `queue_snapshot(...)` and `queue_reload(...)`; add one boot-wired queue endpoint and classify it as `ServiceRestartMode.Reload` in `service_topology.mc`; thread the service through the existing flat owners (`boot`, `kernel_dispatch`, `shell_service`, `serial_protocol`, `init`, `scenarios`) and add one reset-lane workflow fixture proving full-before-restart, preserved FIFO order after restart, and empty-after-consume truth; make no compiler, sema, MIR, backend, ABI, target, runtime, or hal expansion | tradeoff: the repository now proves that retained-state reload carries an ordered consumptive service without widening into a queue framework, but it still deliberately stops short of subscriptions, blocking receive, mailbox semantics, generic collection helpers, count or peek protocol growth, or durability claims | revisit trigger: reopen only when a real admitted consumer needs queue behavior beyond one bounded owner-local FIFO with explicit enqueue and dequeue, or when ordered retained state starts recurring strongly enough to justify a broader shared owner rather than another explicit service-local implementation | reference: docs/plan/active/phase187_retained_queue_service_follow_through.txt, kernel/src/queue_service.mc, kernel/src/init.mc, tests/system/kernel_reset_lane_phase187_retained_queue_reload/src/main.mc
- 2026-04-15 | area: kernel | context: Phase 186 boot decomposition and lifecycle audit after Phase 178 made restart endpoint-driven, Phases 179 and 181 made retained reload explicit, Phase 182 split routing continuity from instance continuity, and Phase 185 kept IPC growth local to the transfer path; the current running system still had no evidence for a supervisor or boot rewrite, but restartability and retained-reload class were being answered by `init.can_restart(...)` even though those facts are static properties of the boot-wired topology rather than dynamic restart policy | limiter: a lifecycle subsystem, supervisor, registry, or compiler-facing service model would have widened far past the concrete blur; the narrowest honest repair was to move only the static lifecycle classification to the nearest existing owner and keep restart enactment flat and explicit | decision: keep `boot.mc` as state construction, `kernel_dispatch.mc` as runtime dispatch, `scenarios.mc` as scripted live-operation owner, and `init.mc` as explicit restart executor; move the static lifecycle classification to `service_topology.mc` by adding `ServiceRestartMode`, `service_restart_mode(...)`, `service_can_restart(...)`, and `service_restart_reloads_state(...)`; remove `init.can_restart(...)`; keep `init.restart(state, endpoint)` as the caller-visible execution path and preserve the current per-service restart semantics unchanged; make no compiler, sema, MIR, backend, ABI, target, runtime, or hal expansion | tradeoff: the source now states lifecycle ownership more honestly and avoids treating a topology fact as restart-policy logic, but the system still deliberately stops short of per-service health state, retry counters, serial or shell restart, dynamic service classes, or any supervisor machinery; topology now names a small amount of lifecycle metadata in addition to slots, which is acceptable because the classification is static and boot-wired rather than dynamic | revisit trigger: reopen when restart policy needs runtime health or retry state that static topology classification cannot express, when a service's restart semantics become dynamic rather than boot-wired, or when a real phase proves the next owner is a supervisor rather than the current explicit split | reference: docs/plan/active/phase186_boot_decomposition_and_lifecycle_audit.txt, kernel/src/service_topology.mc, kernel/src/init.mc, tests/system/kernel_reset_lane_phase178_topology_restart/src/main.mc
- 2026-04-15 | area: kernel | context: Phase 185 IPC shape re-audit after Phase 182 made restart identity explicit, Phase 183 made endpoint-name routing explicit, and Phase 184 made handle-bearing metadata an explicit authority boundary; the running system still had no honest way to admit one transferred-handle path without either reopening payload width or turning handle metadata ambient again | limiter: the narrowest real mismatch was not payload width, typed transport, reply correlation, or a compiler boundary; it was the total absence of one explicit transferred-handle runtime path; a capability framework, registry, rights algebra, shell widening, or compiler, sema, MIR, backend, ABI, target, runtime, or hal expansion would have widened far past the admitted defect | decision: keep Phase 184 gateway rejection as the default ordinary-message rule, but admit one explicit exception through `TRANSFER_ENDPOINT_ID`; add a boot-owned `transfer_grant` table that issues monotonic one-shot grant ids so stale slot values do not regain authority on table reuse; widen `service_effect.Message` only enough to carry attached-handle metadata plus the resolved transferred endpoint id; add `transfer_service` as the one bounded handle consumer that emits one follow-through send; route handle-bearing observations only through a new transfer branch in `kernel_dispatch_step(...)`; add one reset-lane workflow fixture covering named-path rejection, destructive-on-success transfer, stale-holder refusal, restart-stable routing, and post-restart no-ambient-revival; make no compiler or transport expansion | tradeoff: the repository now has one honest transferred-handle runtime path without widening payloads, transport algebra, or the compiler, but the admitted authority model remains deliberately narrow: one handle only, one transfer-only endpoint, no shell syntax changes, no ambient lookup, no multi-handle family, and no general capability subsystem | revisit trigger: reopen when a real admitted consumer needs more than one transferred handle per message, a receiver-visible rights algebra, ordinary handle-bearing traffic outside the transfer endpoint, or another transport feature that cannot stay local to this narrow path | reference: docs/plan/active/phase185_ipc_shape_reaudit_under_restart_and_retained_state.txt, kernel/src/transfer_grant.mc, kernel/src/transfer_service.mc, kernel/src/kernel_dispatch.mc, tests/system/kernel_reset_lane_phase185_transferred_handle/src/main.mc
- 2026-04-15 | area: tooling | context: Phase 184B workflow-suite maintainability follow-through after the reset-lane family and project-validation coverage had accumulated into one large catalog-style `mc_tool_workflow_unit` implementation file | limiter: the suite's explicit assertions and fixture-driven style were already the right semantic level, so a large test DSL, generated registry, or framework rewrite would have hidden user-visible workflow contracts instead of fixing the real problem; the strongest duplication sat in the reset-lane family where build-plus-run success cases differed mostly by fixture path and output stem | decision: split the active workflow implementation by behavioral domain into `tool_help_suite.cpp`, `tool_test_command_suite.cpp`, `tool_project_validation_suite.cpp`, `tool_multifile_module_suite.cpp`, and `tool_kernel_reset_lane_suite.cpp`, keep one thin `tool_workflow_orchestrator.cpp` entrypoint, and table-drive the reset-lane family through one `ResetLaneScenario` list plus one shared runner; make no compiler, sema, MIR, backend, ABI, target, runtime, or `hal` expansion and introduce no generic test DSL | tradeoff: the suite now compiles and reads in smaller behavior-owned units and the reset-lane cluster grows through data entries rather than fresh handwritten runners, but the repository still keeps explicit per-behavior assertions instead of a more compressed registry or metadata-driven assertion layer | revisit trigger: reopen only when one behavior-owned suite grows back into catalog-sized repetition, when another repetitive workflow family is dominated by identical scaffolding rather than distinct assertions, or when filtering and progress reporting pressure justifies a narrow registration table without hiding the user-visible contracts | reference: docs/plan/active/phase184b_tool_workflow_suite_domain_split.txt, tests/tool/tool_workflow_orchestrator.cpp, tests/tool/tool_kernel_reset_lane_suite.cpp
- 2026-04-15 | area: kernel | context: Phase 184 capability propagation and leakage probe after Phase 182 made restart identity explicit and Phase 183 made endpoint-name routing explicit; the running system still accepted any `ReceiveObservation` with non-zero `received_handle_slot` or `received_handle_count` as ordinary named traffic because the dispatch gateway never inspected those fields | limiter: a real capability-transfer subsystem, handle registry, ambient authority framework, or compiler-facing authority feature would have widened far beyond the admitted leak; the narrowest honest fix was to tighten the already named dispatch owner boundary so explicit authority-bearing metadata could not ride along the public-name path silently | decision: add `observation_has_received_handle(obs: ReceiveObservation) bool` to `kernel/src/syscall.mc`; make `kernel_dispatch_step(...)` in `kernel/src/kernel_dispatch.mc` reject any non-zero received-handle metadata with `InvalidCapability` before the boot-wired endpoint-name gate; add one dedicated Phase 184 workflow fixture covering pre-restart rejection, post-restart stale-handle rejection, plain name-based traffic after restart, and repeated hostile attempts; make no capability-framework, supervisor, compiler, sema, MIR, backend, ABI, target, runtime, or hal expansion | tradeoff: the current running-system slice now states the authority boundary honestly and blocks the concrete leak without pretending capability transfer exists; name-based routing stays simple and stable, but any future real handle-transfer phase must reopen the dispatch boundary intentionally instead of inheriting an accidental permissive path | revisit trigger: reopen when a real phase admits handle-bearing receive traffic as ordinary runtime behavior, when a transferred-handle consumer needs something narrower than unconditional gateway rejection, or when authority-routing semantics cease to be separable from stable public-name routing | reference: docs/plan/active/phase184_capability_propagation_and_leakage_probe.txt, kernel/src/syscall.mc, kernel/src/kernel_dispatch.mc, tests/system/kernel_reset_lane_phase184_capability_leakage/src/main.mc
- 2026-04-15 | area: kernel | context: Phase 183 endpoint ownership and authority audit after Phase 182 made restart identity explicit; the running system had five static boot-wired endpoint ids serving as topology constants, routing selectors, and public service names at once, with no explicit statement of whether knowing an endpoint id conveys authority | limiter: a full capability model, endpoint allocator, naming service, or compiler-known endpoint type would have widened far past the real classification question; the narrowest honest answer required only one new helper function and one explicit gateway guard | decision: add `endpoint_is_boot_wired(endpoint: u32) bool` to `service_topology.mc` as the explicit public-name gate; add an early guard in `kernel_dispatch_step` in `kernel_dispatch.mc` that returns `InvalidEndpoint` for any endpoint id that is not boot-wired rather than relying on implicit fall-through; add classification comments to `service_topology.mc` and `kernel_dispatch.mc` stating the split contract: endpoint ids are stable public names and routing keys, not authority-bearing values; the intended future authority surface is the reserved handle-slot transfer mechanism in `syscall.ReceiveObservation`; admit one dedicated behavior-first test fixture; make no compiler, sema, MIR, backend, ABI, target, runtime, or hal expansion | tradeoff: the endpoint address model is now explicit and verifiable in code rather than implied by fall-through behavior; callers that know a valid boot-wired endpoint name may address that service without a capability check, which is the correct and intentional design for the current flat boot-wired simulation kernel; the authority surface and the routing surface remain distinct as two separately named concepts even though the current system has no runtime capability check; later phases working on stale authority, capability leakage, or IPC restart pressure inherit one stated contract rather than an accidental one | revisit trigger: reopen when a real phase needs a capability check at the dispatch gateway (i.e. when knowing the name alone should not be sufficient), when handle-based authority becomes a real routing gate rather than a reserved field, or when dynamic endpoint assignment replaces the current static boot-wired model | reference: docs/plan/active/phase183_endpoint_ownership_and_authority_audit.txt, kernel/src/service_topology.mc, kernel/src/kernel_dispatch.mc, tests/system/kernel_reset_lane_phase183_endpoint_authority/src/main.mc
- 2026-04-15 | area: kernel + compiler | context: boot-state follow-through after Phase 182 restart identity made per-service generation tracking explicit and the resulting `bootwith_*` / `bootrestart_*` family showed repeated state-copy pressure in `kernel/src/boot.mc` | limiter: the runtime shape had enough repeated pressure to justify grouping each restartable service's state with its generation counter, but the first clean helper design (`ServiceCell<T>` plus generic helper functions like `service_cell_with_state<T>(...)` and `service_cell_restart<T>(...)`) immediately hit a current compiler boundary: user-body generic functions that return value-shaped generic records are still rejected by the bootstrap backend as requiring unsupported monomorphization; the language also still lacks record-update syntax, so whole-record replacement helpers remain explicit | decision: keep the structural improvement but stay inside admitted compiler truth: introduce one generic record `ServiceCell<T>` inside `kernel/src/boot.mc`, reshape `KernelBootState` to hold `log`, `kv`, and `echo` cells instead of separate state and generation fields, and replace the rejected generic helper functions with explicit per-service aggregate construction in the small `bootwith_*` and `bootrestart_*` wrappers; preserve `path_state` as the one non-cell field because it is structurally different from the restart-tracked services | tradeoff: the boot surface now removes unrelated generation bookkeeping from every service-specific helper and makes the repeated pressure local to one small wrapper family, but it still does not collapse those wrappers to a generic helper layer or to record-update syntax; the current compiler boundary remains explicit rather than hidden behind a workaround that only compiles accidentally | revisit trigger: reopen compiler work only when a real admitted consumer needs executable support for value-shaped user-body generic helper functions beyond the erased-ABI pointer-only family, or when repeated record-update pressure across owners becomes strong enough to justify a narrow record-update syntax or equivalent language feature | reference: kernel/src/boot.mc, kernel/src/kernel_dispatch.mc, docs/plan/active/phase182_restart_identity_continuity_probe.txt
- 2026-04-15 | area: kernel | context: Phase 182 restart identity continuity probe after Phase 181 proved that both `log_service` and `kv_service` can survive explicit restart with retained state while `ServiceRef` still names a stable endpoint id | limiter: stable endpoint ids, fixed boot-wired pids, and explicit retained-state reload could now make restart look like uninterrupted same-service continuity even though init still replaces the running service state; solving that ambiguity with a registry, supervisor, rebinding layer, compiler-known identity type, or persistence framework would have widened far past the real owner gap | decision: keep `ServiceRef` as the stable routing surface, add a runtime-owned `ServiceMark` with endpoint id, pid, and generation in `kernel/src/service_identity.mc`, carry one generation counter per restartable service in `boot.KernelBootState`, bump that counter in `init.restart_log()`, `init.restart_kv()`, and `init.restart_echo()`, and extend the live restart path plus the service-identity fixture to prove the split contract directly; make no compiler, sema, MIR, backend, ABI, target, runtime, or hal expansion | tradeoff: the repository now exposes a real distinction between stable routing continuity and replaced service-instance identity without inventing a framework, but callers that care about restart boundaries must inspect the additive generation marker explicitly rather than getting hidden invalidation or automatic rediscovery semantics; retained-state continuity also remains intentionally uneven across services because only explicit reload owners preserve state | revisit trigger: reopen only when a later phase needs stale-handle invalidation, richer caller-visible restart markers on the shell protocol, dynamic rebinding, or another identity surface that cannot be served by stable refs plus the current additive generation marker | reference: docs/plan/active/phase182_restart_identity_continuity_probe.txt, kernel/src/service_identity.mc, kernel/src/boot.mc, kernel/src/init.mc, kernel/src/scenarios.mc, tests/system/kernel_reset_lane_service_identity/src/main.mc
- 2026-04-15 | area: kernel | context: Phase 181 key-value retained-state reload follow-through after Phase 179 proved one explicit restart-with-state path on `log_service` and Phase 180 chose retained-state follow-through over second-target portability as the narrower next owner | limiter: the repository needed to test whether the same explicit restart-with-state shape could carry one user-facing retained-state owner, but widening that pressure into a filesystem, persistence namespace, snapshot framework, supervisor, or compiler-facing change would have mixed storage, identity, and lifecycle policy too early; the narrowest real candidate was `kv_service`, whose retained keys, values, overwrite behavior, missing-key result, and fixed-capacity exhaustion are already user-visible through the existing shell path | decision: choose `kv_service` as the second and only new retained-state reload owner; add explicit bounded `KvSnapshot`, `kv_snapshot(...)`, and `kv_reload(...)` in `kernel/src/kv_service.mc`; keep restart policy in `kernel/src/init.mc` and change `restart_kv()` from empty re-init to explicit snapshot reload; extend the live restart path in `kernel/src/scenarios.mc`; add one dedicated Phase 181 workflow fixture; make no filesystem, persistence framework, compiler, sema, MIR, backend, ABI, target, runtime, or hal expansion | tradeoff: the repository now has two honest restart-with-state paths, covering one observational owner and one user-facing owner, but retained reload remains explicit, bounded, and per-service rather than generic; endpoint identity stays stable while user-visible state survives only for services that init explicitly reloads; this still does not claim durability, coordinated snapshot bundles, or a general persistence subsystem | revisit trigger: reopen when a third admitted retained-state owner needs reload, when snapshot lifetime must outlive the immediate restart replacement call, when cross-service restart coordination becomes the narrower blocked path, or when service-identity continuity pressure proves the current explicit contract ambiguous enough to need a new owner-local marker | reference: docs/plan/active/phase181_key_value_retained_state_reload_follow_through.txt, kernel/src/kv_service.mc, kernel/src/init.mc, kernel/src/scenarios.mc, tests/system/kernel_reset_lane_phase181_kv_retained_reload/src/main.mc
- 2026-04-15 | area: docs | context: Phase 180 next frontier plateau decision after Phase 174 named second-target portability seams and Phases 176 through 179 applied the first real external-pressure wave to the running service system | limiter: the repository had two plausible next fronts, but only one could be the honest next owner; Phase 174 made portability distance measurable without producing a blocked admitted consumer, while Phases 176, 177, 178, and 179 each produced one concrete service-system pressure point that was resolved inside `kernel/src/` with flat explicit changes rather than framework or compiler expansion | decision: take Branch A; keep the next wave on service growth, service identity, retained state, or user-facing interaction inside the current running-system slice; defer bounded second-target work and any associated compiler, target, runtime, or `hal` expansion; reopen portability only when a real admitted consumer needs Linux arm64 or another second target for behavior Darwin arm64 cannot supply honestly, and when that target pressure is stronger than any remaining owner-local service-system question | tradeoff: the repository preserves focus and keeps the next frontier evidence-driven, but it also accepts that Darwin arm64 hosting and the current bootstrap target family remain a deliberate near-term boundary even though the portability seams are already named; portability work is ordered later rather than rejected permanently | revisit trigger: reopen when both conditions hold: (1) a real admitted consumer is genuinely blocked on second-target behavior, and (2) service-system growth has stopped being the narrower next owner; until then, continue to prefer service-system follow-through over portability classification turning into implementation drift | reference: docs/plan/active/phase180_next_frontier_plateau_decision.txt, docs/plan/active/phase174_second_target_platform_boundary_identification.txt
- 2026-04-15 | area: kernel | context: Phase 179 bounded retained-state reload probe after the landed topology and restart re-audit confirmed that init-owned explicit restart was still the right owner but all admitted restart paths still reset service state to an empty baseline | limiter: the repository needed one real restart-with-state path to test whether service identity and restart truth could carry retained data honestly, but widening that pressure into a filesystem, persistence namespace, snapshot framework, supervisor, or compiler-facing change would have mixed too many owners at once; only one current service had simple retained data whose reload semantics were both meaningful and easy to reason about | decision: choose `log_service` as the sole retained-state reload candidate; add an explicit bounded `LogSnapshot` representation plus `log_snapshot(...)` and `log_reload(...)` in `kernel/src/log_service.mc`; keep restart policy in `kernel/src/init.mc` and change `restart_log()` to save and reload that snapshot instead of empty re-init; update the live restart probe and add one dedicated workflow fixture; make no filesystem, persistence framework, compiler, sema, MIR, backend, ABI, target, runtime, or hal expansion | tradeoff: the system now has one honest restart-with-state path, but retained reload remains narrowly per-service and explicit rather than generic; log saturation survives restart too, so reload preserves both useful data and the prior exhausted state; `kv_service` and the rest of the system still deliberately reset unless a later phase admits another explicit reload owner | revisit trigger: reopen when a second admitted retained-state owner needs reload, when snapshots need a lifetime beyond immediate restart replacement, when restart policy needs cross-service coordination rather than one owner-local save-and-reload, or when a real consumer proves the next owner is storage or packaging rather than another explicit kernel service path | reference: docs/plan/active/phase179_bounded_retained_state_reload_probe.txt, kernel/src/log_service.mc, kernel/src/init.mc, tests/system/kernel_reset_lane_phase179_retained_reload/src/main.mc
- 2026-04-15 | area: kernel | context: Phase 178 topology and restart growth re-audit after the landed fifth-service growth and hostile-user shell pressure | limiter: the five-service system still did not justify a routing table, supervisor, retry framework, or any compiler-facing change, but the original Phase 170 and Phase 172 shapes had started to show linear-growth friction: service_topology.mc had five unrelated slot constants with no flat enumeration or endpoint lookup owner, and init.mc had only per-service restart entrypoints with no common owner-local way to act on an observed endpoint id | decision: take Branch B with one bounded structural follow-through only; keep the five named slot constants and explicit per-service restart functions, but add flat topology helpers (`service_count()`, `service_slot_at(index)`, `service_slot_for_endpoint(endpoint)`, `service_slot_is_valid(slot)`) and flat init-owned restart helpers (`can_restart(endpoint)`, `restart(state, endpoint)`); use the endpoint-driven restart entry in scenarios.mc and add one reset-lane system fixture plus workflow registration; make no routing-table, supervisor, retry-policy, persistence, compiler, sema, MIR, backend, ABI, target, runtime, or hal expansion | tradeoff: callers can now enumerate the static topology and drive restart from stable endpoint ids without inventing a framework, but the system still deliberately stops short of richer graph data, dynamic endpoint assignment, serial or shell restart, retry counters, or autonomous restart policy; the topology helpers use explicit flat conditionals rather than a constant aggregate table because the phase only needed an ownership-visible bounded follow-through | revisit trigger: reopen when service count exceeds 8, when the topology needs richer graph structure than slot enumeration or endpoint lookup, when restart policy needs per-service retry state rather than endpoint-driven replacement, or when a real phase needs a constant aggregate-table language feature strongly enough to replace the current explicit helpers | reference: docs/plan/active/phase178_topology_and_restart_growth_reaudit.txt, kernel/src/service_topology.mc, kernel/src/init.mc, tests/system/kernel_reset_lane_phase178_topology_restart/src/main.mc
- 2026-04-15 | area: kernel | context: Phase 177 shell as hostile user boundary | limiter: the compact shell already rejected malformed traffic, but every shell-owned invalid consequence collapsed to the same one-byte `?` payload, so repeated hostile use could be rejected without telling the caller whether the failure was an unknown command or a malformed admitted command shape; widening into wider payloads, text-shell parsing, new event families, richer error objects, or compiler-facing work would have solved a broader problem than the one actually exposed | decision: keep the four-byte shell protocol, existing `SyscallStatus` values, and current event codes; extend shell-owned invalid replies to two bytes with `?C` for unknown command family or verb and `?S` for malformed command shape or sentinel usage; keep valid service-owned failures unchanged (`kv_service` still owns missing-key `InvalidArgument`, `echo_service` still owns `Exhausted`); add one hostile-user system fixture plus workflow registration; make no compiler, sema, MIR, backend, ABI, target, runtime, or hal expansion | tradeoff: hostile users now get one extra byte of truth without reopening payload width, but the shell still deliberately stops short of human-readable messages, a broader syntax taxonomy, or line-oriented parsing; event codes remain coarse `ok` versus `invalid` rather than per-kind | revisit trigger: reopen only when a real shell-facing consumer cannot recover or present enough user-visible truth from status plus `?C` or `?S`, and the narrowest honest fix is still a bounded protocol extension rather than a wider shell or error-object redesign | reference: docs/plan/active/phase177_shell_as_hostile_user_boundary.txt, kernel/src/shell_service.mc, tests/system/kernel_reset_lane_phase177_hostile_shell/src/main.mc
- 2026-04-14 | area: kernel | context: Phase 176 adversarial service growth under explicit restart | limiter: the repository needed one real fifth-service pressure step beyond the four-service serial-shell-plus-log-plus-kv system, but widening into a routing table, service bus, supervisor, persistence layer, or compiler-facing expansion would have hidden the actual ownership question; the shell already exposed one compact echo command shape (`ECxy`), making echo the narrowest real service candidate instead of inventing a new protocol family | decision: admit one new boot-wired `echo_service` with bounded request-derived replies and explicit exhaustion after four uses; route `ECxy` through `shell_service` to `service_topology.ECHO_ENDPOINT_ID`; add `ECHO_SLOT` and `SERVICE_COUNT = 5`; extend `init.mc` with `restart_echo()` in the same flat caller-driven style as `restart_log()` and `restart_kv()`; keep kernel dispatch explicit and leaf-shaped; make no compiler, sema, MIR, backend, ABI, target, runtime, or hal expansion | tradeoff: the running system now carries one more real service and one more explicit restart owner, but it still deliberately stops short of topology iteration, supervisor policy, payload-width widening, or any generic service framework; echo pressure is simple (four successful round trips, then Exhausted) rather than a richer queue or mailbox policy | revisit trigger: reopen structural or compiler growth only when a later admitted service cannot fit the current Message/Effect plus flat ServiceSlot and init-owned restart model honestly, or when restart-frequency or routing pressure defeats the current flat forms | reference: docs/plan/active/phase176_adversarial_service_growth_under_explicit_restart.txt, kernel/src/echo_service.mc
- 2026-04-14 | area: stdlib | context: Phase 175 error model widening first gate; re-examined the errors.Error kind/code deferral from Phase 47 to determine whether any admitted real consumer is blocked by the current scalar encoding | limiter: surveyed all admitted real programs (file_walker, evented_echo, evented_partial_write, grep_lite, line_filter_relay, pipe_handoff, pipe_ready); every program uses is_err() or is_ok() for binary flow control and returns fixed OS exit codes on failure; no production code inspects kind() or code() at runtime; no program renders a human-readable error message to the user; the only kind/code inspection site is testing.expect_err() in evented_echo unit tests — a test assertion, not a production branching decision; widening errors.Error to carry message strings or cause chains before a real program needs them would force a heap-ownership policy decision across the runtime and stdlib ahead of evidence | decision: Branch B — explicit deferral; the kind/code scalar ABI from Phase 47 is confirmed sufficient for the current admitted slice; no code changes were admitted; the revisit trigger is sharpened from "another admitted real program that still feels too opaque after kind/code" to the two-condition gate: (1) a production call site genuinely cannot determine what to tell the user or which recovery path to take even with kind and code values, AND (2) the narrowest resolution is a bounded optional message slice or explicit cause field rather than a heap-backed object or hidden propagation | tradeoff: the repository keeps a simple scalar error ABI with no runtime message rendering; programs that need to distinguish failure causes already have the kind/code vocabulary; the admitted slice does not yet include any program that reaches the user-facing diagnostic layer; this is accepted because no current admitted program writes an error description to output | revisit trigger: reopen only when both hold: (1) an admitted program call site is opaque after kind and code, AND (2) the narrowest fix is a bounded message slice or explicit cause field; binary is_err() flow control and test-only kind/code assertions are not triggers | reference: docs/plan/archive/phase175_error_model_widening_first_gate.txt, stdlib/errors.mc
- 2026-04-14 | area: sema + mir | context: Phase 169 const-eval first expansion; consumer matrix audit identified that module-scoped const names used as array dimensions in local variable type annotations were not being evaluated during MIR lowering, causing type mismatches between [CAP]u8 (unevaluated) and [4]u8 (literal) | limiter: SemanticTypeFromAst (inside the Checker class) correctly evaluates array lengths using the full const-eval context; that path is only reached for struct fields, function params, and return types synthesized during sema — local variable type annotations in function bodies went through the bare TypeFromAst path in mir_lower.cpp which has no access to pre-evaluated const values from the Checker; widening the fix into a general const-eval re-run inside the MIR lowerer would have coupled MIR to sema internals unnecessarily | decision: add ResolveTypeFromAst(const ast::TypeExpr*, const Module&) to the public sema API; it calls TypeFromAst then replaces unevaluated array metadata by looking up the name in Module::globals const_values; use it in LowerBindingLike in mir_lower.cpp; handle only name references and integer literals (qualified cross-module names in array dimensions have no current kernel pressure and are deferred); update four kernel service modules to replace literal [4] with the named CAPACITY constant in all array type annotations that the constant was defined to describe | tradeoff: the fix closes the local-const-name-in-array-dimension gap without touching the Checker internals; more complex array length expressions (arithmetic like CAP-1, or cross-module qualified names) still fall back to TypeFromAst and keep the text representation, which would cause a mismatch if a developer uses them in local var annotations — this is accepted because no current consumer needs them | revisit trigger: reopen if arithmetic const expressions (not just names) or cross-module qualified names begin appearing as array dimension annotations in local variable declarations; the revisit should extend EvalArrayLengthFromModule to handle those forms | reference: docs/plan/active/phase169_const_eval_first_expansion.txt, compiler/sema/check.cpp, compiler/mir/mir_lower.cpp, kernel/src/kv_service.mc, kernel/src/log_service.mc, kernel/src/serial_service.mc, kernel/src/serial_shell_event_log.mc
- 2026-04-14 | area: docs | context: Phase 168 running-system and compiler support statement refresh after the Phase 161–166 model-boundary and repair wave | limiter: the Veya running-system support statement and the Modern C position note were both last updated near Phase 120 and Phase 147 respectively, leaving a meaningful gap between published documentation and the admitted landing; expanding the refresh into new language features, broader freestanding workflow claims, or general platform statements would have overstated the current admitted slice | decision: rewrite veya_running_system_support_statement.txt to cover Phase 109–166 accurately; add Phase 148–166 entries to modern_c_veya_readiness_position.txt; add a "Supported Veya running-system slice" section to README.md; update the make public-cut-smoke description to reflect it now covers both the hosted and Veya running-system build step; add the Veya kernel build step to run_public_cut_smoke.cmake; keep the compiler, sema, MIR, backend, ABI, target, runtime, and hal surfaces closed for this phase | tradeoff: the public documentation now accurately represents the post-Phase-166 admitted slice; no new language or compiler features were admitted in this documentation phase; the aggregate public-cut smoke now exercises the kernel build path as the simplest honest coverage of the running-system claim | revisit trigger: reopen when a later phase lands new admitted behavior on either the hosted or the Veya running-system slice that the support statements no longer reflect accurately | reference: docs/plan/active/phase168_running_system_and_compiler_support_statement_refresh.txt, docs/plan/admin/veya_running_system_support_statement.txt, docs/plan/admin/modern_c_veya_readiness_position.txt, README.md, tests/cmake/run_public_cut_smoke.cmake
- 2026-04-14 | area: kernel | context: Phase 166 bounded repair of the second named boundary (service topology without a structural home) | limiter: the topology (which services shell sends to and which endpoint_id maps to which handler) was distributed across three coordinated sites — ShellServiceState constructor fields, boot.kernel_init() shell_init call, and kernel_dispatch endpoint_id if-chains — with no neutral owning module; adding a new shell-reachable service required three coordinated changes; widening the repair into a routing table, service bus, or dynamic registry would have added a framework layer with no current pressure | decision: create service_topology.mc with the four endpoint ID constants and no service imports; boot.mc drops its own endpoint ID constants and imports service_topology; shell_service.mc drops log_endpoint_id and kv_endpoint_id from state and init (now topology-neutral, two fields only: pid, slot); kernel_dispatch.mc and scenarios.mc import service_topology directly; nine system test fixtures drop their local SERIAL_ENDPOINT_ID constant duplicate and import service_topology | tradeoff: the topology is now visible as one named module with four constants rather than three files that must agree by convention; adding a new shell-reachable service now requires one constant in service_topology.mc and one kernel_dispatch arm — not three coordinated changes; the cost is one new module dependency, but service_topology.mc has no imports so it adds no circular risk | revisit trigger: reopen when either the endpoint ID constants need to be dynamic (i.e. assigned at runtime rather than compile-time) or when the topology grows beyond constants into actual graph data | reference: docs/plan/active/phase166_bounded_model_expansion_from_second_boundary.txt, kernel/src/service_topology.mc, kernel/src/shell_service.mc, kernel/src/boot.mc, kernel/src/kernel_dispatch.mc
- 2026-04-14 | area: docs + kernel | context: Phase 165 second model boundary identification; measuring evidence from Phases 161–164 to name the second repeated model failure | limiter: three candidates existed — service topology being implicit (appeared in Phases 162 and 163), 4-byte payload pressure (deferred since Phase 141, no new friction in 161–164), and static service directory (further-horizon item with no strain yet) | decision: named boundary is "service-to-service reachability topology has no structural home" — ShellServiceState carries log_endpoint_id and kv_endpoint_id as constructor-injected fields that are topology facts (not mutable service state), boot.mc owns the ID constants (but shell cannot import boot due to circular dependency), kernel_dispatch.mc carries the full routing in endpoint_id if-chains; these three sites share no named data structure and must agree by convention; this condition appeared concretely in Phase 162 (unnamed dispatch arms) and Phase 163 (constant duplication across shell_service, scenarios, kernel_dispatch); 4-byte payload was not genuine recurring friction in 161–164 so it was not named as the second boundary | tradeoff: the identified boundary is narrow and fixable without compiler changes; Phase 166 repair is one thin service_topology.mc module (four endpoint ID constants, no imports) that both boot.mc and shell_service.mc import, letting shell drop endpoint ID fields from state | revisit trigger: reopen if a third candidate (4-byte payload or service directory) begins causing repeated friction in later phases, or if service topology grows beyond four endpoints in a way that a constants-only module is no longer sufficient | reference: docs/plan/active/phase165_second_model_boundary_identification.txt
- 2026-04-14 | area: language + kernel | context: Phase 164 struct embedding pressure audit; deciding whether to admit one narrow anonymous-field embedding form into Modern C | limiter: the identified pressure sites (KernelBootState updates in kernel_dispatch.mc; SerialShellPathState updates in serial_shell_path.mc) were all the "update one field, carry the rest" pattern; the same pattern is already solved by with-helpers (kvwith, logwith) in kv_service.mc and log_service.mc — the missing piece was that boot.mc and serial_shell_path.mc lacked their own with-helpers | decision: Branch B — explicit deferral; add bootwith_log() and bootwith_kv() to boot.mc, add pathwith() to serial_shell_path.mc (mirroring the kvwith/logwith pattern), use those helpers in kernel_dispatch.mc; no compiler changes; struct embedding is explicitly deferred as a language feature | tradeoff: all identified pressure resolves through helper extraction; four delegating accessor functions in serial_shell_path.mc (path_serial_reply_*) are retained as good encapsulation, not eliminated via embedding | revisit trigger: reopen when a wrapper record exists where helper extraction alone would require more boilerplate than it removes, and the field-promotion semantics can be proved unambiguous in the language; confirmed that Plan 9 clarity means simplicity of structure, not mandatory adoption of Plan 9 language conveniences | reference: docs/plan/active/phase164_struct_embedding_pressure_audit_and_conditional_admission.txt, kernel/src/boot.mc, kernel/src/serial_shell_path.mc, kernel/src/kernel_dispatch.mc
- 2026-04-14 | area: kernel | context: backlog follow-through after the Phase 157 scenario-runner extraction left `kernel/src/scenarios.mc` as a chain of named integration functions | limiter: the hosted reset-lane image still has no admitted blocking receive syscall, mailbox owner, or interrupt-driven ingress source on this path, so widening into a live runtime event source or compiler-facing async surface would have solved a broader problem than the actual backlog item | decision: replace the named `run_*` chain in `kernel/src/scenarios.mc` with one scripted observation loop (`step_obs` + `deliver` + `run`); reduce `kernel/src/main.mc` to init plus one call to `scenarios.run(&state)`; keep `serial_protocol.mc` as the transport encoder and keep compiler, sema, MIR, backend, ABI, target, runtime, and hal surfaces closed | tradeoff: the kernel image entry is now loop-shaped and no longer grows one call per phase, but the event source is still a deterministic scripted observation stream rather than a live receive primitive | revisit trigger: reopen when a later phase needs blocking receive, interrupt-driven ingress, or concurrent mailbox delivery that cannot be modeled by the current scripted observation loop | reference: kernel/src/scenarios.mc, kernel/src/main.mc, docs/plan/backlog.txt
- 2026-04-14 | area: runtime | context: Phase 162 orchestration cleanup after Phase 161 closed the send_dropped gap | limiter: boot.mc carried two structural impurities: the kv advisory dispatch was inline in kernel_dispatch_message alongside routing logic (state-plus-control mixing), and serial post-dispatch was a separate two-phase step in kernel_dispatch_step gated behind an endpoint_id check; kernel_dispatch_message and serial_path_post_dispatch had no clear single responsibility | decision: split kernel_dispatch_message into kernel_dispatch_kv (named advisory concept) and kernel_dispatch_leaf (leaf routing); change execute_effect to call kernel_dispatch_leaf since all Send effects in the chain target log or kv only; add kernel_dispatch_serial which owns the complete serial path including execute_effect, path_commit_reply, event tagging, and send_dropped propagation; simplify kernel_dispatch_step to a thin router; add path_commit_reply to serial_shell_path.mc; no behavior change, no new modules, no Effect variants | tradeoff: the topology invariant (Send effects only reach leaf services) is now expressed in the call graph rather than as a comment; the circular forward-reference between execute_effect and kernel_dispatch_message is eliminated; the cost is removing a single unified dispatch function in favor of two named leaf/serial paths, which requires readers to understand that serial is inherently a two-level path | revisit trigger: reopen when a third dispatch path arrives and the serial-vs-leaf routing in kernel_dispatch_step should become a general routing table | reference: docs/plan/active/phase162_orchestration_cleanup.txt
- 2026-04-14 | area: runtime | context: Phase 161 per-send delivery witness after Phase 160 named advisory audit silencing as the first repeated model boundary | limiter: the kv caller had no way to distinguish a kv write whose advisory log record landed from one whose advisory record was silently dropped when the log was full; repairing this via recovery, two-phase commit, a retry framework, or a new module would have solved a bigger problem than the one proved; the narrowest truthful change was one new field on the Effect struct so the caller can inspect the outcome without the dispatcher hiding it | decision: add send_dropped: u32 to Effect as the last field; add effect_mark_send_dropped() and effect_send_dropped() to service_effect.mc; in boot.mc kv dispatch, set send_dropped=1 when the advisory log append returns Exhausted; in kernel_dispatch_step, propagate send_dropped from the inner effect to the outer serial reply when routing through the serial path; keep compiler, sema, MIR, backend, ABI, target, runtime, and hal surfaces closed | tradeoff: the advisory send outcome is now observable by any caller that checks effect_send_dropped(); existing callers that do not check it observe no behavior change; the witness is purely additive and does not imply recovery, retry, or audit-trail guarantees | revisit trigger: reopen when a later phase needs recovery guarantees rather than just observability, or when the Effect model must carry multiple per-send outcomes from a single dispatch step | reference: docs/plan/active/phase161_per_send_delivery_witness.txt
- 2026-04-14 | area: runtime | context: Phase 159 observability as a system property over the reset-lane kernel after the Phase 158 cross-service failure semantics consistency audit | limiter: the admitted kv service was opaque unless the caller already knew the stored keys — there was no way to determine kv occupancy via the public protocol; widening the gap into a telemetry platform, event-driven instrumentation, or enumeration of keys and values would have grown the observation surface beyond the minimum truthful slice; changing the kv payload_len==0 semantics from InvalidArgument to a count reply was the smallest addition that closed the real user gap without a new module, new struct, or new protocol layer | decision: change kv_service.handle() for payload_len==0 from returning InvalidArgument to returning Ok with s.count as reply_payload_len (consistent with log_tail returning s.len as reply_payload_len); add KC!! route in shell_service.mc; add encode_kv_count() in serial_protocol.mc; add run_observability_inspection scenario; keep compiler, sema, MIR, backend, ABI, target, runtime, and hal surfaces closed | tradeoff: the kv count query closes the fundamental occupancy gap and is non-mutating, symmetric with log tail which already uses payload_len as the count carrier; the tradeoff is that payload_len==0 to the kv endpoint no longer returns InvalidArgument — any caller relying on that as a sentinel must switch to a 1-byte key query; no existing test used payload_len==0 with the kv endpoint, so no regression | revisit trigger: reopen when a later phase needs key enumeration (not just count), or when the observation surface must extend to include which keys are stored without prior knowledge | reference: docs/plan/active/phase159_observability_as_a_system_property.txt
- 2026-04-14 | area: runtime | context: Phase 158 cross-service failure semantics consistency over the reset-lane kernel after the Phase 157 long-lived coherence audit | limiter: comparing failure contracts across the admitted service set revealed one intentional cross-service inconsistency — advisory log drop on kv write when log is full — plus two consistent failure signals across service boundaries; widening the advisory drop into explicit caller notification, a two-phase commit, a log-full callback, or any framework that enforces audit-trail integrity would have pushed policy into the dispatcher or into a new shared owner ahead of evidence; keeping the failure model normalized through a recovery framework or retry policy would have hidden the real user-space cost | decision: classify the advisory log drop as explicitly intentional: kv writes return Ok regardless of whether the log advisory marker succeeded; the Exhausted reply from log is discarded by the dispatcher and the kv caller has no signal that the audit record was lost; name and document this as the one cross-service failure mismatch; confirm that Exhausted and InvalidArgument are both consistent across services; keep compiler, sema, MIR, backend, ABI, target, runtime, and hal surfaces closed | tradeoff: the repository now has one explicit named cross-service failure inconsistency tied to real dispatch behavior, but advisory log observability under pressure is best-effort only; a user who writes kv successfully cannot determine whether the log trail was kept or dropped without reading the log independently; that cost is accepted because the advisory log is documentation-grade observability, not transactional write-ahead journaling | revisit trigger: reopen when a later phase needs audit-trail integrity guarantees across service boundaries, or when a concrete user-space consumer proves that silent advisory drop is structurally insufficient rather than merely verbose to work around | reference: docs/plan/active/phase158_cross_service_failure_semantics_consistency.txt
- 2026-04-14 | area: runtime | context: Phase 154 service identity and addressing semantics in the reset-lane kernel, plus audit separation from main | limiter: the admitted identity model is fixed endpoint_id wiring with no discovery or dynamic binding; widening into a compiler-known ServiceRef type or embedded lifecycle hooks would have moved policy into the compiler ahead of real evidence; keeping identity invariant checks inside kernel/src/main.mc mixed integration behavior with structural validation in the same entry point | decision: ServiceRef is a plain struct wrapping endpoint_id u32 with no compiler changes; boot-wired refs are published as named accessor functions in boot.mc; identity invariant checks (valid, distinct, stable across restart) are placed exclusively in the dedicated system test fixture, not in kernel/src/main.mc; compiler, sema, MIR, backend, ABI, target, runtime, and hal surfaces remain closed | tradeoff: identity semantics are now explicit and contractually stable in library code but there is no compiler enforcement of ref validity; the main-versus-test separation also means the kernel binary does not self-validate identity on startup, but that is appropriate for the admitted model since these are compile-time constants | revisit trigger: reopen when a later multi-client, ownership-handoff, or temporal phase proves that plain u32 endpoint_id is insufficient and a compiler-known ServiceRef type is warranted, or when a concrete owner gap makes the main-test separation impractical | reference: docs/plan/active/phase154_service_identity_and_addressing_semantics.txt
- 2026-04-13 | area: runtime | context: reset-lane serial observability cleanup after shell or kv or boot effect-shape refactors | limiter: tracing still used `serial_shell_event_log.trace_step(...)` to replay service behavior with synthetic log and kv state and infer outcomes from debug-adjacent signals, creating a parallel truth path that could drift from real execution | decision: move event emission into real shared effects and make `serial_shell_event_log` a deterministic ring sink that appends events from those effects, removing replay-based trace execution | tradeoff: observability now depends on event instrumentation at dispatch owners, but runtime truth no longer depends on synthetic replay or debug-derived inference | revisit trigger: reopen only if a future owner needs richer event payload semantics that cannot fit the current bounded effect-event slots without reintroducing replay logic | reference: /Users/ro/dev/c_modern/docs/plan/active/phase153_service_composition_shape_audit.txt
- 2026-04-13 | area: runtime | context: post-Phase-153 follow-through after shell cleanup exposed the same pattern in `kv_service` and during kernel-wide recurrence scan | limiter: `kv_service` had reintroduced service-local `Message`, service-local `Effect`, and service-local `HandleResult` control envelopes, including a composite `ReplyAndEmitLog` variant that hid sequencing and duplicated shared orchestration concepts | decision: remove kv-local execution types, switch kv and shell to the shared `service_effect` contract, make kv state mutation pointer-local and effect return explicit, and keep cross-service sequencing in orchestration code rather than in service-local composite variants | tradeoff: orchestration files still carry transitional syscall-shaped adapters and top-level dispatch envelopes, but service owners no longer define competing runtime-control models | revisit trigger: reopen only if a future owner cannot express a real service slice with one shared effect step without reintroducing service-local dispatch or composite-effect envelopes | reference: /Users/ro/dev/c_modern/docs/plan/active/phase153_service_composition_shape_audit.txt
- 2026-04-13 | area: runtime | context: post-Phase-153 runtime-shape correction after landing the reset-lane shell-to-log-plus-key-value composition seam | limiter: the shell path still encoded control flow through `ShellDispatch` route integers, synthetic reply flags, and path-local branching that duplicated runtime intent and obscured message-driven ownership | decision: introduce one shared message-plus-effect execution contract for the reset lane, rewrite `shell_service` as a protocol translator that emits `Reply` or `Send` effects, and consume those effects in `serial_shell_path` while keeping framework, broker, dynamic registry, and compiler expansion closed | tradeoff: reset-lane composition now reads more directly as message-to-effect transitions, but transitional syscall-shaped adapters still exist in some service boundaries and must be retired incrementally | revisit trigger: reopen only if a later kernel slice proves the shared effect contract cannot express a real owner-local service without reintroducing duplicated control structs or hidden routing policy | reference: /Users/ro/dev/c_modern/docs/plan/active/phase153_service_composition_shape_audit.txt
- 2026-04-13 | area: runtime | context: Phase 153 service-composition audit in the reset-lane kernel after the Phase 152 controlled-variation result | limiter: the reset-lane shell path had drifted into shell-local echo behavior, so the planned composition audit would have been judging a placeholder instead of a real shell-to-service path, but widening the repair into a broker, generic routing helper, or compiler work would have hidden the actual ownership question | decision: restore the real compact shell route in `kernel/` by keeping command classification in `shell_service`, keeping the explicit service handoff in `serial_shell_path`, admitting only one bounded kv-write observation into `log_service`, and leaving compiler, sema, MIR, backend, ABI, target, runtime, and `hal` surfaces closed | tradeoff: the reset lane now carries a real shell-to-log and shell-to-key-value composition seam plus one visible two-service follow-through, but it still deliberately stops short of a generic request framework, discovery, broader service middleware, or compiler-facing changes | revisit trigger: reopen only when a later real composition slice shows repeated adapter families or identity pressure that cannot stay explicit through the landed owner-local glue, or when Phase 154 identity work proves a narrower primitive is actually required | reference: /Users/ro/dev/c_modern/docs/plan/active/phase153_service_composition_shape_audit.txt
- 2026-04-13 | area: runtime | context: Phase 152c closeout needed one observability replacement that preserved debug value without reintroducing verifier-owned service shape, and the first reset-lane slice now had enough evidence to name the next rewrite target honestly | limiter: the legacy kernel tree still stores proof-owned observations and phase routing, but pushing logging back into the reset-lane service files would have recreated the same architecture under different names | decision: keep service files in `kernel/` limited to state, init, handlers, and thin debug hooks, move reset-lane observability into the flat four-entry `kernel/src/serial_shell_event_log.mc` ring buffer, and treat the legacy serial bootstrap glue plus `main.mc` proof-routing burden as the next rewrite target because that is where the remaining complexity now lives | tradeoff: the repository keeps a narrow slice-local debug module and still carries the old proof-shaped orchestration in the legacy tree for now, but observability no longer distorts the reset-lane services and the next cut is chosen from landed evidence rather than speculation | revisit trigger: reopen when a second reset-lane path needs broader shared observability than the current flat ring buffer can justify, or when the next kernel phase begins cutting the legacy serial and `main.mc` proof-routing band directly | reference: /Users/ro/dev/c_modern/docs/plan/active/phase152c_kernel_behavioral_reset_and_verifier_extraction.txt
- 2026-04-13 | area: testing | context: Phase 152c kernel cleanup now extends to the test tree because the active runtime-proof suites and projection-heavy audits are reinforcing the same verifier-first architecture that the kernel reset is trying to remove | limiter: the existing `tests/tool/freestanding/kernel/runtime/phase*` surfaces, MIR projections, and audit-oriented runners encode legacy architecture and are too cross-coupled to simplify safely in one destructive pass, but continuing to treat them as the default acceptance path would keep new cleanup work pinned to old proof shape | decision: establish `tests/smoke/` and `tests/system/` as the new behavior-first destinations for kernel cleanup checks, keep parser, sema, MIR, and codegen under their current compiler-validation owners, and freeze the old runtime-proof suites as read-only legacy surfaces during the reset instead of expanding them | tradeoff: the repository temporarily carries both legacy proof-driven tests and new behavior-first entrypoints, but the new test architecture can now grow without being forced to preserve phase-driven audit structure | revisit trigger: reopen when enough smoke and system coverage exists to archive or move the legacy runtime-proof suites, or when a concrete compiler-validation gap requires keeping a narrow projection-style check | reference: /Users/ro/dev/c_modern/docs/plan/active/phase152c_kernel_behavioral_reset_and_verifier_extraction.txt
- 2026-04-13 | area: runtime | context: Phase 152c behavioral reset after the serial-service and late bootstrap slice exposed that verifier-owned observations, immutable-copy envelopes, and phase-indexed audits were shaping the runtime code more strongly than the kernel behavior itself | limiter: the legacy kernel tree is now too cross-coupled to verifier-owned structures for a safe in-place simplification pass, and forcing compatibility during every edit would keep the reset architecture subordinate to the very proof machinery it is meant to replace | decision: keep `kernel_old/` as the frozen legacy reference tree, start the clean reset lane under `kernel/`, define the reset-lane service rule as state plus init plus handlers plus optional debug only, and keep compiler, sema, MIR, backend, ABI, target, runtime, and `hal` surfaces closed while the first simple service shape is established | tradeoff: the first reset work is now integrated under `kernel/` as the canonical lane, so the repository carries both a clean reset lane and a frozen legacy tree in `kernel_old/`, but that split avoids more verifier-shaped compatibility churn in the new design | revisit trigger: reopen when the first `kernel/` service slice is stable enough to replace a legacy path directly or when a concrete blocker proves the clean lane needs a narrow language or build integration change | reference: /Users/ro/dev/c_modern/docs/plan/active/phase152c_kernel_behavioral_reset_and_verifier_extraction.txt
- 2026-04-13 | area: docs | context: Phase 152 pattern stability under one controlled variation after the landed Phase 150 rebuilt-system helper and Phase 151 owner-local main simplification | limiter: the repository needed one honest discriminator for whether the extracted shell-plus-log-plus-key-value pattern survives a real difference, but widening that check into more services, broader subsystem growth, framework glue, or compiler work would have hidden the answer under fresh surface area | decision: keep compiler, sema, MIR, backend, ABI, target, runtime, and `hal` surfaces closed, choose exactly one retained-state variation by restarting `log_service` instead of `kv_service`, keep that variation inside the existing `bootstrap_services` rebuilt-system owner, and classify the result as partial generalization rather than forcing a new framework or compiler-facing abstraction | tradeoff: the repository now has one honest controlled-variation result over the real interactive-service slice, but it still does not claim a fully general service pattern because restart evidence cannot remain wholly inside the restarted retained-log owner after that owner resets | revisit trigger: reopen only when the next composition or identity phase proves that the Phase 152 awkwardness cannot stay explicit through owner-local glue, or when a later real consumer justifies a stronger evidence contract without widening into hidden framework behavior | reference: /Users/ro/dev/c_modern/docs/plan/active/phase152_pattern_stability_under_one_controlled_variation.txt
- 2026-04-13 | area: docs | context: Phase 151 main simplification after the landed Phase 150 rebuilt-system helper and the earlier runtime snapshot grouping work | limiter: the repository still needed to shrink `kernel/src/main.mc`, but the active evidence only justified moving the concrete late Phase 140 through Phase 150 service-system scenario band into an existing owner; widening that cleanup into a generic orchestration framework, broader service runtime, or compiler work would have hidden the ownership lesson under new abstraction vocabulary | decision: keep compiler, sema, MIR, backend, ABI, target, runtime, and `hal` surfaces closed, reuse the split `bootstrap_services` owner surface, move the late service-system proofs behind one explicit `LateServiceSystemProofInputs` record plus one owner-local `execute_late_service_system_proofs(...)` helper, and treat the remaining early-boot and mid-phase simplification as follow-on Phase 151 work rather than faking full completion | tradeoff: `main.mc` is materially thinner on the late service-system band and the focused freestanding runtime proof still passes, but the root file still owns the early-boot and mid-phase proof bands and the repository still does not claim a reusable orchestration framework, hidden policy layer, or compiler-facing service support | revisit trigger: reopen only to finish moving the remaining early-boot and Phase 104 through Phase 137 bands into their nearest owners, or if a later real simplification slice proves that the current owner-local helper boundary is still too awkward without introducing hidden framework behavior | reference: /Users/ro/dev/c_modern/docs/plan/active/phase151_complete_main_simplification.txt
- 2026-04-13 | area: docs | context: Phase 150 one system rebuilt cleanly after the landed Phase 149 restart-contract audit and the earlier Phase 146 through Phase 148 structural audits | limiter: the repository now had enough evidence to attempt a clean second pass over the bounded shell-plus-log-plus-key-value system, but widening that simplification pressure into a generic service runtime, supervisor model, dynamic registration, or compiler work would have hidden the owner-local pattern under new abstraction vocabulary | decision: keep compiler, sema, MIR, backend, ABI, target, runtime, and `hal` surfaces closed while removing the hand-threaded five-state choreography from `kernel/src/main.mc`, land one bounded `bootstrap_services` rebuilt-system helper plus one Phase 150 comparison audit, and accept only the additive boot-log capacity increase required to record the extra late-phase marker | tradeoff: the repository now has one honest second-pass rebuild discriminator over the real interactive-service slice, but it still does not claim a reusable runtime framework, broader restart machinery, dynamic registration, or compiler-facing service support | revisit trigger: reopen only when a later real service cannot fit the landed rebuilt-system helper without forcing hidden framework behavior, or when a concrete consumer proves that the current owner-local helper boundary is still too awkward to reuse honestly | reference: /Users/ro/dev/c_modern/docs/plan/active/phase150_one_system_rebuilt_cleanly.txt
- 2026-04-13 | area: docs | context: Phase 149 restart semantics as a first-class pattern after the landed Phase 148 authority-ergonomics audit under reuse | limiter: the repository already had one real key-value restart path with explicit unavailable and reset-state truth, but restart meaning still lived as a Phase 145 outcome and widening that pressure into kernel supervision, transparent retry, request-tracking machinery, or compiler work would have hidden the existing init-owned and service-owned boundary under a broader framework | decision: keep compiler, sema, MIR, backend, ABI, target, and `hal` surfaces closed while publishing one explicit restartable-service contract over the existing key-value restart path, make caller retry obligation and post-restart empty-state truth explicit, keep request identity and replay tracking explicitly unimplemented, and refresh only the freestanding bootstrap pass-line shim so acceptance runs recognize the new Phase 149 marker | tradeoff: the repository now has one honest restart contract over the real interactive-service slice, but it still does not claim kernel supervision, transparent retry, durable recovery, request identity, or replay guarantees | revisit trigger: reopen only when a later real service proves that caller-visible manual retry and reset-state truth are insufficient, or when a concrete consumer justifies explicit request identity, idempotency guarantees, or broader recovery policy | reference: /Users/ro/dev/c_modern/docs/plan/active/phase149_restart_semantics_as_a_first_class_pattern.txt
- 2026-04-13 | area: docs | context: Phase 148 authority ergonomics under reuse after the landed Phase 147 IPC-shape audit under real usage | limiter: the repository now had enough repeated shell, retained-state, and restart pressure to judge authority comfort honestly, but widening that discomfort into ambient authority, overscoped helpers, rebinding machinery, or compiler work would have hidden the existing owner-local endpoint-handle truth under convenience abstractions | decision: keep compiler, sema, MIR, backend, ABI, target, runtime, and `hal` surfaces closed while confirming that explicit endpoint-handle slots remain acceptable authority boundaries under repeated use, admit exactly one narrow capability-local helper `handle_carries_endpoint_authority(...)` to reduce repeated slot-validation ceremony without hiding slot, endpoint, or exact-rights truth, and keep restart handoff init-owned and explicit | tradeoff: the repository now has one honest authority-ergonomics judgment plus one narrow validation helper over the real interactive-service slice, but it still does not claim ambient borrowing, broader authority frameworks, hidden rebinding, or a language-level authority redesign | revisit trigger: reopen only when a later repeated-use phase proves that explicit slot-owned authority is structurally awkward even with the landed helper, or when a concrete owner gap justifies a broader authority contract | reference: /Users/ro/dev/c_modern/docs/plan/active/phase148_authority_ergonomics_under_reuse.txt
- 2026-04-12 | area: docs | context: Phase 147 IPC-shape audit under real usage after the landed Phase 146 service-shape consolidation step | limiter: the repository now had enough real shell, log, and key-value traffic to re-audit request and reply shape honestly, but widening that pressure into a message bus, dynamic payload typing, transport rewrite, or compiler work would have hidden explicit service ownership under generic messaging vocabulary | decision: keep compiler, sema, MIR, backend, ABI, target, runtime, and `hal` surfaces closed while confirming the current compact shell-plus-log-plus-key-value vocabulary is sufficient under repeated mixed command traffic, keep request construction shell-owned and explicit, keep reply-shape divergence service-local, and preserve the fixed `kv_service` write observation into `log_service` instead of adding a bus or generic reply algebra | tradeoff: the repository now has one honest repeated-usage IPC-shape judgment over the real interactive-service slice, but it still does not claim dynamic payloads, generic serialization, a reusable bus, wider shell transport, or broader compiler-facing messaging support | revisit trigger: reopen only when a later landed repeated-usage phase proves that the current compact vocabulary materially blocks another real service path or when a concrete consumer justifies a stronger reply contract or wider payload boundary | reference: /Users/ro/dev/c_modern/docs/plan/active/phase147_ipc_shape_audit_under_real_usage.txt
- 2026-04-12 | area: docs | context: Phase 146 service-shape consolidation after the landed Phase 145 service restart, failure, and usage pressure audit | limiter: the repository now had enough real shell, log, and key-value usage truth to publish one honest common service shape, but turning that comparison into a runtime framework, dynamic registration model, compiler work, or an early reply or retry contract would have hidden explicit service ownership under new abstraction vocabulary | decision: keep compiler, sema, MIR, backend, ABI, target, runtime, and `hal` surfaces closed while publishing one explicit minimal common service shape from the landed shell, log, and key-value owners instead of introducing a framework, keep serial transport ownership separate from shell syntax ownership and peer-service semantics, classify the current shell-versus-stateful differences as intentional service-local structure, and leave reply algebra, request identity, idempotency, replay semantics, and restart-completion guarantees explicitly undefined in Phase 146 | tradeoff: the repository now has one honest published service-shape statement over the real interactive-service slice, but it still does not claim a reusable runtime framework, generic service registration, inheritance, broader compiler-facing expansion, or a stable over-time service contract | revisit trigger: reopen only when a later landed IPC, authority, or restart phase proves that the published owner-local pattern is insufficient or when a concrete consumer justifies a real framework, explicit request identity, or a stronger service-time contract | reference: /Users/ro/dev/c_modern/docs/plan/active/phase146_service_shape_consolidation.txt
- 2026-04-12 | area: docs | context: Phase 145 service restart, failure, and usage pressure audit after the landed Phase 144 stateful key-value service follow-through | limiter: the repository needed one honest failure-and-restart answer over the useful shell-plus-log-plus-key-value system, but widening that pressure into a kernel supervisor, durable recovery, dynamic rebinding, or compiler work would have hidden the service-owned and init-owned boundaries under a broader framework | decision: keep compiler, sema, MIR, backend, ABI, target, and `hal` surfaces closed while routing one explicit shell-visible key-value unavailable consequence, one init-owned fixed-wiring restart, and one explicit post-restart empty-state truth, and keep the resulting manual retry cost user-space visible rather than masking it inside a supervisor or shell framework | tradeoff: the repository now has one honest bounded failure-and-restart answer over the real interactive-service slice, but it still does not claim kernel supervision, transparent recovery, durable state preservation, dynamic rebinding, or broader reliability policy | revisit trigger: reopen only when a later landed service phase proves that fixed-wiring restart and explicit manual retry are no longer sufficient or when a concrete consumer justifies broader recovery or compiler-facing work | reference: /Users/ro/dev/c_modern/docs/plan/active/phase145_service_restart_failure_and_usage_pressure_audit.txt
- 2026-04-12 | area: docs | context: Phase 144 stateful key-value service follow-through after the landed Phase 143 long-lived log-service follow-through | limiter: the repository needed one richer retained-state owner over the real shell path, but widening that pressure into a storage subsystem, database abstraction, middleware framework, or compiler work would have hidden the service-owned state boundary under broader infrastructure | decision: keep compiler, sema, MIR, backend, ABI, target, and `hal` surfaces closed while keeping key-value state bounded in-memory and routing one fixed key-value-write observation into `log_service`, make missing-key and overwrite consequences explicit inside `kv_service`, and stop there rather than widening into persistence, namespaces, transactions, or middleware | tradeoff: the repository now has one honest bounded key-value owner over the real kernel artifact, but it still does not claim durable storage, deletion, iteration, indexing, transactions, broader data modeling, or general service middleware | revisit trigger: reopen only when a later landed restart, storage, or broader namespace phase proves that bounded in-memory key-value state is insufficient or when another concrete consumer justifies widening persistence or compiler-facing surfaces | reference: /Users/ro/dev/c_modern/docs/plan/active/phase144_stateful_key_value_service_follow_through.txt
- 2026-04-12 | area: docs | context: Phase 143 long-lived log service follow-through after the landed Phase 142 serial shell command-routing step | limiter: the repository needed one real retained-state owner over the new shell path, but widening that pressure into durable persistence, files, indexing, observability frameworks, or compiler work would have hidden the service-owned state boundary under broader infrastructure | decision: keep compiler, sema, MIR, backend, ABI, target, and `hal` surfaces closed while keeping log retention bounded and in-memory, route `log append` and `log tail` through `log_service` as the explicit retained-state owner, and make full-capacity behavior one explicit overwrite-on-full consequence rather than a persistence or storage-system claim | tradeoff: the repository now has one honest long-lived retained-log owner over the real kernel artifact, but it still does not claim durability, filesystem semantics, rich log queries, indexing, or broader observability policy | revisit trigger: reopen only when a later landed key-value, restart, or storage phase proves that bounded in-memory retention is insufficient or when another concrete consumer justifies widening persistence or compiler-facing surfaces | reference: /Users/ro/dev/c_modern/docs/plan/active/phase143_long_lived_log_service_follow_through.txt
- 2026-04-12 | area: docs | context: Phase 142 serial shell command routing after the landed Phase 141 interactive service system scope freeze | limiter: the repository needed one real shell-owned command-routing step, but the active serial and IPC path still carries only a bounded four-byte payload and widening that transport together with the shell phase would have reopened IPC, transport, and potentially compiler-owned surfaces all at once | decision: keep compiler, sema, MIR, backend, ABI, target, and `hal` surfaces closed while keeping the serial and IPC payload width fixed at four bytes, map the admitted shell vocabulary onto one compact shell command encoding, route echo, log append, log tail, kv set, kv get, and one malformed command through explicit shell-owned helpers, and keep final reply observation in `serial_service` | tradeoff: the repository now has one honest shell-owned command-routing path over the real kernel artifact, but it still does not claim a wider line-oriented shell transport, a broader REPL framework, dynamic discovery, or any payload-width expansion | revisit trigger: reopen only when the next landed long-lived-state or restart phase proves that four-byte shell frames materially block the admitted service model or when another concrete consumer justifies widening IPC payload ownership itself | reference: /Users/ro/dev/c_modern/docs/plan/active/phase142_serial_shell_command_routing.txt
- 2026-04-12 | area: docs | context: Phase 141 interactive service system scope freeze after the landed Phase 140 serial-ingress composed service graph | limiter: the repository needed one concrete shell owner and one concrete bounded stateful service owner to stop the next Canopus wave from diffusing into shell frameworks, dynamic discovery, persistence policy, or compiler work, but landing the full later command-routing or restart wave here would have hidden the ownership boundary under too much new behavior at once | decision: keep compiler, sema, MIR, backend, ABI, target, and `hal` surfaces closed while landing one concrete shell owner, one concrete key-value owner, one fixed serial-service-to-shell handoff, one fixed admitted service graph, and one focused Phase 141 audit over that frozen boundary | tradeoff: the repository now has a real shell-owned and key-value-owned system boundary in the kernel tree, but it still does not claim full shell command follow-through, persistence semantics, restart policy, dynamic discovery, or a broader framework layer | revisit trigger: reopen only when Phase 142 through Phase 145 land enough repeated command, restart, or state pressure that one additional owner or framework boundary is required beyond the explicit shell, service, and init owners | reference: /Users/ro/dev/c_modern/docs/plan/active/phase141_interactive_service_system_scope_freeze.txt
- 2026-04-12 | area: docs | context: Phase 140 serial-ingress composed service graph after the explicit Phase 139 rebuild deferral and the landed Phase 137 completion-backed serial ingress path | limiter: the repository needed one richer service-to-service composition proof over the real serial ingress path, but widening that pressure into a broker, dynamic routing, a general service graph, or compiler work would have hidden the owner-local routing truth under framework vocabulary | decision: keep compiler, sema, MIR, backend, ABI, target, and `hal` surfaces closed while expressing the richer graph as one serial-service-owned forward plus one reused composition-service fan-out, one bounded aggregate reply back into the serial-service owner, and one debug-owned Phase 140 audit over that fixed path | tradeoff: the repository now proves one deeper serial-to-composition graph over the real kernel artifact, but it still does not claim a broker, dynamic routing, namespace growth, a general service-graph subsystem, or another compiler-facing expansion | revisit trigger: reopen only when a later landed phase shows repeated nontrivial routing, directory, or authority policy duplication that cannot stay inside explicit service owners without a real broker or subsystem replacement decision | reference: /Users/ro/dev/c_modern/docs/plan/active/phase140_serial_ingress_composed_service_graph.txt
- 2026-04-12 | area: docs | context: Phase 139 rebuild decision after the landed Phase 131 through Phase 138 composition, device-boundary, and fatal-primitive wave | limiter: the speculative roadmap reserved a conditional subsystem rebuild, but the landed stress phases still widened the kernel slice through additive owner-local follow-through rather than by exposing repeated owner distortion inside IPC, scheduler or lifecycle, or capability handling | decision: explicitly defer Phase 139, keep the current subsystem cuts intact, and choose one richer serial-ingress composed service graph as the next stress discriminator rather than starting a speculative rewrite | tradeoff: the repository now has one explicit no-rebuild decision tied to real evidence, but it still does not claim that IPC or another owner is permanently settled, and it deliberately postpones structural cleanup until a later concrete consumer proves duplication or blurred ownership | revisit trigger: reopen only when a later landed phase shows repeated nontrivial queue, wake, routing, retirement, or authority policy duplication that one replacement owner would reduce without widening the system | reference: /Users/ro/dev/c_modern/docs/plan/active/phase139_conditional_subsystem_rebuild_decision.txt
- 2026-04-12 | area: docs | context: Phase 138 fatal primitive admission after the explicit `(value, Error)` plateau and ongoing Canopus freestanding pressure | limiter: the repository needed one compiler-owned no-return fatal path that sema, MIR, backend, and real proof code could all share, but widening that pressure into `require`, `expect`, a panic keyword, hidden propagation, unwinding, or a broader runtime failure framework would have turned one explicit invariant edge into a second control-flow system | decision: admit exactly one compiler-known `panic(code)` primitive using ordinary call syntax, require one non-negative compile-time integer fault code, preserve the fatal edge explicitly through sema and MIR, lower it through the existing narrow trap or abort path, and stop there rather than adding broader syntax or runtime policy | tradeoff: the repository now has one honest fatal invariant path over the real compiler and freestanding proof slice, but it still does not claim `require`, recoverable convenience flow, panic payload objects, stack unwinding, or cleanup-on-fatal guarantees | revisit trigger: reopen only when real invariant-heavy code proves plain `if !cond { panic(code) }` insufficient or when a later admitted consumer needs richer fatal payload semantics that cannot stay inside the landed narrow primitive | reference: /Users/ro/dev/c_modern/docs/plan/active/phase138_fatal_primitive_admission.txt
- 2026-04-12 | area: docs | context: Phase 137 optional completion-backed follow-through after the completed Phase 136 device failure containment probe | limiter: the repository needed one real completion-backed device path over the active UART owner, but widening that pressure into `DmaManager`, descriptor pools, generic device-memory policy, or compiler changes would have hidden the concrete ownership boundary under framework vocabulary | decision: keep compiler, sema, MIR, backend, ABI, target, and `hal` surfaces closed, land one active completion-backed receive buffer plus one completion interrupt plus one explicit service consume-and-retire path, and stop there rather than adding reusable DMA or descriptor frameworks | tradeoff: the repository now proves one honest completion-backed transfer slice over the real kernel artifact, but it still does not claim general DMA subsystems, reusable descriptor policy, broader device-memory ownership, or another compiler-facing expansion | revisit trigger: reopen only when a later real device path cannot be expressed by another bounded completion-backed owner-local slice without duplicating nontrivial policy or when a second concrete device requires shared descriptor ownership for correctness | reference: /Users/ro/dev/c_modern/docs/plan/active/phase137_optional_dma_or_equivalent_follow_through.txt
- 2026-04-12 | area: docs | context: Phase 136 device failure containment probe after the completed Phase 135 buffer ownership boundary audit | limiter: the repository needed one explicit unhappy-path statement on the real UART ingress path, but widening that pressure into supervision, retry policy, protocol parsing in the kernel, or new compiler work would have hidden the real device-edge and service-edge boundary under framework language | decision: keep compiler, sema, MIR, backend, ABI, target, and `hal` surfaces closed, land one service-local malformed-byte classification path plus one bounded queue-full drop plus one explicit endpoint-close consequence for serial-service death, and stop there rather than adding restart policy, flow-control policy, or a broader fault-management subsystem | tradeoff: the repository now proves one honest UART unhappy-path slice over the real kernel artifact, but it still does not claim supervision, retry, dynamic flow control, broad device-failure policy, or another compiler-facing expansion | revisit trigger: reopen only when a later real device path cannot be expressed by another bounded drop-or-close consequence without duplicating nontrivial recovery policy or when a real target requires a richer completion-backed failure owner | reference: /Users/ro/dev/c_modern/docs/plan/active/phase136_device_failure_containment_probe.txt
- 2026-04-12 | area: docs | context: Phase 135 buffer ownership boundary audit after the completed Phase 134 minimal device-service handoff | limiter: the repository needed one explicit receive-frame ownership story once the UART path stopped being a one-byte demonstration, but widening that pressure into shared ring buffers, general buffer pools, zero-copy transport, or new compiler work would have hidden the real owner transition under framework vocabulary | decision: keep copy semantics as the explicit ownership baseline, keep compiler, sema, MIR, backend, ABI, target, and `hal` surfaces closed, land one fixed UART-owned staging frame plus one copied IPC payload plus one serial-service-owned consumed log, and stop there rather than adding reusable buffer machinery | tradeoff: the repository now proves one repeated copied receive-frame path over the real kernel artifact, but it still does not claim zero-copy receive, reusable buffer pools, a generic memory subsystem, or another compiler-facing expansion | revisit trigger: reopen only when a later real device path cannot be expressed by another bounded copied frame handoff without duplicating nontrivial ownership policy or when a real target exposes a concrete completion-backed buffer path worth admitting | reference: /Users/ro/dev/c_modern/docs/plan/active/phase135_buffer_ownership_boundary_audit.txt
- 2026-04-12 | area: docs | context: Phase 134 minimal device-service handoff after the completed Phase 133 message lifetime and reuse audit | limiter: the repository needed one honest device-facing interrupt -> byte-read -> IPC -> service path, but widening that pressure into a reusable driver framework, generic device events, `hal` growth, or new compiler work would have turned one concrete UART proof into speculative infrastructure | decision: keep compiler, sema, MIR, backend, ABI, target, and `hal` surfaces closed, land one bounded UART receive owner plus one fixed serial-service handoff path, and record no reusable driver framework beyond the target-local UART slice | tradeoff: the repository now proves one concrete device-service boundary over the real kernel artifact, but it still does not claim a general driver model, broader device policy, DMA, or another compiler-facing expansion | revisit trigger: reopen only when a later real device path cannot be expressed by another narrow owner-local slice without duplicating nontrivial policy or target-local glue | reference: /Users/ro/dev/c_modern/docs/plan/active/phase134_minimal_device_service_handoff.txt
- 2026-04-11 | area: docs | context: post-Phase-133 follow-through on queued transfer abort semantics and the question of whether endpoint close should become syscall-visible | limiter: the repository now has ipc-owned close-or-death cleanup and a landed message-lifetime audit, but no real user-space consumer currently needs an explicit close syscall and widening the syscall surface now would add policy without new pressure | decision: strengthen Phase 133 so it proves a queued attached transfer is scrubbed on close after sender-side invalidation, and keep endpoint close as an ipc-owned internal action rather than adding a syscall-facing close surface | tradeoff: the repository now proves attached-handle invalidation truth across both delivery and queued abort, but user-space still has no explicit endpoint-close syscall and must rely on the current ipc-owned close-or-death path | revisit trigger: reopen only when a real user-space consumer needs explicit endpoint-close semantics that cannot be closed inside the landed ipc owner path or owner-death cleanup | reference: /Users/ro/dev/c_modern/docs/plan/active/phase133_message_lifetime_and_reuse_audit.txt
- 2026-04-11 | area: docs | context: Phase 133 message lifetime and reuse audit after the completed Phase 132 backpressure and blocking audit | limiter: the repository already had one published queue-pressure step and newly landed ipc close-or-death helpers, but it still lacked one published proof that queue slots retire deterministically, payload bytes scrub on reuse, attached-handle lifetime remains explicit after delivery, and endpoint close or owner death aborts queued state without widening into memory management, recovery policy, or compiler-owned work | decision: keep the compiler, sema, MIR, backend, ABI, target, and `hal` surfaces closed, land one bounded repeated-message path plus one direct close-or-death abort path through ipc and syscall owners, publish one canonical Phase 133 note plus one debug-owned Phase 133 audit over slot reuse, payload scrubbing, attached-handle retirement, and close-or-death wake truth, advance the kernel proof marker to 133, and stop there rather than adding refcounting, a general heap, capability GC, replay policy, or new compiler work | tradeoff: the repository now has one explicit published message-lifetime step tied to the real kernel artifact, but it still does not claim general memory management, zero-copy transport, broad object retention, or a wider recovery framework | revisit trigger: reopen only when a later real kernel step needs richer queued-object lifetime, broader capability retirement, or another owned IPC boundary that cannot be closed inside the landed bounded message-lifetime slice | reference: /Users/ro/dev/c_modern/docs/plan/active/phase133_message_lifetime_and_reuse_audit.txt
- 2026-04-11 | area: docs | context: Phase 132 backpressure and blocking audit after the completed Phase 131 fan-out composition probe | limiter: the repository already had one bounded composed topology and honest queue-full or queue-empty observations, but it still lacked one published proof that queue pressure mutates real endpoint-owned waiter state, real lifecycle-owned blocked task state, and real wake-to-ready follow-through without importing fairness policy, queue growth, broader scheduler rewrites, or compiler-owned machinery | decision: keep the compiler, sema, MIR, backend, ABI, target, and `hal` surfaces closed, land one additive endpoint waiter model plus one additive endpoint-send and endpoint-receive blocked task-state slice and one additive backpressure syscall path, publish one canonical Phase 132 note plus one debug-owned Phase 132 audit over one bounded sender-block and one bounded receiver-block queue-pressure path, advance the kernel proof marker to 132, and stop there rather than adding fairness policy, dynamic queue sizing, peer-routing policy, a broader blocking framework, or new compiler work | tradeoff: the repository now has one explicit published backpressure step tied to the real kernel artifact, but it still does not claim fairness, throughput tuning, peer-close wake policy, broader scheduler redesign, or evidence that the next owner is the compiler rather than the landed kernel and documentation layers | revisit trigger: reopen only when a later real kernel step needs message-lifetime reuse, peer-close wake behavior, richer blocking semantics, or another owned IPC boundary that cannot be closed inside the landed bounded backpressure slice | reference: /Users/ro/dev/c_modern/docs/plan/active/phase132_backpressure_and_blocking_audit.txt
- 2026-04-11 | area: docs | context: Phase 131 fan-out composition probe after the completed Phase 130 explicit restart or replacement step | limiter: the repository already had published one-hop service proofs, one dead-service observation, one partial-failure statement, and one explicit init-owned replacement path, but it still lacked one published proof that the same explicit model can carry one bounded composed topology without importing kernel-side broker policy, dynamic namespace growth, or compiler-owned machinery | decision: keep the compiler, sema, MIR, backend, ABI, and `hal` surfaces closed, land one bounded `bootstrap_services` composition owner plus one additive endpoint, capability, and syscall mechanism slice, publish one canonical Phase 131 note plus one debug-owned Phase 131 audit over one init-owned echo-and-log fan-out path, advance the kernel proof marker to 131, and stop there rather than adding a general broker, routing language, dynamic namespace growth, or new compiler work | tradeoff: the repository now has one explicit published composition step tied to the real kernel artifact, but it still does not claim general broker policy, broader routing semantics, namespace growth, or evidence that the next owner is the compiler rather than the landed kernel and documentation layers | revisit trigger: reopen only when a later real kernel step needs backpressure, message-lifetime policy, richer composition routing, or another owned IPC boundary that cannot be closed inside the landed bounded fan-out composition step | reference: /Users/ro/dev/c_modern/docs/plan/active/phase131_fan_in_or_fan_out_composition_probe.txt
- 2026-04-11 | area: docs | context: Phase 130 explicit restart or replacement probe after the completed Phase 129 partial failure propagation step | limiter: the repository already had one published dead-service observation and one published partial-failure statement, but it still lacked one published proof that the current fixed-directory and retained-program-capability model can express one bounded replacement path without importing kernel-side supervision, automatic recovery, rebinding, or compiler-owned machinery | decision: keep the compiler, sema, MIR, backend, ABI, and `hal` surfaces closed, publish one canonical restart-or-replacement note plus one debug-owned Phase 130 audit over one explicit init-owned rerun of the bounded log-service path through the retained program capability and same fixed control endpoint, advance the kernel proof marker to 130, and stop there rather than adding a broader supervision framework, dynamic namespace repair, service rebinding layer, or new compiler work | tradeoff: the repository now has one explicit published restart-or-replacement step tied to the real kernel artifact, but it still does not claim general recovery routing, kernel supervision, dynamic namespace repair, service rebinding, or evidence that the next owner is the compiler rather than the landed kernel and documentation layers | revisit trigger: reopen only when a later real kernel step needs richer IPC-failure routing, a broader replacement policy, or another owned failure boundary that cannot be closed inside the landed explicit restart-or-replacement step | reference: /Users/ro/dev/c_modern/docs/plan/phase130_explicit_restart_or_replacement_probe.txt
- 2026-04-11 | area: docs | context: Phase 129 partial failure propagation after the completed Phase 128 service death observation step | limiter: the repository already had one published service-death observation and one bounded surviving echo-service reply proof, but it still lacked one published statement that the current model carries failure as a partial failure rather than as a whole-system collapse while no restart policy, service rebinding layer, broader failure algebra, or compiler-owned proof yet justified widening | decision: keep the compiler, sema, MIR, backend, ABI, and `hal` surfaces closed, publish one canonical partial-failure note plus one debug-owned Phase 129 audit over one local dead log-service wait result and one fresh bounded echo-service reply path, advance the kernel proof marker to 129, and stop there rather than adding restart machinery, service replacement, rebinding, dynamic namespace repair, or new compiler work | tradeoff: the repository now has one explicit published partial-failure propagation step tied to the real kernel artifact, but it still does not claim general failure routing, recovery supervision, service rebinding, dynamic namespace repair, or evidence that the next owner is the compiler rather than the landed kernel and documentation layers | revisit trigger: reopen only when a later real kernel step needs restart or replacement policy, richer IPC failure routing, or another owned failure boundary that cannot be closed inside the landed partial-failure propagation step | reference: /Users/ro/dev/c_modern/docs/plan/phase129_partial_failure_propagation.txt
- 2026-04-11 | area: docs | context: Phase 128 service death observation after the completed Phase 126 authority lifetime classification step | limiter: the repository already had explicit service wait observations in the landed log-service path, but it still lacked one published failure-boundary statement while no restart policy, supervision framework, broader failure algebra, or compiler-owned proof yet justified widening | decision: keep the compiler, sema, MIR, backend, ABI, and `hal` surfaces closed, publish one canonical service-death note plus one debug-owned Phase 128 audit over the landed log-service wait observation path, advance the kernel proof marker to 128, and stop there rather than adding a broader supervision framework, restart policy, service rebinding layer, or new compiler work | tradeoff: the repository now has one explicit published service-death observation step tied to the real kernel artifact, but it still does not claim general failure routing, restart semantics, supervision policy, dynamic namespace recovery, or evidence that the next owner is the compiler rather than the landed kernel and documentation layers | revisit trigger: reopen only when a later real kernel step needs partial-failure propagation, restart or replacement policy, or another owned failure boundary that cannot be closed inside the landed service death observation step | reference: /Users/ro/dev/c_modern/docs/plan/phase128_service_death_observation.txt
- 2026-04-11 | area: tooling | context: first real kernel adoption of Phase 127 multi-file project-mode modules reached the larger `debug` and `bootstrap_audit` audit owners after successful `capability`, `endpoint`, and `bootstrap_services` migrations | limiter: merged logical modules were parsed part-by-part, but sema stored expression and statement facts in maps keyed only by `SourceSpan`, so later parts could collide with earlier parts because their line numbers restarted at 1 | decision: keep the first-cut merged-module model, but offset every later part into a unique synthetic line range before semantic checking so span-keyed sema facts and MIR lowering no longer collide across logical-module parts | tradeoff: the repository now supports the larger `debug` and `bootstrap_audit` owners as real multi-file logical modules without widening the language model or artifact identity again, but semantic diagnostics for module-set parts still use one canonical source path rather than exact per-part file ownership | revisit trigger: reopen when real project pressure justifies per-part source identity in diagnostics rather than the current canonical-path plus synthetic-line approach | reference: /Users/ro/dev/c_modern/compiler/driver/builder.cpp
- 2026-04-11 | area: docs | context: landed Phase 127 project-mode multi-file module implementation after confirming real kernel-shaped pressure for subdividing one ordinary module without inventing a second public boundary | limiter: the repository still needed a bounded answer that avoided widening immediately into hierarchical packages, source-language partial modules, implicit directory ownership, direct-source multi-file behavior, or multi-file `internal` semantics | decision: land explicit target-owned `module_sets.<module>.files = [...]`, keep import syntax unchanged, key `.mci` and build-state artifacts by logical module identity only when a module set is used, and keep direct-source, implicit-directory, and multi-file `internal` behavior deferred | tradeoff: project mode now supports one logical ordinary module across multiple files while legacy single-file project artifacts remain stable, but the repository still deliberately stops short of a broader module-system rewrite | revisit trigger: reopen if explicit file-list source sets prove too narrow, or if honest pressure appears for direct-source multi-file workflows, multi-file `internal` modules, or source-language module-part declarations | reference: /Users/ro/dev/c_modern/docs/plan/active/phase127_project_mode_multi_file_modules.txt
- 2026-04-11 | area: tooling | context: first-cut Phase 127 implementation needed multi-file logical modules without reopening AST and diagnostics plumbing across the entire compiler | limiter: semantic diagnostics still carry one file path plus one span, and the AST does not yet track per-node owning source file across a merged logical module | decision: merge the parsed source files for one logical project-mode module into one semantic input and keep one canonical primary source path for interface metadata and logical-module diagnostics in the first cut instead of widening the AST and diagnostic model immediately | tradeoff: the first admitted multi-file module slice is implemented with deterministic checking, visibility, `.mci`, and incremental rebuild behavior, but some semantic diagnostics for module-set parts can still anchor to the logical module's canonical source path rather than the exact contributing file | revisit trigger: reopen when real project pressure makes per-part semantic diagnostic paths worth the broader AST and diagnostics plumbing work | reference: /Users/ro/dev/c_modern/compiler/driver/builder.cpp
- 2026-04-11 | area: docs | context: Phase 126 authority lifetime classification after the completed Phase 125 invalidation and rejection audit step | limiter: the repository already had one published final-holder authority-loss proof from Phase 125, but it still lacked one published statement distinguishing a bounded long-lived authority path from a bounded short-lived delegated path while no broader lifetime framework, failure semantics, or compiler-owned proof yet justified widening | decision: keep the compiler and low-level surfaces closed, publish one canonical authority lifetime classification note plus one debug-owned Phase 126 audit over the landed surviving control handle and invalidated delegated handle paths, advance the kernel proof marker to 126, and stop there rather than adding a broader lifetime framework, failure policy, or new compiler work | tradeoff: the repository now has one explicit published long-lived versus short-lived authority classification step tied to the real kernel artifact, but it still does not claim a general lifetime framework, broader failure policy, dynamic namespace growth, or evidence that the next owner is the compiler rather than the landed kernel and documentation layers | revisit trigger: reopen only when a later real kernel step needs service-failure routing, IPC composition under failure, or another owned authority boundary that cannot be closed inside the landed authority lifetime classification step | reference: /Users/ro/dev/c_modern/docs/plan/phase126_authority_lifetime_classification.txt
- 2026-04-11 | area: docs | context: Phase 125 invalidation and rejection audit after the completed Phase 124 delegation-chain stress step | limiter: the repository already had one explicit invalidated-source refusal from Phase 118 and one deeper delegation-chain proof from Phase 124, but it still lacked one published aggregate proof that an explicitly invalidated holder is rejected consistently at the current send and receive use sites while no broader revocation framework, failure semantics, or compiler-owned proof yet justified widening | decision: keep the compiler and low-level surfaces closed, publish one canonical invalidation note plus one debug-owned Phase 125 audit over a bounded final-holder authority-loss path, advance the kernel proof marker to 125, and stop there rather than adding a broader revocation algebra, service-failure policy, or new compiler work | tradeoff: the repository now has one explicit published authority-loss step tied to the real kernel artifact, but it still does not claim a revocation framework, broader failure policy, dynamic namespace growth, or evidence that the next owner is the compiler rather than the landed kernel and documentation layers | revisit trigger: reopen only when a later real kernel step needs broader lifetime classification, service-failure routing, or another owned authority boundary that cannot be closed inside the landed invalidation and rejection audit step | reference: /Users/ro/dev/c_modern/docs/plan/phase125_invalidation_and_rejection_audit.txt
- 2026-04-11 | area: docs | context: Phase 124 delegation-chain stress after the completed Phase 123 next-plateau audit | limiter: the repository already had one retained transferred handle and one explicit invalidated-source refusal from Phase 118 plus one published Phase 123 plateau, but it still lacked one proof that the same authority could survive a deeper delegation hop while no broader revocation framework, broader failure semantics, or compiler-owned proof yet justified widening | decision: keep the compiler and low-level surfaces closed, publish one canonical delegation-chain note plus one debug-owned Phase 124 audit over a bounded two-hop transfer chain and refused reuse at each prior holder, advance the kernel proof marker to 124, and stop there rather than adding a broader authority framework, another service owner, or new compiler work | tradeoff: the repository now has one explicit published post-plateau authority-stress step tied to the real kernel artifact, but it still does not claim a revocation algebra, broader failure policy, dynamic namespace growth, or evidence that the next owner is the compiler rather than the landed kernel and documentation layers | revisit trigger: reopen only when a later real kernel step needs broader invalidation semantics, failure propagation, or another owned authority boundary that cannot be closed inside the landed delegation-chain stress step | reference: /Users/ro/dev/c_modern/docs/plan/phase124_delegation_chain_stress.txt
- 2026-04-11 | area: docs | context: Phase 123 next-plateau audit after the completed Phase 122 target-surface audit | limiter: the repository already had a real bounded running-kernel statement, running-system support statement, image-contract hardening step, and target-surface audit, but it still lacked one explicit plateau statement naming those truths together while no broader platform claim, broader target-family claim, general loading policy, or compiler-owned proof yet justified widening | decision: keep the compiler and low-level surfaces closed, publish one canonical next-plateau note plus one debug-owned Phase 123 audit over the landed Phase 122 target truth, advance the kernel proof marker to 123, and stop there rather than adding broader platform, loading, target, or new compiler work | tradeoff: the repository now has one explicit published plateau statement tied to the real kernel artifact, but it still does not claim a broader Canopus platform, broader target support, general loading or packaging, or evidence that the next owner is the compiler rather than the landed kernel and documentation layers | revisit trigger: reopen only when a later real kernel step needs broader target support, loading policy, packaging policy, or another owned boundary that cannot be closed inside the landed next-plateau audit step | reference: /Users/ro/dev/c_modern/docs/plan/phase123_next_plateau_audit.txt
- 2026-04-11 | area: docs | context: Phase 122 target-surface audit after the completed Phase 121 kernel image-contract hardening step | limiter: the repository already had a real bounded running-system support statement from Phase 120 and one explicit image-contract hardening step from Phase 121, but the bootstrap target family still needed one explicit proof-owned audit step while no dynamic loading, broader target-family claim, or compiler-owned proof yet justified widening | decision: keep the compiler and low-level surfaces closed, publish one canonical target-surface note plus one debug-owned Phase 122 audit over the landed Phase 121 image truth, advance the kernel proof marker to 122, and stop there rather than adding dynamic loading, a service manager, a broader target-family claim, or new compiler work | tradeoff: the repository now has one explicit published target-surface statement tied to the real kernel artifact, but it still does not claim broader loader policy, broader target support, or evidence that the next owner is the compiler rather than the landed kernel and documentation layers | revisit trigger: reopen only when a later real kernel step needs target-family widening, loader policy, or another owned boundary that cannot be closed inside the landed target-surface audit step | reference: /Users/ro/dev/c_modern/docs/plan/phase122_target_surface_audit.txt
- 2026-04-10 | area: docs | context: Phase 121 kernel image-contract hardening after the completed Phase 120 running-system support statement | limiter: the repository already had a real bounded running-system support statement from Phase 120, but the manifest, bootstrap target identity, runtime startup, emitted image-input contract, and linked executable shape still needed one explicit proof-owned hardening step while no dynamic loading, broader target-family claim, or compiler-owned proof yet justified widening | decision: keep the compiler and low-level surfaces closed, publish one canonical kernel image-contract hardening note plus one debug-owned Phase 121 audit over the landed Phase 120 support truth, advance the kernel proof marker to 121, and stop there rather than adding dynamic loading, a service manager, a broader target-family claim, or new compiler work | tradeoff: the repository now has one explicit published image-contract statement tied to the real kernel artifact, but it still does not claim broader loader policy, broader target support, or evidence that the next owner is the compiler rather than the landed kernel and documentation layers | revisit trigger: reopen only when a later real kernel step needs target-family widening, loader policy, or another owned boundary that cannot be closed inside the landed image-contract hardening step | reference: /Users/ro/dev/c_modern/docs/plan/phase121_kernel_image_contract_hardening.txt
- 2026-04-10 | area: docs | context: Phase 120 running-system support statement after the completed Phase 119 namespace-pressure audit | limiter: the repository already had a real bounded multi-service bring-up from Phase 117, a real delegated request-reply path from Phase 118, and a real fixed init-owned namespace answer from Phase 119, but it still had no single canonical support statement naming that admitted running-system slice while no dynamic discovery, dynamic loading, broader init framework, or compiler-owned proof yet justified another expansion | decision: keep the compiler and low-level surfaces closed, publish one canonical running-system support statement plus one debug-owned Phase 120 aggregate audit over the landed Phase 117 through Phase 119 truth, advance the kernel proof marker to 120, and stop there rather than adding dynamic discovery, dynamic loading, a service manager, or new compiler work | tradeoff: the repository now has one explicit published running-system statement tied to the real kernel artifact, but it still does not claim a general namespace model, a broader freestanding workflow, or evidence that the next owner is the compiler rather than the landed kernel and documentation layers | revisit trigger: reopen only when a later real kernel step needs image-contract hardening, target-surface work, a broader namespace or loader policy, or another owned boundary that cannot be closed inside the landed running-system support statement | reference: /Users/ro/dev/c_modern/docs/plan/phase120_running_system_support_statement.txt
- 2026-04-10 | area: docs | context: Phase 119 namespace-pressure audit after the completed Phase 118 request-reply and delegation follow-through | limiter: the repository already had a real bounded multi-service bring-up from Phase 117 and a real delegated reply-capable transfer-service path from Phase 118, but it still had not published the smallest explicit answer to the next namespace pressure point, while no dynamic discovery, general namespace model, or compiler-owned proof yet justified a broader expansion | decision: keep the compiler and low-level surfaces closed, publish one fixed init-owned three-entry service directory through a debug-owned Phase 119 audit plus one root-proof helper, advance the kernel proof marker to 119, and stop there rather than adding dynamic service discovery, a broader namespace framework, a service manager, or new compiler work | tradeoff: the repository now has one honest fixed-directory answer to the current namespace pressure over the real kernel artifact, but it still does not claim general namespace policy, dynamic discovery, or evidence that the next owner is the compiler rather than the landed kernel and documentation layers | revisit trigger: reopen only when a later real kernel step needs a broader running-system support statement, a real namespace lookup mechanism, image-contract work, or another owned boundary that cannot be closed inside the landed init-owned directory answer and current service owners | reference: /Users/ro/dev/c_modern/docs/plan/phase119_namespace_pressure_audit.txt
- 2026-04-10 | area: docs | context: Phase 118 request-reply and delegation follow-through after the completed Phase 117 init-orchestrated multi-service bring-up | limiter: the repository already had a real delegated reply-capable transfer-service path from Phase 107 and an aggregate running-system publication from Phase 117, but it still had not published the delegated request-reply contract together with the refused invalidated-source action, while no namespace, multi-child, or compiler-owned proof yet justified a broader expansion | decision: keep the compiler and low-level surfaces closed, publish the landed delegated reply route through one debug-owned Phase 118 audit plus one invalidated-source send probe, advance the kernel proof marker to 118, and stop there rather than adding namespace policy, multi-child service orchestration, a general service framework, or new compiler work | tradeoff: the repository now has one honest delegated request-reply plus refused-source-action claim over the real kernel artifact, but it still does not claim namespace policy, multi-child orchestration, or evidence that the next owner is the compiler rather than the landed kernel and documentation layers | revisit trigger: reopen only when a later real kernel step needs namespace pressure resolution, simultaneous multi-child service interaction, a broader running-system support statement, image-contract work, or another owned boundary that cannot be closed inside the landed service owners and current init orchestration | reference: /Users/ro/dev/c_modern/docs/plan/phase118_request_reply_and_delegation_follow_through.txt
- 2026-04-10 | area: docs | context: Phase 117 init-orchestrated multi-service bring-up after the completed Phase 116 MMU activation barrier follow-through | limiter: the repository already had real log, echo, and transfer service proofs over the running kernel slice, but those flows were still published only as separate phase-local slices while no broader service-discovery, dynamic-loading, or compiler-owned framework proof yet justified widening the service boundary further | decision: keep the compiler and low-level surfaces closed, publish the landed service owners through one aggregate init-owned Phase 117 audit over explicit program-cap and endpoint authority, advance the kernel proof marker to 117, and stop there rather than adding dynamic loading, namespace policy, a general service framework, or new compiler work | tradeoff: the repository now has one honest bounded multi-service bring-up claim over the real kernel artifact, but it still does not claim general running-system policy, service discovery, or evidence that the next owner is the compiler rather than the landed kernel and documentation layers | revisit trigger: reopen only when a later real kernel step needs richer delegation, bounded failure routing, namespace pressure, image-contract work, or another owned boundary that cannot be closed inside the landed service owners and init orchestration | reference: /Users/ro/dev/c_modern/docs/plan/phase117_init_orchestrated_multi_service_bring_up.txt
- 2026-04-10 | area: docs | context: Phase 116 MMU activation barrier follow-through after the completed Phase 115 timer ownership hardening | limiter: the repository already had an explicit MMU owner and a real translation-root activation path inside the running kernel slice, but activation still looked like a pure owner-local state flip while no broader target-entry or device proof yet justified a larger low-level surface | decision: add exactly one `hal.memory_barrier()` hook, route translation-root activation through that hook inside `kernel/src/mmu.mc`, keep `kernel/src/address_space.mc` and the running kernel path behavior unchanged, and stop there rather than widening into a broader barrier taxonomy, target-entry redesign, interrupt redesign, or speculative compiler growth | tradeoff: the repository now exposes one honest barrier-facing hook at the MMU boundary over the landed running kernel slice, but it still does not claim SMP completion, broader barrier policy, or evidence that another compiler or target wave is the right next owner | revisit trigger: reopen only when a later real kernel step exposes a concrete barrier, entry-convention, ABI, target, or emitted-artifact failure that cannot be closed inside the landed `hal` plus kernel owners | reference: /Users/ro/dev/c_modern/docs/plan/phase116_mmu_activation_barrier_follow_through.txt
- 2026-04-10 | area: docs | context: Phase 115 timer ownership hardening after the completed Phase 114 address-space and MMU ownership split | limiter: the repository already had a real timer-backed wake path inside the running kernel slice, but `kernel/src/interrupt.mc` still advanced timer state and selected wake observations directly while no concrete kernel proof required reopening sema, MIR, backend, ABI, or language work to expose a cleaner timer owner | decision: keep compiler work closed, move interrupt-tick advancement, wake observation, and single-delivery wake consumption into `kernel/src/timer.mc`, keep `kernel/src/interrupt.mc` at architecture entry plus dispatch classification only, add one debug-owned Phase 115 audit, and stop there rather than widening into a broad timer framework, device policy layer, scheduler redesign, or speculative compiler changes | tradeoff: the repository now exposes one honest timer-owned wake-delivery boundary over the landed running kernel slice, but it still does not claim broader device policy, timer APIs, preemption, or evidence that the next owner is the compiler rather than the kernel tree | revisit trigger: reopen only when a later real kernel step exposes a concrete owned failure in representation, layout, ABI, target entry, emitted artifact truth, barrier behavior, or low-level timer mechanics that cannot be closed inside the landed kernel owners | reference: /Users/ro/dev/c_modern/docs/plan/phase115_timer_ownership_hardening.txt
- 2026-04-10 | area: docs | context: Phase 114 address-space and MMU ownership split after the completed Phase 113 interrupt entry and generic dispatch boundary | limiter: the repository already had explicit address-space mappings, saved user-entry frames, and real spawn follow-through inside the running kernel slice, but `kernel/src/address_space.mc` still carried raw translation-root mechanics directly and no concrete kernel proof required reopening sema, MIR, backend, ABI, or language work to expose that boundary honestly | decision: keep compiler work closed, add one narrow `kernel/src/mmu.mc` owner for translation-root construction and activation, route first-user and child-spawn follow-through through MMU-owned translation roots instead of raw page-table values, add one debug-owned Phase 114 audit, and stop there rather than widening into a broad VM subsystem, loader redesign, timer hardening, or speculative compiler changes | tradeoff: the repository now exposes one honest address-space/MMU ownership split over the landed running kernel slice, but it still does not claim general MMU policy, broader loader contracts, or evidence that the next owner is the compiler rather than the kernel tree | revisit trigger: reopen only when a later real kernel step exposes a concrete owned failure in representation, layout, ABI, target entry, emitted artifact truth, timer hardening, or low-level MMU behavior that cannot be closed inside the landed kernel owners | reference: /Users/ro/dev/c_modern/docs/plan/phase114_address_space_and_mmu_ownership_split.txt
- 2026-04-10 | area: docs | context: Phase 113 interrupt entry and generic dispatch boundary after the completed Phase 112 syscall boundary thinness audit | limiter: the repository already had a real timer-backed wake path inside the running kernel slice, but `kernel/src/interrupt.mc` was still only a controller skeleton and no concrete kernel proof required reopening sema, MIR, backend, ABI, or language work to express one explicit architecture-entry plus generic-dispatch handoff | decision: keep compiler work closed, land one interrupt-owned architecture-entry record plus one generic dispatch helper over the existing timer-backed wake path, add one owner-local validator and one debug-owned Phase 113 audit, and stop there rather than widening into a broad interrupt framework, public timer API, MMU work, or speculative compiler changes | tradeoff: the repository now exposes one honest interrupt boundary over the landed running kernel slice, but it still does not claim trap-framework completion, preemption, public timer policy, or evidence that the next owner is the compiler rather than the kernel tree | revisit trigger: reopen only when a later real kernel step exposes a concrete owned failure in representation, layout, ABI, target entry, emitted artifact truth, timer policy, or hardware-boundary behavior that cannot be closed inside the landed kernel owners | reference: /Users/ro/dev/c_modern/docs/plan/phase113_interrupt_entry_and_generic_dispatch_boundary.txt
- 2026-04-10 | area: docs | context: Phase 112 syscall boundary thinness audit after the completed Phase 111 scheduler and lifecycle ownership clarification | limiter: the repository already had explicit capability, endpoint, address-space, lifecycle, scheduler, and debug owners over the running kernel slice, but `kernel/src/syscall.mc` still retained local helpers for queue semantics, handle-transfer semantics, and child bootstrap construction, while no concrete kernel proof required reopening sema, MIR, backend, ABI, or language work | decision: keep compiler work closed, move the remaining landed syscall-family semantics behind the existing kernel owner modules, add owner-local validators plus one debug-owned Phase 112 audit, and stop there rather than widening syscall families, introducing public syscall wrappers, or inventing new compiler surfaces | tradeoff: the syscall edge is now thinner and more mechanically honest, but the repository still does not claim broader syscall coverage, hardware-boundary completion, or evidence that the next owner is the compiler rather than the kernel tree | revisit trigger: reopen only when a later real kernel step exposes a concrete owned failure in representation, layout, ABI, target entry, emitted artifact truth, or another boundary that cannot be closed inside the landed kernel owners | reference: /Users/ro/dev/c_modern/docs/plan/phase112_syscall_boundary_thinness_audit.txt
- 2026-04-10 | area: docs | context: Phase 109 first running Canopus kernel slice audit after the completed Phase 108 kernel image and program-cap audit | limiter: the repository now had one honest real-kernel artifact with explicit boot, user-entry, IPC, spawn, timer, service, user-to-user authority, and image/program-cap contract proofs, but it still had no single canonical support statement naming exactly what the first running kernel slice admits and what it still defers | decision: implement Phase 109 as one bounded support-statement audit over the landed kernel artifact using one aggregate root-module validator, one Phase 109 proof marker, one focused freestanding regression that checks build/run/MIR plus the published support wording in the canonical docs, and stop there rather than admitting broader platform, packaging, loader, or multi-service completion claims | tradeoff: the repository now has one explicit published first-running-kernel statement tied to the real artifact, but it still does not claim a general Canopus platform, broader target support, or broader user-space policy completion | revisit trigger: reopen when the next active kernel phase needs claims beyond the admitted running slice, such as broader service policy, platform packaging, loader ownership, or another target boundary that cannot be closed inside the landed support audit | reference: /Users/ro/dev/c_modern/docs/plan/phase109_first_running_canopus_kernel_slice_audit.txt
- 2026-04-10 | area: docs | context: Phase 108 kernel image and program-cap audit after the completed Phase 107 real user-to-user capability transfer proof | limiter: the repository now had one honest real-kernel artifact with explicit boot, user-entry, IPC, spawn, timer, service, and user-to-user authority proofs, but the emitted kernel image-input artifacts and explicit program-cap provenance were still only implicit across `kernel/build.toml`, build outputs, and the root proof module | decision: implement Phase 108 as one bounded audit over the real kernel project using the existing emitted module objects plus freestanding runtime object as the explicit image-input artifact set, one manual relink follow-through, one root-module provenance check for bootstrap and service program capabilities, and stop there rather than admitting boot-image packaging, install policy, or a public loader surface | tradeoff: the repository now has one honest kernel-image and program-cap contract anchor over the landed service slice, but it still does not claim boot-image assembly, broader target support, or the final first-running-system support statement | revisit trigger: reopen when the next active proof needs a published first-running-kernel support statement, boot-image packaging, broader loader policy, or another target-owned artifact boundary that cannot be closed inside the landed emitted-artifact audit | reference: /Users/ro/dev/c_modern/docs/plan/phase108_kernel_image_and_program_cap_audit.txt
- 2026-04-10 | area: docs | context: Phase 107 real user-to-user capability transfer proof after the completed Phase 106 real echo-service request-reply proof | limiter: the repository now had one honest real-kernel boot, first-user-entry, endpoint-plus-handle object core, real syscall IPC, attached-handle transfer, spawn-plus-wait, timer-backed block-wake, explicit init bootstrap handoff, critique-hardened helper contracts, and two bounded real service proofs, but the landed transfer helper still only completed sender-side handle removal when receive ran against the sender-owned table, so the path was not yet honest for a real user-to-user transfer | decision: implement Phase 107 as one repository-owned kernel follow-through using one corrected syscall transfer owner, one bounded `transfer_service` helper module, one spawned service task with one explicit service-visible control endpoint handle, one init-retained local endpoint handle, one init-transferred duplicate endpoint handle, one service-issued follow-through send on the transferred endpoint, and one bounded service reap through the landed wait path, and stop there rather than admitting wait-handle transfer, general authority routing, or a broader user-space runtime | tradeoff: the repository now has one honest real user-to-user endpoint-transfer anchor over the landed service slice, but it still does not claim general capability delegation policy, wait-handle transfer, or a running multi-service system | revisit trigger: reopen when the next active proof needs broader authority routing, wait-handle transfer, image or program-cap audit pressure, or another service-policy owner that cannot be closed inside the landed transfer-service slice | reference: /Users/ro/dev/c_modern/docs/plan/phase107_real_user_to_user_capability_transfer.txt
- 2026-04-10 | area: docs | context: Phase 106 real echo-service request-reply proof after the completed Phase 105 real log-service handshake | limiter: the repository now had one honest real-kernel boot, first-user-entry, endpoint-plus-handle object core, real syscall IPC, attached-handle transfer, spawn-plus-wait, timer-backed block-wake, explicit init bootstrap handoff, critique-hardened helper contracts, and one bounded real service handshake, but it still had no active proof that a second service shape could preserve request-dependent reply behavior over that same landed kernel-owned boundary | decision: implement Phase 106 as one repository-owned kernel follow-through using one bounded `echo_service` helper module, one dedicated echo-service program capability, one spawned service task with one explicit service-visible endpoint handle, one init-issued two-byte request, one request-derived two-byte reply, and one bounded service reap through the landed wait path, and stop there rather than admitting service registries, multi-service startup policy, or a broader user-space runtime | tradeoff: the repository now has one honest second service-policy anchor over the landed Phase 105 slice, but it still does not claim a general service framework, user-to-user transfer, or a running multi-service system | revisit trigger: reopen when the next active proof needs explicit user-to-user capability transfer across the real kernel boundary, a third service shape, or another service-policy owner that cannot be closed inside the landed echo-service slice | reference: /Users/ro/dev/c_modern/docs/plan/phase106_real_echo_service_request_reply.txt
- 2026-04-10 | area: docs | context: Phase 105 real log-service handshake after the completed Phase 104 critique-response hardening pass | limiter: the repository now had one honest real-kernel boot, first-user-entry, endpoint-plus-handle object core, real syscall IPC, attached-handle transfer, spawn-plus-wait, timer-backed block-wake, explicit init bootstrap handoff, and critique-hardened helper contracts, but it still had no active proof that one real service task could launch and complete a bounded request-and-acknowledgment flow over that same landed kernel-owned boundary | decision: implement Phase 105 as one repository-owned kernel follow-through using one bounded `log_service` helper module, one dedicated log-service program capability, one spawned service task with one explicit service-visible endpoint handle, one init-issued request, one service-issued acknowledgment, and one bounded service reap through the landed wait path, and stop there rather than admitting echo-service generalization, user-to-user capability transfer, or a broader service framework | tradeoff: the repository now has one honest real-kernel service-policy anchor over the landed hardening pass, but it still does not claim multi-service startup, registry policy, or a running general user-space system | revisit trigger: reopen when the next active proof needs a second bounded service, explicit user-to-user transfer across the real kernel boundary, or another service-policy owner that cannot be closed inside the landed log-service slice | reference: /Users/ro/dev/c_modern/docs/plan/phase105_real_log_service_handshake.txt
- 2026-04-10 | area: docs | context: Phase 103 init bootstrap capability handoff after the completed Phase 102 timer sleep and scheduler block-wake proof | limiter: the repository now had one honest real-kernel boot, first-user-entry, endpoint-plus-handle object core, real syscall IPC, attached-handle transfer, spawn-plus-wait, and timer-backed block-wake path, but it still had no explicit boot contract proving which authorities `init` actually receives and that root authority remains kernel-only | decision: implement Phase 103 as one repository-owned kernel follow-through using one bounded init bootstrap-capability-set record, one deterministic handoff observation, one init-visible endpoint handle plus one launchable program capability, and one explicit validation that wait handles are not ambient and root authority stays kernel-owned, and stop there rather than admitting real service launch, namespace policy, or broader init orchestration | tradeoff: the repository now has one honest explicit boot-authority anchor over the landed timer-backed lifecycle proof, but it still does not claim log-service truth, loader-service design, or broader user-space policy | revisit trigger: reopen when the next active proof needs real service launch, broader bootstrap-capability taxonomy, namespace policy, or another kernel-owned boundary that cannot be closed inside the landed handoff slice | reference: /Users/ro/dev/c_modern/docs/plan/phase103_init_bootstrap_capability_handoff.txt
- 2026-04-09 | area: docs | context: Phase 102 timer sleep and scheduler block-wake proof after the completed Phase 101 program-cap spawn and wait path | limiter: the repository now had one honest real-kernel boot, first-user-entry, endpoint-plus-handle object core, real syscall IPC, attached-handle transfer, and spawn-plus-wait path, but it still had no active proof that one child task could be blocked by an explicit sleep request and then woken by one monotonic tick source without widening immediately into a public timer API or broader scheduler policy | decision: implement Phase 102 as one repository-owned kernel follow-through using widened state, timer, and syscall helper records, one consumed-on-success sleep request, one deterministic `WouldBlock` sleep observation, one explicit wake delivery, and one successful ready restoration before the existing wait path reaps the child, and stop there rather than admitting kill semantics or a public timer surface | tradeoff: the repository now has one honest timer-backed block-wake anchor over the landed spawn-and-wait boundary, but it still does not claim kill semantics, a public timer API, or broader scheduler completion | revisit trigger: reopen when the next active proof needs kill semantics, multi-child scheduling policy, periodic timers, or another kernel-owned boundary that cannot be closed inside the landed sleep slice | reference: /Users/ro/dev/c_modern/docs/plan/phase102_timer_sleep_and_scheduler_block_wake_proof.txt
- 2026-04-09 | area: docs | context: Phase 101 program-cap spawn and wait path after the completed Phase 100 capability-carrying IPC transfer proof | limiter: the repository now had one honest real-kernel boot, first-user-entry, endpoint-plus-handle object core, real syscall IPC, and one attached-handle transfer path, but it still had no active proof that one explicit launchable program capability could create one child lifecycle path and that the parent could observe and reap that child through one explicit wait handle without widening immediately into timer-backed blocking or service bring-up | decision: implement Phase 101 as one repository-owned kernel follow-through using widened state, capability, and syscall helper records, one consumed-on-success program capability, one explicit wait-handle installation, one deterministic pre-exit `WouldBlock` wait, and one successful post-exit wait that reaps the child, and stop there rather than admitting timer-backed blocking, kill semantics, or service launch | tradeoff: the repository now has one honest explicit spawn-and-wait anchor over the landed syscall boundary, but it still does not claim timer-backed blocking, kill semantics, or init-owned service truth | revisit trigger: reopen when the next active proof needs timer-backed blocking, kill semantics, multi-child policy, or another kernel-owned boundary that cannot be closed inside the landed spawn-and-wait slice | reference: /Users/ro/dev/c_modern/docs/plan/phase101_program_cap_spawn_and_wait_path.txt
- 2026-04-09 | area: docs | context: Phase 100 capability-carrying IPC transfer after the completed Phase 99 syscall entry and byte-only IPC proof | limiter: the repository now had one honest real-kernel boot, first-user-entry, endpoint-plus-handle object core, and one real byte-only syscall boundary, but it still had no active proof that one attached endpoint handle could move through that boundary with destructive-on-success send behavior and receiver-local reinstall without widening immediately into spawn, wait, or timer-backed blocking | decision: implement Phase 100 as one repository-owned kernel follow-through using widened capability, endpoint, and syscall helper records, one attached-handle send request, one receiver-local reinstall request, sender-side handle removal only after successful queue admission, and one bounded post-receive use of the transferred handle, and stop there rather than admitting spawn, wait, timer-backed blocking, or service bring-up | tradeoff: the repository now has one honest capability-carrying syscall anchor over the landed byte-only IPC boundary, but it still does not claim spawn, wait, timer-backed blocking, or init-owned service truth | revisit trigger: reopen when the next active proof needs process spawn, wait semantics, timer-backed blocking, or another kernel-owned boundary that cannot be closed inside the landed attached-handle transfer slice | reference: /Users/ro/dev/c_modern/docs/plan/phase100_capability_carrying_ipc_transfer.txt
- 2026-04-09 | area: docs | context: Phase 99 syscall entry and byte-only IPC after the completed Phase 98 kernel endpoint and handle core proof | limiter: the repository now had one honest real-kernel boot, first-user-entry, and endpoint-plus-handle object-core artifact, but it still had no active proof that one init-visible handle could drive one real send path and one real receive path through an explicit syscall gate without widening immediately into attached-handle transfer, spawn, wait, or timer-backed blocking | decision: implement Phase 99 as one repository-owned kernel follow-through using widened syscall helper records, one explicit gate-open step, one byte-only send request, one byte-only receive request, one explicit receive observation, and one deterministic `WouldBlock` receive after queue drain, and stop there rather than admitting attached-handle transfer, spawn, wait, timer-backed blocking, or service bring-up | tradeoff: the repository now has one honest syscall-boundary anchor over the landed endpoint and handle object core, but it still does not claim attached-handle transfer, spawn, wait, timer-backed blocking, or init-owned service truth | revisit trigger: reopen when the next active proof needs attached-handle transfer across the syscall boundary, spawn or wait semantics, timer-backed blocking, or another kernel-owned boundary that cannot be closed inside the landed byte-only syscall slice | reference: /Users/ro/dev/c_modern/docs/plan/phase99_syscall_entry_and_byte_only_ipc.txt
- 2026-04-09 | area: docs | context: Phase 98 kernel endpoint and handle core after the completed Phase 97 address-space and first-user-entry proof | limiter: the repository now had one honest real-kernel boot and first-user-entry artifact, but it still had no active proof that the same kernel can own one explicit endpoint table, one explicit per-process handle table, and one bounded in-kernel queued-message lifetime without widening immediately into syscall entry, byte-only IPC, or service policy | decision: implement Phase 98 as one repository-owned kernel follow-through using widened capability and endpoint helper modules, one init-owned endpoint installation, one init-visible handle-table entry, one bounded queued bootstrap message, and one deterministic consume-and-reset message lifetime proof, and stop there rather than admitting syscall entry, user-visible IPC, attached-handle transfer, or service bring-up | tradeoff: the repository now has one honest kernel object-core anchor for endpoints and handles, but it still does not claim real syscalls, byte-only IPC, attached-handle transfer, timer-backed blocking, or init-owned service truth | revisit trigger: reopen when the next active proof needs syscall entry, user-visible send-or-receive semantics, handle transfer across the syscall boundary, or another kernel-owned boundary that cannot be closed inside the landed endpoint-and-handle-core slice | reference: /Users/ro/dev/c_modern/docs/plan/phase98_kernel_endpoint_and_handle_core.txt
- 2026-04-09 | area: docs | context: Phase 97 address space and first user entry after the completed Phase 96 kernel boot entry and state-skeleton proof | limiter: the repository now had one honest real-kernel boot artifact and a trusted boot-bundled init image descriptor, but it still had no active proof that one process can own one address space, that one bounded image and stack can be mapped into it, or that the kernel can prepare one saved user-entry frame and transfer control into one first task without widening immediately into spawn, syscall truth, or broader init policy | decision: implement Phase 97 as one repository-owned kernel follow-through using one imported address-space helper module, one widened boot-bundled init image descriptor, one init process slot plus init task slot, one explicit saved user-entry frame, and one bounded transfer into the first user task, and stop there rather than admitting spawn, syscall execution, endpoint delivery, or service bring-up | tradeoff: the repository now has one honest address-space and first-user-entry anchor, but it still does not claim a general loader, real syscalls, endpoint delivery, timer-backed scheduling, or a running init-owned service set | revisit trigger: reopen when the next active proof needs explicit endpoint and handle-table state, syscall entry, or another kernel-owned boundary that cannot be closed inside the landed first-user-entry slice | reference: /Users/ro/dev/c_modern/docs/plan/phase97_address_space_and_first_user_entry.txt
- 2026-04-09 | area: docs | context: Phase 96 kernel boot entry and state skeleton after the completed Phase 95 freestanding `v0.3` slice audit | limiter: the repository now had a full freestanding proof ladder and one admitted `v0.3` slice statement, but it still had no honest real-kernel artifact proving explicit architecture entry and deterministic kernel-owned tables, while Phase 97-scale address-space and user-entry work would have widened past the next smallest owner | decision: implement Phase 96 as one freestanding multi-module tool regression with one imported helper module that owns kernel descriptor, process-slot, task-slot, ready-queue, and boot-record shapes while the root proof module keeps architecture entry, state seeding, boot-marker emission, and stop-before-user-space policy explicit, and stop there rather than admitting first user-space entry, syscall truth, IPC, or scheduler policy | tradeoff: the repository now has one honest real-kernel boot artifact and deterministic state-skeleton proof, but it still does not claim address-space setup, user-space handoff, or broader kernel runtime behavior | revisit trigger: reopen when the next active proof needs bounded address-space construction, first user entry, or another kernel-owned boundary that cannot be closed inside the landed boot-state skeleton | reference: /Users/ro/dev/c_modern/docs/plan/phase96_kernel_boot_entry_and_state_skeleton.txt
- 2026-04-09 | area: docs | context: Phase 93 timer wake proof after the completed Phase 81 through Phase 92 freestanding wave | limiter: the repository now had real freestanding boot, kernel mechanism, static-data, external build-integration, first user-space handshake, explicit capability-transfer, helper-boundary, and user-space lifecycle proofs, but it still had no evidence that one real event source beyond pure synchronous setup could stay explicit without widening into a public timer API, general interrupt framework, or scheduler runtime | decision: implement Phase 93 as one freestanding multi-module tool regression with one imported timer-helper module that owns timer-capability, timer-device, wake-endpoint, and wake-observation records while the root proof module keeps timer arming, blocked-wait policy, and wake delivery explicit, and stop there rather than admitting a public timer surface, interrupt controller API, or broader scheduler framework | tradeoff: the repository now has one honest narrow event-source anchor, but it still does not claim general interrupt handling, timer queues, preemption, or first-system integration | revisit trigger: reopen when a later proof needs multiple timers, periodic delivery, richer scheduler policy, or another event-source boundary not covered by one deterministic timer-backed wake proof | reference: /Users/ro/dev/c_modern/docs/plan/phase93_timer_wake_proof.txt
- 2026-04-09 | area: docs | context: Phase 92 user-space lifecycle follow-through after the completed Phase 81 through Phase 91 freestanding wave | limiter: the repository now had real freestanding boot, kernel mechanism, static-data, external build-integration, first user-space handshake, explicit capability-transfer, and first helper-boundary proofs, but it still had no evidence that explicit launch authority and explicit exit observation could stay in ordinary user code without widening into a public spawn API, loader framework, or broader lifecycle runtime | decision: implement Phase 92 as one freestanding multi-module tool regression with one imported lifecycle helper module that owns launch-capability, wait-handle, child-state, and exit-observation records while the root proof module keeps launch-authority consumption and parent observation policy explicit, and stop there rather than admitting a public spawn or loader layer | tradeoff: the repository now has one honest explicit user-space lifecycle anchor, but it still does not claim a public spawn API, loader implementation, restart policy, or full process-management support | revisit trigger: reopen when a later proof needs multi-child orchestration, kill semantics, loader behavior beyond one launchable capability, or another lifecycle boundary not covered by one child launch-and-observation proof | reference: /Users/ro/dev/c_modern/docs/plan/phase92_user_space_lifecycle_follow_through.txt
- 2026-04-09 | area: docs | context: Phase 91 early user-space helper-boundary audit after the completed Phase 81 through Phase 90 freestanding wave | limiter: the repository now had real freestanding boot, kernel mechanism, static-data, external build-integration, first user-space handshake, and explicit capability-transfer proofs, but the first repeated transfer and request scaffolding was still duplicated in proof programs and there was no evidence yet for whether one narrow helper module could own that duplication without widening into a public `sys` wrapper or capability-runtime layer | decision: implement Phase 91 as one freestanding multi-module tool regression with one imported helper module that owns transfer-message construction, received-handle installation, request construction, and acknowledgment validation while the root proof module keeps destructive sender-side transfer and service-owned policy explicit, and stop there rather than admitting a public wrapper layer or broader helper framework | tradeoff: the repository now has one honest ordinary helper-boundary anchor, but it still does not claim a public Canopus std wrapper, general helper-library policy, process spawning, or full user-space lifecycle support | revisit trigger: reopen when a later proof needs a public `sys` wrapper, multiple helper modules, process spawn, richer service startup, or another policy boundary not covered by one imported helper-module proof | reference: /Users/ro/dev/c_modern/docs/plan/phase91_early_user_space_helper_boundary_audit.txt
- 2026-04-09 | area: docs | context: Phase 90 capability-handle transfer proof after the completed Phase 81 through Phase 89 freestanding wave | limiter: the repository now had real freestanding boot, kernel mechanism, static-data, external build-integration, and first user-space handshake proofs, but it still had no evidence that explicit authority could move between user-space components without ambient access, hidden capability-runtime helpers, or a widened `sys` boundary | decision: implement Phase 90 as one freestanding tool regression that models init-owned authority, one explicit transfer message carrying one log-service endpoint capability and one receiver-visible slot id, sender-side authority removal on successful transfer, and one bounded receiver-issued acknowledged request through ordinary structs, enums, arrays, and functions, and stop there rather than widening into capability tables, service registries, spawning, or broader lifecycle work | tradeoff: the repository now has one honest explicit-authority-transfer anchor, but it still does not claim general capability management, ambient authority shortcuts, wait-handle transfer, or full user-space bring-up | revisit trigger: reopen when a later proof needs multi-handle transfer, process spawn, wait-handle transfer, helper-library consolidation, or another policy boundary not covered by one transferred endpoint-capability proof | reference: /Users/ro/dev/c_modern/docs/plan/phase90_capability_handle_transfer_proof.txt
- 2026-04-09 | area: docs | context: Phase 89 init-to-log-service handshake after the completed Phase 81 through Phase 88 freestanding wave | limiter: the repository now had real freestanding boot, kernel mechanism, static-data, and external build-integration proofs, but it still had no evidence that the first small user-space policy path could be expressed honestly without a new `sys` surface, service framework, or capability-runtime helper family | decision: implement Phase 89 as one freestanding tool regression that models kernel boot state, one init context, one explicit log-service endpoint capability, and one bounded request-and-acknowledgment flow through ordinary structs, enums, arrays, and functions, and stop there rather than widening into service registries, namespace policy, capability transfer, or broader user-space lifecycle work | tradeoff: the repository now has one honest user-space policy anchor, but it still does not claim general service management, ambient authority shortcuts, capability-helper libraries, or full init orchestration | revisit trigger: reopen when a later proof needs explicit capability transfer, multi-service startup, user-space lifecycle follow-through, or another policy boundary not covered by one kernel-issued log-service handshake | reference: /Users/ro/dev/c_modern/docs/plan/phase89_init_to_log_service_handshake.txt
- 2026-04-09 | area: docs | context: Phase 88 kernel build integration audit after the completed Phase 81 through Phase 87 freestanding wave | limiter: the repository now had explicit freestanding target identity, manifest-owned link inputs, and kernel static-data truth, but it still had no evidence that the emitted entry object and generated freestanding runtime object could be reused by an external kernel build step without hidden driver behavior | decision: implement Phase 88 as one freestanding tool regression that builds a target through `mc`, then manually re-links the emitted module object and generated runtime object with a target-owned support object through the host Clang toolchain, and stop there rather than widening into linker scripts, image packaging, or a general external build contract | tradeoff: the repository now has one honest external composition proof, but it still does not claim kernel-image assembly, broader linker control, or a broad non-hosted build workflow guarantee | revisit trigger: reopen when a later proof needs linker scripts, multi-object image composition rules, or another external build boundary not covered by one emitted-object relink audit | reference: /Users/ro/dev/c_modern/docs/plan/phase88_kernel_build_integration_audit.txt
- 2026-04-09 | area: docs | context: Phase 87 kernel static-data proof after the completed Phase 81 through Phase 86 freestanding wave | limiter: the earlier freestanding kernel proofs validated queue and lifecycle logic, but the repository still had no evidence for whether kernel-owned aggregate constants, zero-initialized mutable globals, and explicit global-address writes survived the freestanding artifact path honestly rather than only as local proof state | decision: implement Phase 87 as one freestanding tool regression that models kernel-owned task templates as aggregate const globals, active kernel state as top-level mutable globals, and one pointer-based wait-status write through explicit global-address lowering, and stop there rather than widening into linker scripts, image packaging, or broader kernel build integration | tradeoff: the repository now has one concrete static-data and artifact anchor, but it still does not claim binary-image layout policy, broader linker control, or full kernel build integration | revisit trigger: reopen when a later proof needs linker-script control, image composition rules, or another static-data shape that cannot be expressed honestly with the current core language and existing freestanding path | reference: /Users/ro/dev/c_modern/docs/plan/phase87_kernel_static_data_and_artifact_follow_through.txt
- 2026-04-09 | area: docs | context: Phase 86 task lifecycle proof after the completed Phase 81 through Phase 85 freestanding wave | limiter: the Canopus proof ladder now needed the first explicit task-lifecycle check, but the repository still had no evidence for whether one minimal create, ready, exit, and wait path required a new `sys` layer, task-handle helpers, scheduler-visible policy, or reopened compiler work | decision: implement Phase 86 as one freestanding tool regression that models one boot task and one child task with an ordinary task-state enum, ordinary kernel-owned task slots, and explicit create, ready, exit, and wait transitions, and stop there rather than widening into syscall wrappers, blocking queues, scheduler policy, or broader process management | tradeoff: the repository now has one concrete task-lifecycle anchor, but it still does not claim full scheduler design, capability transfer, or user-space service bring-up | revisit trigger: reopen when a later proof needs kernel-owned static data follow-through, blocking wait queues, task-handle transfer, or another lifecycle boundary that cannot be expressed honestly with the current core language and existing freestanding path | reference: /Users/ro/dev/c_modern/docs/plan/phase86_task_lifecycle_kernel_proof.txt
- 2026-04-09 | area: docs | context: Phase 85 kernel endpoint-queue proof after the completed Phase 81 through Phase 84 freestanding wave | limiter: the Canopus proof ladder now needed the first kernel-mechanism representation check, but the repository still had no evidence for whether one bounded endpoint queue required a new `sys` layer, a public IPC helper taxonomy, or reopened compiler work | decision: implement Phase 85 as one freestanding tool regression that models a bounded endpoint queue and message buffer with ordinary structs, enums, fixed arrays, and pointer-based mutation, and stop there rather than widening into syscall wrappers, capability helpers, scheduler policy, or broader IPC design | tradeoff: the repository now has one concrete kernel-representation anchor, but it still does not claim full IPC, capability transfer, or user-space service bring-up | revisit trigger: reopen when a later proof needs task lifecycle, capability transfer, queue blocking semantics, or another kernel or user-space boundary that cannot be expressed honestly with the current core language and existing freestanding path | reference: /Users/ro/dev/c_modern/docs/plan/phase85_kernel_endpoint_queue_smoke.txt
- 2026-04-08 | area: docs | context: Phase 84 freestanding support-statement audit after the completed Phase 81 through Phase 83 freestanding proof wave | limiter: the repository now had real freestanding boot-marker, manifest-hardening, and narrow `hal` console proofs, but public support wording still treated non-hosted work as blanket-unsupported and no repo-owned audit guarded a separate freestanding statement from either silence or overclaim | decision: publish one repository-bounded freestanding support statement separate from the hosted `v0.2` release note, refresh README and the hosted note so the split is explicit, and add one focused `mc_freestanding_support_audit` CTest plus `make freestanding-support-audit` to keep that wording honest | tradeoff: the repository now carries another maintenance-owned documentation audit, but the first freestanding proof slice is explicit and reviewable without pretending it is already a broad non-hosted support claim | revisit trigger: reopen whenever a later phase changes the admitted freestanding proof slice, manifest boundary, `hal` boundary, or workflow claim enough that the freestanding note, hosted note, README, or focused audit no longer match | reference: /Users/ro/dev/c_modern/docs/plan/phase84_freestanding_support_statement_and_tooling_audit.txt
- 2026-04-14 | area: tooling | context: post-Phase-163 reset-lane cleanup after the active kernel moved onto a direct repo-owned `kernel/build.toml` workflow path | limiter: the remaining `mc_tool_freestanding_tests` harness, `tests/tool/freestanding/**`, `kernel_old/`, and the focused freestanding support audit still advertised a live legacy proof lane even though active kernel work now lands on the hosted reset-lane tree and the old proofs were no longer the repository-owned validation surface | decision: retire the freestanding legacy suite from the live build, selector, and README contracts; archive the retired harness plus `kernel_old/` under `archive/legacy_freestanding/`; and route active kernel validation through `mc_tool_workflow_unit` instead of keeping partial freestanding ownership live | tradeoff: the repository keeps the old proof material for historical reference, but it stops promising that those archived surfaces are maintained or required for ordinary local validation | revisit trigger: reopen only if a future phase needs a new live freestanding validation lane with current owners rather than the archived proof harness | reference: /Users/ro/dev/c_modern/docs/plan/active/phase202_legacy_archive_retirement_and_reset_lane_maintenance_refresh.txt
- 2026-04-08 | area: tooling | context: Phase 82 freestanding artifact and entry hardening after the initial Phase 81 freestanding and narrow `hal` proof | limiter: the repository needed a more honest freestanding build-model contract, but the bootstrap backend still only supports the frozen bootstrap target family and does not yet have a justified design for linker scripts, arbitrary linker flags, or broader cross-target toolchain policy | decision: require explicit `target` identity on freestanding targets, add manifest-owned `[targets.<name>.link] inputs = [...]` for explicit extra prebuilt object or archive inputs on executable targets, and stop there rather than widening into a general linker-control surface | tradeoff: freestanding artifacts are now more inspectable and less dependent on hidden support objects, but broader target-owned linker policy and non-bootstrap target admission remain deferred | revisit trigger: reopen when a later Canopus proof needs linker scripts, broader target-toolchain settings, or packaging behavior beyond explicit prebuilt object or archive inputs | reference: /Users/ro/dev/c_modern/docs/plan/phase82_freestanding_artifact_and_entry_hardening.txt
- 2026-04-08 | area: docs | context: post-`v0.2` execution planning after defining the Canopus-driven `v0.3` direction, consumer matrix, and proof ladder | limiter: the repository now had a strategic answer for what `v0.3` should mean, but it still lacked one active next phase, one implementation checklist for the first freestanding and `hal` proofs, and one bounded target-model shape for kernel, init, and early service artifacts | decision: define Phase 81 as the first active Canopus-driven execution wave around freestanding boot-marker truth plus narrow `hal` admission, add a focused implementation checklist for those first two proofs, and freeze a minimal explicit target model with kernel, init, and early-service artifact classes instead of broadening immediately into general cross-compilation, packaging, or IPC framework work | tradeoff: the repository now has a concrete execution lane for the next wave, but it still deliberately stops short of claiming admitted freestanding support, broad target coverage, or larger Canopus subsystem completion before the first proof pair lands | revisit trigger: reopen when Phase 81 implementation exposes a concrete parser, sema, MIR, backend, or build-model blocker that the current plan cannot absorb, or when the first proof pair lands and the next Canopus proof owner must be chosen | reference: /Users/ro/dev/c_modern/docs/plan/phase81_freestanding_and_hal_bootstrap_execution.txt
- 2026-04-08 | area: docs | context: post-`v0.2` planning after the completed hosted release-hardening wave and the emergence of Canopus Lite as the next concrete consumer | limiter: the repository had an explicit semantic plateau for the admitted hosted `v0.2` core but no equally explicit answer for whether a future `v0.3` should mean more core-language widening or a different kind of tranche altogether, while the upcoming OS effort creates real pressure for freestanding startup and `hal` without yet justifying hidden runtime policy or a broad language redesign | decision: frame the next plausible wave as a Canopus-driven `v0.3` centered on freestanding execution truth, narrow `hal` admission, honest non-hosted artifact production, and a small bring-up proof ladder, while keeping broad `os` growth, language-level IPC abstractions, scheduler-visible features, and capability-runtime machinery deferred unless the proof set exposes a concrete owner gap | tradeoff: the repository now has a clearer next-step direction, but it still deliberately avoids treating Canopus as permission to widen the language surface speculatively or to mark freestanding and `hal` as already admitted | revisit trigger: reopen when the first Canopus proof programs expose a concrete parser, sema, MIR, backend, or target owner gap that the current core language cannot carry, or when a later planning decision chooses a different next consumer altogether | reference: /Users/ro/dev/c_modern/docs/plan/admin/v0_3_direction_canopus.txt
- 2026-04-08 | area: tooling | context: Phase 80 release-snapshot preparation after the completed Phase 79 `v0.2` gate defer | limiter: Phase 79 made maintainer-owned release execution explicit, but the repository still lacked one concrete path that reran the final gate from committed state, captured the exact reviewed commit snapshot, and printed the exact tag command without automatically mutating git refs | decision: add `tools/release/prepare_v0_2_snapshot.sh`, require a clean committed worktree on the real maintainer path, validate the helper through `mc_release_snapshot_prep`, and stop at emitting the exact snapshot and `git tag -a v0.2 ...` command rather than creating the tag automatically | tradeoff: release execution is now concrete and repeatable, but the repository still deliberately leaves final tag creation as an explicit maintainer action rather than as a repo-owned mutation step | revisit trigger: reopen only if the repository later chooses to let automation create release tags directly or if the admitted release-hardening boundary changes enough that the preparation helper or audit must be redesigned | reference: /Users/ro/dev/c_modern/docs/plan/phase80_release_snapshot_preparation.txt
- 2026-04-08 | area: docs | context: Phase 79 final `v0.2` gate after the completed Phase 78 release-readiness re-audit | limiter: the repository now has a technically acceptable narrow source-only Darwin arm64 hosted release claim, but the actual act of creating a release tag still requires choosing a final reviewed commit snapshot and applying the tag from committed repository state rather than from a live working tree | decision: do not create the `v0.2` tag inside the repository worktree at this phase, write down maintainer-owned release execution as the one remaining plateau blocking that tag, and add one aggregate `mc_v0_2_gate` CTest plus `make v0_2_gate` so the final documented outcome is repo-owned and repeatable | tradeoff: the repository ends this wave with an explicit defer instead of a tag, but the remaining blocker is now operational and explicit rather than hidden inside the supported technical slice | revisit trigger: reopen when a maintainer is ready to cut the final reviewed commit snapshot and apply the `v0.2` tag from committed state, or when the admitted hosted slice changes enough that the final gate outcome must be re-audited | reference: /Users/ro/dev/c_modern/docs/plan/phase79_v0_2_gate.txt
- 2026-04-08 | area: docs | context: Phase 78 release-readiness re-audit after the completed Phase 77 public-cut smoke refresh | limiter: the repository now had a frozen support statement, a narrow first-use smoke, a supported-slice doc audit, and a broader public-cut smoke, but it still lacked one explicit answer on whether the remaining deferred fronts were small enough to tolerate in a source-only `v0.2` cut or large enough to keep the release deferred | decision: treat the current Darwin arm64 hosted slice as release-ready enough for a narrow source-only `v0.2` claim, add one aggregate `mc_release_readiness_audit` CTest plus `make release-readiness-audit`, and keep Phase 79 as the final policy gate for tagging or writing down one last plateau | tradeoff: the repository still stops short of a broad portability or distribution claim, but the release discussion is now grounded in one repeatable audit path instead of in implied confidence from separate smoke notes | revisit trigger: reopen whenever the admitted hosted slice, supported claims, or aggregate audit path changes enough that the release-readiness conclusion no longer follows from the repo-owned checks | reference: /Users/ro/dev/c_modern/docs/plan/phase78_release_readiness_reaudit.txt
- 2026-04-08 | area: docs | context: Phase 77 public-cut smoke refresh after the completed Phase 76 support-statement audit | limiter: the repository already documented a broader public-cut smoke set, but that list still mixed stale command inventory with newer proof claims, including selected-target reuse without an explicit report-target command and an implied first-use replay that actually belonged to the separate Phase 75 path | decision: add one focused `mc_public_cut_smoke` CTest plus `make public-cut-smoke`, refresh the README and hosted release note so the public-cut command list matches the current admitted proof set exactly, and keep the narrower first-use path documented separately | tradeoff: the repository now carries another maintenance-owned smoke entrypoint, but the broad hosted claim is backed by one repeatable command path instead of by documentation alone | revisit trigger: reopen whenever the admitted hosted example set, target-switching proof, or public-cut claim changes enough that the smoke command list or repo-owned smoke script no longer matches | reference: /Users/ro/dev/c_modern/docs/plan/phase77_public_cut_smoke_refresh.txt
- 2026-04-08 | area: docs | context: Phase 76 supported-slice audit after the completed Phase 75 first-use automation refresh | limiter: the repository already had a published hosted support note plus aligned README guidance, but later workflow and helper-slice changes had left stale false negatives and historical blur in the support wording, including treating `condvar_broadcast(...)` as unsupported and failing to state the admitted narrow `time` plus `path` helper boundary | decision: re-audit the published support statement against the actual admitted hosted slice, correct the README and hosted release note, and add one focused doc-audit CTest so future drift is caught at the same repository-owned boundary | tradeoff: the repository now carries a small amount of maintenance-sensitive documentation validation, but it avoids letting public support claims drift away from the actual admitted surface between broader release-hardening waves | revisit trigger: reopen whenever a later phase changes the admitted hosted slice, helper-family boundary, or supported workflow guarantee enough that the README, release note, first-use smoke, or doc audit no longer match | reference: /Users/ro/dev/c_modern/docs/plan/phase76_supported_slice_audit.txt
- 2026-04-07 | area: stdlib | context: Phase 73 split stderr follow-through after the completed merged-diagnostics subprocess wave | limiter: the admitted subprocess relay family could already surface child diagnostics through a merged-output helper, but that still conflated stdout and stderr; the current admitted pressure justified one bounded separate-stderr helper, not a broader file-descriptor routing framework or general process redesign | decision: reuse the landed stderr-capable lower-level owner in `stdlib/os.mc`, add additive `os.spawn_pipe_argv_split_output(...)` for the admitted one-stdin-pipe plus one-stdout-pipe plus one-stderr-pipe family, and prove it through one focused stdlib smoke plus one ordinary test in the admitted `line_filter_relay` project | tradeoff: the repository now supports bounded separate stdout and stderr capture on the admitted relay family, but it still does not claim arbitrary fd routing, multi-stage pipelines, environment APIs, cwd mutation, signals, or broader process lifecycle policy | revisit trigger: reopen only when another admitted consumer needs behavior beyond the landed split relay family, such as more than one output pipe per stream group, arbitrary descriptor routing, or broader process-management semantics | reference: /Users/ro/dev/c_modern/docs/plan/phase73_process_split_stderr_follow_through.txt
- 2026-04-07 | area: stdlib | context: Phase 72 process diagnostics capture follow-through after the completed Phase 71 relay-helper wave | limiter: the admitted subprocess slice could already relay stdin and stdout honestly through one bounded pipe family, but a failing child still exposed only status unless callers reopened broader stderr-routing policy; the current admitted consumer pressure did not justify separate stderr pipes, environment policy, or more general process control | decision: add hosted runtime support for explicit child stderr redirection, keep that lower-level wiring inside `stdlib/os.mc`, expose additive `os.spawn_pipe_argv_merged_output(...)` as the public owner for the admitted merged-output relay shape, and prove it through one focused stdlib smoke plus one ordinary test in the admitted `line_filter_relay` project | tradeoff: the repository now makes child diagnostics observable on the admitted pipe-relay family, but it still does not claim separate stderr routing, multi-pipe child topologies, environment APIs, cwd mutation, signals, or a broader subprocess framework | revisit trigger: reopen only when another admitted consumer needs stderr kept separate from stdout, more than one output pipe, or another process-policy front beyond the landed merged-output relay shape | reference: /Users/ro/dev/c_modern/docs/plan/phase72_process_diagnostics_capture_follow_through.txt
- 2026-04-07 | area: stdlib | context: Phase 71 process pipe-helper follow-through after the completed Phase 70 sync and mem gate | limiter: the admitted real subprocess proof and the focused stdlib subprocess smoke both relied on the same one-input-pipe plus one-output-pipe `spawn_argv(...)` call shape, but the current public API still exposed the underdocumented `close_file0` and `close_file1` pairing directly at those call sites; a broader process redesign would have widened beyond the admitted consumer pressure | decision: keep `spawn_argv(...)` as the lower-level owner, add additive `os.spawn_pipe_argv(...)` for the admitted relay shape, and move the existing subprocess proofs onto that helper instead of widening broader process policy | tradeoff: the repository still keeps the raw close-slot form for callers that need custom file wiring, and it still does not solve multi-pipe process plumbing, environment policy, cwd mutation, stderr routing, signals, or `wait(...)` lifetime redesign, but the admitted relay shape is now owned at the stdlib boundary instead of repeated at each proof site | revisit trigger: reopen only when another admitted consumer needs broader child-handle lifecycle semantics, more than one pipe pair, or another process-policy front beyond the landed argv plus pipe-relay slice | reference: /Users/ro/dev/c_modern/docs/plan/phase71_process_pipe_helper_follow_through.txt
- 2026-04-07 | area: stdlib | context: Phase 70 sync and mem follow-on gate after the completed Phase 69 testing-surface hardening wave | limiter: the admitted real sync projects still stop at thread spawn or join, mutex, condvar wait or signal, and narrow atomic publication, while the admitted real mem projects still stop at explicit `Buffer<u8>` ownership, `slice_from_buffer<u8>(...)`, and arena-backed local allocation; low-level MIR and backend support for atomic read-modify-write exists, but no admitted public consumer yet requires it | decision: implement Phase 70 as an explicit consumer gate, keep public sync and mem widening deferred, correct support notes so they reflect that `condvar_broadcast(...)` is already implemented, and route the next step toward explicit deferral unless a new admitted consumer appears | tradeoff: the repository leaves compare-exchange, exchange, fetch-add, richer allocator helpers, and broader arena policy out of the public surface for now, but it avoids widening stdlib APIs ahead of real project pressure | revisit trigger: reopen sync or mem only when an admitted real project needs public atomic read-modify-write, broader coordination, or richer buffer or arena semantics beyond the landed slice | reference: /Users/ro/dev/c_modern/docs/plan/phase70_sync_and_mem_follow_on_gate.txt
- 2026-04-07 | area: stdlib | context: Phase 69 testing-surface hardening after the completed filesystem and argv subprocess follow-through waves | limiter: admitted ordinary project tests already had a usable minimal `testing` surface, but they still repeated nil-pointer checks and explicit `errors.Error` success or kind/code assertions often enough that the boilerplate had become stable friction without yet justifying fixture frameworks or compiler magic, while the current generic call syntax could not express an instantiated generic pointee such as `Buffer<u8>` as the explicit type argument to one generic pointer helper | decision: keep the existing ordinary `*i32` result convention, add additive `testing.expect_non_nil<T>(...)`, `testing.expect_buffer_non_nil<T>(...)`, `testing.expect_nil<T>(...)`, `testing.expect_ok(...)`, and `testing.expect_err(...)` helpers, and prove them through representative admitted real project tests instead of redesigning the test model | tradeoff: the repository still does not provide fixtures, output capture, timeout helpers, or richer result objects, but ordinary pointer, buffer-pointer, and classified-error checks are now more uniform and diagnostic without widening the testing model into a framework | revisit trigger: reopen only when a repeated admitted project pattern beyond pointer or explicit error expectations is common enough to justify another small ordinary-library helper slice, or when the language can express richer generic pointee shapes directly enough to collapse the split pointer and buffer-pointer helpers | reference: /Users/ro/dev/c_modern/docs/plan/phase69_testing_surface_hardening.txt
- 2026-04-07 | area: stdlib | context: Phase 66 adjacent stdlib and runtime consumer matrix after the Phase 65 plateau decision | limiter: several adjacent fronts remain real, but they no longer have equal weight: the repository already landed the narrow `time` slice, broader subprocess policy still has only one admitted real consumer, and sync or mem follow-on still lacks a stronger admitted proof, while file-oriented utilities broadly rely on `fs.read_all(...)` and `fs.list_dir(...)` even though those calls still collapse failures to nil | decision: route the next adjacent wave to filesystem and cross-module error-contract follow-through, keep testing helper hardening as the clearest later candidate, and keep broader `os`, `sync`, `mem`, and further `time` widening deferred | tradeoff: the repository chooses a more user-facing contract mismatch before helper-surface polish, so some ordinary-test boilerplate and subprocess rough edges remain for later waves | revisit trigger: reopen process follow-through only when a second admitted subprocess consumer or stronger child-handle pressure appears, reopen sync or mem only when a stronger admitted consumer lands, and reopen `time` widening only when the already admitted narrow slice stops being sufficient | reference: /Users/ro/dev/c_modern/docs/plan/phase66_adjacent_stdlib_and_runtime_consumer_matrix.txt
- 2026-04-07 | area: stdlib | context: Phase 67 filesystem and cross-module error-contract follow-through after the Phase 66 matrix | limiter: `fs.read_all(...)` and `fs.list_dir(...)` were already used broadly enough that replacing them in place would have caused repository-wide churn, but leaving them nil-only would keep the most visible filesystem error-contract mismatch active | decision: add `fs.read_all_err(...)` and `fs.list_dir_err(...)` as additive companions, route the coarse wrappers through the new runtime-backed error path, and migrate one admitted real consumer to the widened contract | tradeoff: the repository now carries both coarse and explicit-error variants for the same operations, but it gains actionable filesystem diagnostics without a forced compatibility break | revisit trigger: consider deprecating the coarse wrappers only if later admitted consumers show that maintaining both variants is more harmful than a coordinated source migration | reference: /Users/ro/dev/c_modern/docs/plan/phase67_filesystem_and_error_contract_follow_through.txt
- 2026-04-07 | area: stdlib | context: Phase 68 process or deferred-time follow-through after the completed Phase 67 filesystem work | limiter: the admitted `time` slice still lacked a new consumer for wall-clock, sleep, or broader deadline work, while the subprocess slice still routed its main public API through a fixed two-argument helper that could not model an honest argv vector | decision: choose process follow-through over reopening `time`, add `os.spawn_argv(...)` as the honest subprocess owner, preserve the fixed `spawn(...)` entry as a compatibility wrapper, and move the admitted `line_filter_relay` proof onto the argv-shaped path | tradeoff: the repository now carries both a compatibility helper and the real argv-slice API, but it avoids broader process-policy widening and keeps time deferred until a real consumer appears | revisit trigger: reopen broader process policy only when another admitted consumer needs environment, cwd, stderr, signals, or close-policy shaping, and reopen `time` only when the landed monotonic slice stops being sufficient | reference: /Users/ro/dev/c_modern/docs/plan/phase68_process_or_deferred_time_follow_through.txt
- 2026-04-07 | area: docs | context: Phase 65 plateau decision after Phase 60 through Phase 64 closed the remaining known admitted core-language ownership gaps on the supported slice | limiter: the repository still has many explicit deferred fronts, but they now belong to support breadth, adjacent family admission, portability, workflow, or artifact policy rather than to a known parser/sema/MIR gap inside the admitted v0.2 core, and the published release-hardening note needed that grounding stated explicitly | decision: treat the admitted v0.2 core as semantically complete enough for repository-bounded release hardening and route the next planning wave toward hardening or outward growth instead of another unspecified core-language closure pass | tradeoff: the repository now speaks more clearly about its current core plateau, but this is not a claim of full language completion, portability, or the end of compiler work | revisit trigger: reopen only when a fresh admitted parser, sema, MIR, or backend distortion is found inside the already admitted core slice rather than in a still-deferred extension family | reference: /Users/ro/dev/c_modern/docs/plan/phase65_language_surface_plateau_decision.txt
- 2026-04-07 | area: codegen | context: Phase 58 backend follow-through after Phase 42 had already closed structural aggregate constant truth at sema and `.mci` boundaries | limiter: MIR globals and the executable LLVM path still modeled top-level initializers as rendered source text, which worked for scalar literals but lost field-level structure and mis-modeled per-name globals before real executable emission | decision: preserve sema-owned per-name `ConstValue` facts on MIR globals and render admitted structural LLVM global initializers recursively from typed semantic values during executable emission instead of reconstructing them from source text | tradeoff: the admitted executable slice now supports top-level structural aggregate const globals for the real `issue_rollup` consumer, but the repository still does not claim a universal solution for every string-backed or otherwise unproved constant family | revisit trigger: reopen only when another admitted executable-build consumer needs constant global behavior beyond the landed scalar-plus-structural path | reference: /Users/ro/dev/c_modern/docs/plan/phase58_structural_aggregate_const_backend_emission.txt
- 2026-04-07 | area: stdlib | context: Phase 35 narrow `time` admission and the now-implemented `path` helper slice after the stdlib module model cleanup made the deferred buckets honest enough to land | limiter: the repository had no admitted wall-clock, sleep, or richer pathname consumer, so broad `time` or full POSIX path semantics would have widened too quickly | decision: admit a tiny `time.Duration` plus monotonic-nanosecond hook and a pure slash-separated `path` helper slice (`join`, `basename`, `dirname`) instead of a broader calendar, timezone, or normalization API | tradeoff: the repository now has useful bootstrap time and path helpers, but it still avoids sleep, wall-clock, and full-path canonicalization semantics | revisit trigger: reopen only when an admitted consumer needs sleep, wall-clock, timezone, or richer path normalization beyond the current slash-based helpers | reference: /Users/ro/dev/c_modern/docs/plan/phase35_duration_and_monotonic_clock_admission.txt
- 2026-04-06 | area: mir | context: Phase 49 imported generic call closure after Phase 48 identified the admitted `sync.thread_spawn<T>` rewrite as the strongest remaining owned distortion on the supported slice | limiter: bootstrap sema already instantiated imported generic call signatures, but MIR validation and LLVM emission still assumed generic callees had to disappear before the executable boundary, so the admitted sync path survived only through a `sync.thread_spawn_raw` rewrite | decision: remove the MIR-only rewrite, validate generic direct-call metadata against the callee's generic signature, and emit only the erased-ABI generic declarations or definitions whose signatures and lowered bodies remain representable without monomorphization | tradeoff: the admitted sync surface now works honestly as a direct imported generic call, but the repository still does not claim general generic-function completion or executable support for signatures whose ABI depends on unresolved type parameters | revisit trigger: reopen broader generic-function work only when another admitted consumer needs support beyond the erased-ABI family closed here | reference: /Users/ro/dev/c_modern/docs/plan/phase49_imported_generic_call_closure.txt
- 2026-04-06 | area: mir | context: Phase 48 post-Phase-47 frontier audit after the repository completed the explicit error-treatment wave and re-audited the remaining deferred fronts against the current admitted slice | limiter: broader linker or artifact work, `time`, and richer error-object follow-through still lack stronger admitted pressure, while executable aggregate const global emission remains a real backend follow-up but not yet a required proof surface for the supported slice | decision: route the next active phase to compiler-owned imported generic procedure-call closure because the admitted `sync.thread_spawn<T>` surface still depends on a live `sync.thread_spawn_raw` MIR rewrite inside the supported slice | tradeoff: the repository reopens imported generic call work before a second admitted consumer has landed, but keeps the follow-through narrowly focused on removing the existing admitted rewrite rather than broad generic-function completion | revisit trigger: widen beyond the sync-shaped admitted call family only when a second imported generic procedure-typed consumer or another concrete admitted distortion appears | reference: /Users/ro/dev/c_modern/docs/plan/phase48_post_phase47_frontier_audit.txt
- 2026-04-06 | area: stdlib | context: Phase 47 full error treatment after the earlier phase-1 encoded `Error` redesign proved the representation but still left naming and classification drift across admitted examples and narrow stdlib helpers | limiter: the repository wanted the explicit error model to feel finished enough for real programs without widening into message-owned errors, wrapped causes, or hidden propagation | decision: complete the small source-level error vocabulary, standardize named module-local error-code constants where semantics were stable, classify shared helpers such as `net.parse_port(...)` through subsystem-specific constructors, and prove the resulting contract through both direct-source and project-level regressions | tradeoff: the admitted error surface is now more consistent and self-describing, but richer message payloads, wrapped causes, and any explicit propagation helper remain deferred | revisit trigger: reopen only when another admitted real program still needs more actionable failure detail than kind or code classification provides, or when explicit propagation pressure is strong enough to justify a separate narrow phase | reference: /Users/ro/dev/c_modern/docs/plan/phase47_full_error_treatment.txt
- 2026-04-06 | area: stdlib | context: error-handling ergonomics follow-up after the first admitted subprocess relay and other real programs showed that raw `fail_code(...)` sentinels were too opaque | limiter: a full heap-backed or message-owning error object would widen the hosted ABI and force unresolved ownership policy across runtime, stdlib, and user code in one step | decision: keep `Error` scalar at the ABI boundary but encode it as an opaque kind/code value with stdlib constructors and inspectors, and classify hosted runtime errno failures through that channel instead of exposing bare integers | tradeoff: callers gain stable error kinds and codes immediately, but error messages, wrapped causes, and richer source context remain deferred | revisit trigger: reopen only when another admitted real program still needs more actionable failure detail after kind/code classification or when the repository is ready to own explicit error-object lifetime policy end-to-end | reference: /Users/ro/dev/c_modern/stdlib/errors.mc
- 2026-04-06 | area: stdlib | context: hosted runtime follow-up after the subprocess admission wave surfaced general runtime critique on errno and filesystem error reporting | limiter: `fs.read_all(...)` and `fs.list_dir(...)` are already the admitted nil-or-buffer convenience surface across examples, tests, and docs, and threading `errors.Error` through them now would widen a broadly used API boundary rather than fix a localized runtime bug | decision: fix the hosted runtime's synthetic errno fallback and add EINTR retry loops for blocking host syscalls, but keep the existing `fs` APIs intentionally coarse for now with success-or-failure semantics only | tradeoff: filesystem callers still cannot inspect host-specific failure causes through the stdlib surface, but the repository avoids a broad source and ABI churn that is not yet justified by admitted consumers | revisit trigger: reopen only when a real admitted utility needs actionable filesystem failure classification instead of plain nil-or-buffer success semantics | reference: /Users/ro/dev/c_modern/runtime/hosted/mc_hosted_runtime.c
- 2026-04-05 | area: stdlib | context: Phase 46 real-consumer follow-through on the newly admitted subprocess slice | limiter: the first `os` admission needed one concrete consumer proving that child-process ownership, not thread-plus-pipe transport alone, was the missing boundary | decision: land `examples/real/line_filter_relay/` as a bounded `/usr/bin/tr` relay using explicit argv plus stdin and stdout pipes through ordinary `io.File` handles, and prove it through checked ordinary tests plus project `mc run` and `mc test` workflow coverage | tradeoff: the repository now admits one honest child-process relay consumer, but it still does not claim shells, stderr capture policy, environment mutation, cwd control, or broader command frameworks | revisit trigger: reopen only when another admitted project needs behavior beyond one-child explicit argv relay semantics, such as broader stderr control, environment policy, or more than one pipeline stage | reference: /Users/ro/dev/c_modern/docs/plan/phase46_pipeline_and_stdio_followthrough.txt
- 2026-04-05 | area: stdlib | context: Phase 45 first hosted subprocess or stdio admission after the Phase 44 consumer-matrix gate | limiter: the repository needed an owned child-process boundary for one admitted consumer, but still had no honest basis for shells, env APIs, cwd mutation, signals, or broader process management | decision: admit a minimal hosted `os` slice with `Child`, explicit argv-based `spawn(...)`, blocking `wait(...)`, and stdin/stdout redirection through existing `io.File` handles, and make `io.pipe()` exec-safe so the existing pipe model composes honestly with spawned children | tradeoff: subprocess support is now real on the admitted hosted slice, but it remains intentionally narrow and platform-facing rather than a general process-management family | revisit trigger: reopen only when an admitted consumer needs broader process policy beyond explicit argv spawn plus wait plus bounded stdin/stdout pipe composition | reference: /Users/ro/dev/c_modern/docs/plan/phase45_minimal_subprocess_and_stdio_pipe_admission.txt
- 2026-04-05 | area: stdlib | context: Phase 44 subprocess and stdio consumer-matrix audit after the admitted `io.pipe()` blocking and readiness proof pair | limiter: the repository now proves explicit handle-first communication across both blocking and poller-coupled pipe workflows, but still needed one admitted project that was materially clearer with child-process ownership or stdio redirection rather than thread-plus-pipe transport alone | decision: choose one bounded argv-based line-filter relay as the strongest concrete consumer and authorize Phase 45 around explicit spawn plus wait plus bounded stdin/stdout piping instead of keeping `os` fully deferred or widening straight to general process policy | tradeoff: the repository commits to one narrow subprocess frontier, but still rejects shell command strings, environment policy, cwd control, and broader process management as collateral scope | revisit trigger: reopen the Phase 44 matrix only if the admitted consumer set changes enough that a different owner than narrow `os` subprocess support becomes the stronger next step | reference: /Users/ro/dev/c_modern/docs/plan/phase44_subprocess_stdio_consumer_matrix.txt
- 2026-04-05 | area: stdlib | context: Phase 43 `io.pipe` admission and the first direct handle-first communication proof | limiter: the repository needed one honest portable communication primitive and one real project proof, but it still lacked admitted pressure for non-blocking pipe policy, subprocess piping, half-close semantics, or channel-like library abstractions | decision: admit a minimal `io.Pipe` plus `io.pipe()` surface as ordinary `io.File` endpoints, implement it in the hosted runtime, prove it through the real `examples/real/pipe_handoff/` project, and keep the proof blocking rather than widening into poller-coupled or scheduler-driven semantics in the same phase | tradeoff: the repository now has an honest direct handle-first communication proof, but it still does not claim richer IPC policy, non-blocking pipe semantics, or subprocess integration beyond the bounded blocking handoff slice | revisit trigger: reopen only when an admitted consumer needs pipe readiness coordination, subprocess stdio plumbing, half-close behavior, or another behavior beyond the current blocking thread-plus-pipe proof | reference: /Users/ro/dev/c_modern/docs/plan/phase43_io_pipe_and_handle_first_sync_reframing.txt
- 2026-04-05 | area: stdlib | context: post-Phase-43 follow-through on admitted `io.pipe()` pressure | limiter: the repository now had one direct blocking pipe proof, but it still needed a second real consumer to justify claiming that the existing portable `io.poller_*` surface composes honestly with pipe endpoints while still stopping short of broader non-blocking pipe policy or scheduler semantics | decision: land the real `examples/real/pipe_ready/` project as a bounded thread-plus-pipe readiness proof over `io.poller_wait(...)` and keep the portable surface unchanged instead of widening into half-close rules, non-blocking toggles, or channel-like abstractions | tradeoff: the repository now proves both blocking and readiness-based pipe composition, but still does not claim richer non-blocking pipe policy, subprocess piping, or broader IPC frameworks | revisit trigger: reopen only when an admitted project needs behavior beyond the current blocking and readiness-based pipe proofs, such as half-close semantics, subprocess stdio integration, or other non-blocking policy details | reference: /Users/ro/dev/c_modern/docs/plan/phase43_io_pipe_and_handle_first_sync_reframing.txt
- 2026-04-05 | area: sema | context: Phase 42 follow-through on the structural aggregate constants winner from Phase 40 | limiter: the repository needed to close imported aggregate compile-time fact transport at the sema-plus-`.mci` boundary first, while the LLVM backend still lowers top-level global initializers from scalar literal text and would have mixed a separate owner concern into the same phase | decision: add recursive structural `ConstValue` support in sema, serialize it deterministically through `.mci`, and prove the refreshed `issue_rollup` artifact in project `check` mode plus direct sema and `.mci` round-trip coverage, instead of broadening linker behavior or taking on backend aggregate-global emission in the same wave | tradeoff: imported aggregate compile-time constants now work honestly across interface boundaries, but executable emission of top-level aggregate const globals remains a separate explicit backend follow-up rather than part of this phase | revisit trigger: reopen backend aggregate-const global emission only when an admitted executable-build proof needs it, and keep the broader linker frontier deferred until a real project needs behavior beyond the current hosted `staticlib` plateau | reference: /Users/ro/dev/c_modern/docs/plan/phase42_imported_aggregate_constants_or_broader_linking_followthrough.txt
- 2026-04-05 | area: sema | context: Phase 40 consumer-pressure discovery after the Phase 37 through Phase 39 maintenance plateau | limiter: the repository still had three credible deferred fronts, but only one new admitted pressure artifact was allowed and the first naive `issue_rollup` aggregate-constant build probe mixed the structural-constant question with a separate backend aggregate-global emission failure | decision: refresh the real `examples/real/issue_rollup/` proof in project `check` form inside the tool regression suite, export `rollup_model.DEFAULT_SUMMARY`, and let the dependent executable target declare `const DEFAULT_SUMMARY_COPY: rollup_model.Summary = rollup_model.DEFAULT_SUMMARY` so the phase isolates imported aggregate compile-time pressure at the sema-plus-`.mci` boundary | tradeoff: Phase 40 now chooses the structural aggregate constant frontier as the next honest owner while deliberately leaving the compiler-owned imported generic procedure-call frontier and the broader linker surface deferred because neither won on admitted evidence | revisit trigger: follow through in Phase 42 by designing the narrowest structural aggregate constant artifact shape that makes the refreshed `issue_rollup` consumer honest, while keeping Phase 41 and broader linking deferred until their existing real-project triggers are met | reference: /Users/ro/dev/c_modern/docs/plan/phase40_consumer_pressure_discovery_and_plateau_decision.txt
- 2026-04-05 | area: tooling | context: Phase 30 separate-compilation and cross-target workflow hardening on the admitted hosted project graph | limiter: the repository already had implementation-only and interface-visible rebuild proof on richer project graphs, but the new Phase 29 archive boundary still needed a real selected-target same-build-dir proof showing that one consumer rebuild does not churn its sibling executable until that sibling is explicitly chosen | decision: keep the proof on the real `examples/real/issue_rollup/` project, extend it with a second executable consumer linked to the same admitted static library, and harden the workflow with selected-target rebuild assertions instead of inventing a broader build-graph cache redesign or a synthetic fixture-only graph | tradeoff: the repository now proves deterministic cross-target non-churn and selected-target reuse on one admitted richer project, but broader artifact coordination, install policy, and `sharedlib` remain explicitly deferred | revisit trigger: reopen only when an admitted project needs behavior beyond the current one-manifest selected-target hardening proof, such as external libraries, install-tree policy, or runtime-loaded shared artifacts | reference: /Users/ro/dev/c_modern/docs/plan/phase30_separate_compilation_and_cross_target_workflow_hardening.txt
- 2026-04-07 | area: tooling | context: Phase 74 deterministic rebuild and cross-target reuse refresh after the completed subprocess follow-through wave and the published hosted support note | limiter: the repository already implemented and tested selected-target same-build-dir reuse, but the strongest proof still depended on a cloned synthetic variant of `issue_rollup` while the shipped public manifest exposed only one executable consumer | decision: make `issue-rollup-report` a permanent second executable target on the real `examples/real/issue_rollup/` project and prove no-op selected-target build or run reuse there instead of redesigning the build graph or widening broader linker policy | tradeoff: the support claim is now anchored to the shipped admitted project rather than a synthetic harness, but the repository still intentionally stops short of shared libraries, install coordination, external library links, or broader cross-project artifact reuse | revisit trigger: reopen only when an admitted project needs artifact coordination beyond the current one-manifest static-library and selected-target proof, such as install-tree policy, external libraries, or runtime-loaded shared artifacts | reference: /Users/ro/dev/c_modern/docs/plan/phase74_deterministic_rebuild_and_cross_target_reuse.txt
- 2026-04-08 | area: docs | context: Phase 75 install-and-usage hardening refresh after the completed Phase 74 target-switching proof | limiter: the repository already documented a supported build-from-source and first-use path, but the guidance still depended on humans replaying prose while the repo-owned smoke surface remained fragmented across lower-level checks | decision: encode the documented supported path as one focused `mc_first_use_smoke` CTest plus `make first-use-smoke`, using direct-source `hello` check and real `issue_rollup` run or test workflow instead of widening into the full public-cut smoke matrix | tradeoff: the supported first-use story is now mechanically repeatable, but broader public-cut audit, install-tree policy, packaging, and distribution remain separate explicit release-hardening work | revisit trigger: reopen only when the admitted hosted slice changes enough that the documented first-use path or its narrow smoke stops matching the written support boundary | reference: /Users/ro/dev/c_modern/docs/plan/phase75_install_and_usage_path_hardening.txt
- 2026-04-05 | area: tooling | context: Phase 29 narrow static-library admission on the hosted project workflow | limiter: the grouped package proof from Phase 28 showed a real library-first project shape, but the repository still needed to stop short of shared libraries, install policy, or arbitrary external linker surface | decision: admit one hosted `staticlib` target kind, interpret project-manifest `links` as in-project static-library target dependencies on the admitted slice, and route ordinary `mc test` runners through the same target graph so archive consumption is proved by normal workflow | tradeoff: the repository now emits reusable static archives and links them into executable targets, but `sharedlib`, external system-library links in `build.toml`, and broader linker policy remain explicitly unsupported | revisit trigger: reopen only when an admitted project needs shared-library runtime loading, external library integration, or broader cross-target coordination beyond the current in-project static-library proof | reference: /Users/ro/dev/c_modern/docs/plan/phase29_narrow_static_library_target_admission.txt
- 2026-04-05 | area: tooling | context: Phase 28 grouped package pressure on the admitted hosted project workflow | limiter: a real library-first project needed grouped internal module roots beyond one source root, but the pressure still stopped short of requiring reusable non-executable artifacts or package-management scope | decision: admit grouped multi-root internal package layouts inside the existing executable-plus-checked-test manifest model and keep `staticlib` or `sharedlib` deferred after the proof lands | tradeoff: the repository now proves more honest internal package organization for real hosted projects, but still does not emit reusable library artifacts or broaden the public target-kind surface | revisit trigger: reopen broader target kinds only when a real project needs reusable artifact consumption beyond executable targets or when separate-compilation hardening exposes an executable-only limitation | reference: /Users/ro/dev/c_modern/docs/plan/phase28_package_grouping_and_library_first_project_pressure.txt
- 2026-04-05 | area: sema | context: Phase 27 Lane A follow-through after the landed Phase 26 `@noalias` closure | limiter: builtin generic shells and imported qualified nominal names already survived several boundaries, but user-defined generic nominals with type-parameter-dependent fields still failed by computing raw declaration layout and field or payload types before substituting concrete type arguments | decision: close generic nominal type-argument identity with a sema-first instantiated nominal path, skipping eager raw generic struct layout, substituting concrete type args for alias, field, and variant-payload consumers, and mirroring that substitution through MIR, backend type lowering, and `.mci` round-trip proof | tradeoff: broader imported generic procedure-call lowering remains intentionally deferred, but nominal type arguments are now preserved as owned semantic truth instead of being reconstructed or guessed downstream | revisit trigger: reopen only when a second admitted imported generic procedure-typed call surface appears beyond `sync.thread_spawn<T>` or another consumer exposes a different imported generic ownership gap | reference: /Users/ro/dev/c_modern/docs/plan/phase27_lane_a_pressure_proof_and_reaudit.txt
- 2026-04-05 | area: codegen | context: Phase 26 selected semantic surface widening after the landed Phase 25 sema baseline cleanup | limiter: parser, sema, sema dumps, and `.mci` already preserved parameter-level `@noalias`, but the admitted promise was still dropped at the MIR and executable LLVM boundary, while broader imported generic procedure-call lowering still lacked its required second consumer and tuple or range forms were already materially deeper than parser-only sugar | decision: choose `@noalias` as the Phase 26 family and carry the admitted flag through MIR parameter locals, backend inspectability, and LLVM parameter emission rather than reopening a broader semantic family | tradeoff: generic nominal type-argument identity remains open as the next stronger imported-boundary candidate, but the repository closes one narrower admitted contract end-to-end instead of widening multiple partials at once | revisit trigger: reopen generic nominal identity when an admitted imported surface exposes downstream type-argument reconstruction, and keep broader imported generic procedure-call lowering deferred until the existing second-consumer trigger is satisfied | reference: /Users/ro/dev/c_modern/docs/plan/phase26_selected_semantic_surface_widening.txt
- 2026-04-05 | area: sema | context: Phase 25 compile-time and top-level static-storage closure | limiter: exported and imported global constant facts still use a scalar-only `ConstValue` encoding, while aggregate compile-time values would require a broader structural artifact format and downstream reconstruction policy | decision: close Phase 25 with typed scalar constant evaluation plus explicit zero-init flags for top-level static vars, and keep structural aggregate constant serialization out of scope for this wave | tradeoff: top-level aggregate static initializers can still require later follow-up for richer serialized constant facts, but the admitted repository slice now has honest sema-owned compile-time scalar checking and deterministic static-storage truth without forcing a premature artifact redesign | revisit trigger: reopen only when an admitted imported or project workflow needs structural aggregate constant facts across `.mci` boundaries rather than scalar constants plus zero-init storage facts | reference: /Users/ro/dev/c_modern/docs/plan/phase25_compile_time_and_static_storage_semantic_closure.txt
- 2026-04-04 | area: stdlib | context: Phase 24 narrow companion-family admission after the richer project-graph and compiler re-audit waves | limiter: admitted ordinary project tests were carrying repeated boolean, integer, nil, and string-check boilerplate, but the repository still did not have comparably strong admitted runtime pressure for `time` or a broader consumer matrix for `os` or `hal` | decision: expand `stdlib/testing.mc` only modestly with expectation helpers (`expect`, `expect_false`, `expect_i32_eq`, `expect_usize_eq`, `expect_str_eq`) and prove them through real project ordinary tests instead of admitting a new runtime-facing family | tradeoff: the repository now carries a slightly richer toolchain companion module, but it still avoids turning `testing` into compiler magic or claiming `time` before clocks and sleep have an honest admitted consumer | revisit trigger: reopen `time` only when admitted projects need deadlines, retries, sleep, or timeout-aware runtime behavior beyond host-side workflow control, and reopen broader `testing` only when repeated fixture or capture patterns justify another narrow helper slice | reference: `/Users/ro/dev/c_modern/docs/plan/phase24_narrow_companion_family_admission_from_project_pressure.txt`
- 2026-04-04 | area: mir | context: Phase 23 compiler follow-up re-audit after the landed Phase 22 richer project graph | limiter: the new `review_board` project and its shared-module multi-target workflow pressure still did not create a second independent imported generic procedure-call consumer or expose another compiler-owned limitation materially harming an admitted consumer | decision: keep the bounded compiler-owned `sync.thread_spawn<T>` rewrite deferred and close Phase 23 as a documented no-op compiler re-audit instead of widening imported generic call lowering speculatively | tradeoff: the compiler still carries one explicit imported generic procedure-call workaround, but the repository avoids broadening MIR call lowering without a real admitted consumer | revisit trigger: reopen when an admitted project lands a second imported generic procedure-typed call site beyond `sync.thread_spawn<T>` or another clearly owned compiler limitation becomes the narrowest honest blocker | reference: `/Users/ro/dev/c_modern/docs/plan/phase23_compiler_followup_from_phase22_pressure.txt`
- 2026-04-04 | area: codegen | context: Phase 21 compiler debt closeout for the admitted networking `io.Event` consumer surface | limiter: the hosted LLVM backend kept `bool` as `i1` everywhere, while the host C ABI stores aggregate bool fields as byte-shaped values, so the admitted typed `io.Event` surface had to keep readiness flags as `u8` to stay honest | decision: split backend register truth from aggregate storage truth by lowering struct bool fields with byte-shaped storage while preserving ordinary bool values as `i1`, then restore `io.Event.{readable,writable,failed}` to `bool` in the stdlib, runtime, and admitted networking proofs | tradeoff: the backend now carries one explicit storage-representation bridge for aggregate bool fields instead of pretending the scalar LLVM `i1` representation is ABI-correct in aggregates | revisit trigger: reopen only if a broader host ABI surface exposes another representation-sensitive aggregate case beyond the now-restored admitted `io.Event` path | reference: `/Users/ro/dev/c_modern/docs/plan/phase21_compiler_debt_closeout_from_admitted_consumers.txt`
- 2026-04-04 | area: tooling | context: Phase 19 workflow trust hardening on the admitted networking project slice | limiter: the driver could parse mixed direct-source and project-mode test invocations, but the repository still has no admitted direct-source test artifact model and silently reinterpreting those calls made the workflow boundary harder to trust | decision: keep `mc test` explicitly project-only, reject mixed direct-source and project-mode invocations early, and sharpen per-target ordinary-test plus compiler-regression reporting instead of inventing a hybrid test path | tradeoff: users still use separate direct-source `check`, `build`, or `run` commands rather than a unified direct-source test command, but the admitted workflow model stays predictable and reviewable | revisit trigger: reopen only when the repository admits a real direct-source test artifact contract or a broader multi-target workflow needs a different command boundary | reference: `/Users/ro/dev/c_modern/docs/plan/phase19_workflow_trust_hardening.txt`
- 2026-04-04 | phase 18 plateau decision | the repository had landed richer networking project fixtures and bounded compiler widening, but the remaining highest-leverage pressure was still around project workflow trust, ordinary-test proof, and deterministic reuse rather than a missing new capability family | choose Option A and continue with Phase 13 trust and reporting hardening as the next plateau, using the admitted networking projects as the proof surface | the repository deliberately does not widen `sync`, admit `hal`, or move to broader release hardening yet, so capability breadth pauses while workflow trust is tightened | revisit when a second real `sync` consumer appears, a concrete MMIO consumer matrix exists, or the richer project fixtures stop exposing workflow friction materially | reference: `/Users/ro/dev/c_modern/docs/plan/phase18_workflow_hardening_and_plateau_decision.txt`
- 2026-04-04 | phase 12 sync lifecycle cleanup | the admitted mutex and condvar proofs were still relying on process-lifetime backing allocations after init because no destroy surface had been admitted yet | admit narrow hosted `mutex_destroy` and `condvar_destroy` APIs that free backing state on successful pthread destroy and zero the opaque raw handle afterwards | the repository closes a real lifecycle leak in the admitted hosted slice without yet widening into condvar broadcast, richer ownership helpers, or reusable object-state diagnostics | revisit when a real consumer needs broader lifecycle semantics beyond init, destroy, lock, unlock, wait, and signal | reference: `/Users/ro/dev/c_modern/stdlib/sync.mc`
- 2026-04-04 | phase 17 imported typed-surface widening first slice | imported `sync.MemoryOrder` operands flowing into atomic intrinsics were being preserved in MIR as symbolic metadata like `order=order`, but the MIR validator incorrectly treated every metadata spelling as if it already had to be one of the backend's constant-only atomic orders | relax MIR validation so symbolic imported `MemoryOrder` operands remain valid at the MIR layer while the constant-only restriction stays in the LLVM backend where executable emission actually needs it | the repository now accepts a real imported typed-surface use-case and unblocks the failing imported-atomic MIR fixture, but this narrows only one compiler limitation and does not yet restore the broader networking `File` plus `Event` surface | revisit when the next networking-shaped compiler widening slice is ready, especially imported handle aliases or qualified event-buffer typing | reference: `/Users/ro/dev/c_modern/docs/plan/phase17_compiler_widening_import_visible_typed_surfaces.txt`
- 2026-04-04 | phase 17 networking typed-surface widening second slice | the hosted networking proof was still flattened to raw `i32` handles plus parallel poller output arrays because imported handle aliases, imported event-record use, and qualified event-buffer passing had not all survived the executable path honestly | restore the admitted surface to `io.File` plus `io.Event` with `poller_wait(p, events: Slice<Event>, timeout_ms)`, and fix executable `array_to_slice` lowering so local arrays passed to imported slice-taking functions preserve their real storage instead of an ephemeral copy | the real networking examples and proofs now use a typed `Event` record instead of parallel buffers; the temporary `u8` readiness spelling was later removed once Phase 21 landed aggregate-bool ABI cleanup in the backend | revisit when a different admitted host-ABI aggregate representation gap appears beyond the restored `io.Event` surface | reference: `/Users/ro/dev/c_modern/docs/plan/phase17_compiler_widening_import_visible_typed_surfaces.txt`
- 2026-04-04 | phase 12 narrow sync admission | the more ergonomic typed `thread_spawn<T>(entry: func(*T), ctx: *T)` surface still runs into bootstrap MIR call-metadata limits for imported generic functions with procedure-typed parameters | keep the public typed `thread_spawn<T>` surface, but lower it through a compiler-owned rewrite to `thread_spawn_raw(uintptr, uintptr)` underneath | the admitted sync API matches the intended typed surface for current consumers, but the compiler still does not provide full general imported generic procedure-call lowering | revisit when a second real consumer justifies removing the rewrite in favor of broader imported generic call support | reference: `/Users/ro/dev/c_modern/docs/plan/phase12_stdlib_runtime_maturity.txt`
- 2026-04-03 | phase 12 networking second wave | the first hosted `io` poller plus `net` slice ran into bootstrap limits around imported handle aliases, imported event-record types, and qualified generic event buffers | flatten the admitted bootstrap surface to plain `i32` handles plus caller-owned parallel poller output buffers (`files`, `readable`, `writable`, `failed`) while keeping readiness ownership explicit and the hosted proofs real | the public surface is less elegant than the intended long-term `File` plus `Event` design, but the repository now has honest end-to-end networking proof instead of a cleaner false sketch | revisit when parser and backend support imported alias/type use and event-buffer passing without bootstrap-specific workarounds | reference: `/Users/ro/dev/c_modern/docs/plan/phase12_stdlib_runtime_maturity.txt`
- 2026-04-03 | phase 8 file walker | the hosted bootstrap slice had whole-file loading but no admitted directory traversal surface | admit a narrow hosted `fs.is_dir` plus newline-delimited, sorted `fs.list_dir` surface | avoids inventing a richer directory-entry API before the repository can prove it honestly | revisit when directory-iteration pressure grows
- 2026-04-03 | phase 8 hashing tool | the admitted slice could prove a checksum utility with whole-file reads, but streaming reads were not yet honest pressure | land a narrow whole-file checksum over `fs.read_all` with explicit fixed-width hex formatting | postpones streaming and richer buffer helpers to later pressure work | revisit when streaming I/O becomes an admitted repository need
- 2026-04-03 | phase 8 arena-backed parser utility | the bootstrap slice did not yet expose richer arena string helpers | store AST nodes in an arena while borrowing token slices from the owned file buffer | keeps allocator pairing and tree construction honest without widening mem prematurely | revisit when owned text helpers become real pressure
- 2026-04-03 | phase 11 address-flow probe | the second probe exposed an upstream MIR limitation on `&pair.right`, not a backend-owned defect | narrow the probe to the supported address-sensitive shape `&local` so 11C still lands a real backend regression | avoids papering over the wrong layer while keeping backend coverage honest | revisit when MIR owns the broader address-sensitive member-flow shape
- 2026-04-03 | phase 12 deferred module admission | there was no honest consumer matrix for `time`, `os`, `sync`, `hal`, and `testing` yet | keep those modules deferred until real program or toolchain pressure justifies a stated API | preserves a narrower stdlib surface for now | revisit when a concrete consumer matrix exists
- 2026-04-14 | area: kernel | context: Phase 155 temporal semantics and backpressure audit | decision: backpressure is now explicit — `log_service.handle` returns `Exhausted` when the 4-entry buffer is full and an append is requested; `kv_service.handle` returns `Exhausted` when the 4-key store is full and a new key is submitted; an overwrite of an existing key is always `Ok` regardless of count; the kv-write advisory log marker discards its own `Exhausted` reply at the boot dispatcher — the kv caller sees only the kv reply | limiter: no scheduler, async framework, dynamic queue growth, or timing API was added; all temporal truth is stated as owner-local state transformations | tradeoff: callers now receive an explicit `Exhausted` failure instead of a silent successful-looking ok-with-no-effect; earlier tests that filled a store and expected `Ok` required updating | revisit trigger: reopen queue or timing policy only when a real multi-client phase (156+) exposes interleaving gaps that cannot be expressed through explicit backpressure replies | reference: docs/plan/active/phase155_temporal_semantics_and_backpressure_audit.txt
- 2026-04-14 | area: kernel | context: Phase 155 compiler and toolchain surfaces | decision: no compiler, sema, MIR, backend, ABI, target, runtime, or hal expansion was made in Phase 155; the audit is entirely within kernel service state transitions and their reply codes | limiter: temporal pressure did not expose any compiler gap in the admitted service system; the only surfaced need was explicit backpressure codes already representable through the existing `SyscallStatus.Exhausted` variant | revisit trigger: reopen compiler surfaces only if a later phase proves a temporal or scheduling gap that cannot be modeled by owner-local state plus explicit reply codes | reference: docs/plan/active/phase155_temporal_semantics_and_backpressure_audit.txt
- 2026-04-14 | area: kernel | context: Phase 156 multi-client interaction audit | decision: no per-client state isolation was introduced; kv_service and log_service hold one globally-shared state value; a write from any source_pid is immediately visible to all subsequent dispatch steps regardless of which source_pid submits them; reply routing is by call-return rather than by source_pid — the effect returned by kernel_dispatch_step belongs to whichever call owns that return value; backpressure is per-service capacity, not per-client quota; last writer wins with no read-your-writes isolation between clients | limiter: no reply mailbox, per-client namespace, session concept, or fairness mechanism was added; the dispatcher remains a purely sequential single-state-machine; these are the first explicit ordering and isolation assumptions that would fail under real concurrent clients | revisit trigger: reopen only if a later phase requires per-client state isolation or concurrent reply routing and can prove that need from a real system requirement | reference: docs/plan/active/phase156_multi_client_interaction_audit.txt
- 2026-04-14 | area: kernel | context: Phase 156 compiler and toolchain surfaces | decision: no compiler, sema, MIR, backend, ABI, target, runtime, or hal expansion was made in Phase 156; multi-client pressure is entirely expressible through existing source_pid fields in Message and ReceiveObservation, the existing sequential dispatcher, and explicit backpressure reply codes already present from Phase 155 | limiter: multi-client interleaving at this scale does not require any language or toolchain feature beyond what is already admitted; the only new surface is a second source_pid value passed to the same dispatch call | revisit trigger: reopen compiler surfaces only when a real concurrent-client phase proves a gap that cannot be expressed through sequential dispatch plus explicit per-step return values | reference: docs/plan/active/phase156_multi_client_interaction_audit.txt
- 2026-04-14 | area: kernel | context: Phase 157 long-lived system coherence audit | decision: kv state is stable across repeated reads — a key set once returns the same value until explicitly overwritten with no silent expiry or drift; log overwrite-on-full is the permanent policy — a saturated 4-entry log returns Exhausted on every further append and tail returns the original entries unchanged; restart via kernel_init() resets all service state to the empty baseline with no hidden residue; kv-write advisory log markers degrade gracefully at log saturation — the marker step discards its own Exhausted silently and the kv reply and kv state remain unaffected; repeated overwrites of the same key do not grow the store count | limiter: no persistence, durability, expiry, supervisor, or dynamic capacity change was added; all coherence properties are emergent from the existing pure state-machine model | tradeoff: the system is now explicitly audited as coherent for long-lived use at fixed capacity; the admitted model has no silent failure modes under repeated interaction, but it is bounded at 4-entry capacity for both services | revisit trigger: reopen only when a later phase proves a coherence gap that cannot be expressed through current owner-local fixed-capacity state machines | reference: docs/plan/active/phase157_long_lived_system_coherence_audit.txt
- 2026-04-14 | area: kernel | context: Phase 157 structural refactoring — main.mc, serial_protocol.mc, scenarios.mc | decision: extracted serial shell command construction into kernel/src/serial_protocol.mc and named integration scenarios into kernel/src/scenarios.mc; main.mc is now a pure orchestrator: init state, call named scenario functions, return status; protocol encoding, command construction, and correctness assertions no longer live in main | limiter: no new language features, ABI changes, or runtime library additions; the refactoring is purely within kernel source structure | tradeoff: main.mc is now a stable narrow entry point that will not grow with future phases; new scenarios add a named function to scenarios.mc rather than extending main directly; the next natural step is a real event-loop dispatcher replacing the scenario runner | revisit trigger: add a new scenario module or promote scenarios.mc to a real event-loop driver when a phase requires non-sequential or interrupt-driven dispatch; do not extend main.mc with inline protocol bytes or inline assertions | reference: kernel/src/serial_protocol.mc, kernel/src/scenarios.mc
- 2026-04-14 | area: kernel | context: Phase 157 compiler and toolchain surfaces | decision: no compiler, sema, MIR, backend, ABI, target, runtime, or hal expansion was made in Phase 157; long-lived coherence pressure is entirely expressible through existing owner-local fixed-capacity state machines and pure state-transition functions | limiter: long-lived interaction at fixed service capacity does not expose any language gap; the only new surfaces are two kernel source modules within the existing module admission model | revisit trigger: reopen compiler surfaces only when a later phase proves a coherence or capacity gap that cannot be modeled by owner-local pure state machines | reference: docs/plan/active/phase157_long_lived_system_coherence_audit.txt
- 2026-04-14 | area: compiler | context: Phase 163 bootstrap constant linkage and aggregate const follow-through over the active kernel and project-mode logical-module paths | limiter: the repository needed to spend already-admitted constant support honestly without using that as a reason to reopen const functions, compile-time execution, or broader linker and packaging policy | decision: keep Phase 163 limited to direct cross-module reuse of exported module-scoped constants and one admitted aggregate-const follow-through; spend that support in `kernel/src/` and cover the newer `module_sets.<name>.files` path with a focused project regression instead of widening compiler semantics | tradeoff: the repository now removes stale constant duplication and proves the active logical-module path honestly, but still deliberately stops short of const functions, arbitrary compile-time execution, and broader linker-policy work | revisit trigger: reopen only when a real admitted consumer needs compile-time function execution or a broader constant artifact policy beyond direct exported constants plus aggregate literals built from admitted constant ingredients | reference: docs/plan/active/phase163_bootstrap_constant_linkage_and_aggregate_const_follow_through.txt

- 2026-04-14 | area: kernel | context: Phase 158 cross-service failure semantics consistency | decision: the advisory kv-write log marker silencing is intentional and named — when the log is full, a kv write still returns Ok to the caller; the dispatcher discards the advisory log marker without returning Dropped or any signal; kv success does not imply audit success; aligned status codes (Exhausted, InvalidArgument) are consistent across service boundaries; Exhausted state is sticky per service; no recovery framework, retry policy, or log-full callback was added | limiter: the advisory-send model has no per-send delivery witness; the dispatcher can only chain effects and discard Exhausted from advisory sends silently | tradeoff: the kv caller gets a clean Ok but loses the audit trail silently; this is the named cross-service inconsistency; it is directly observable via the log tail but invisible in the kv reply | revisit trigger: reopen when Phase 160 or later names per-send delivery witnesses as the narrowest next expansion and the evidence justifies implementing them | reference: docs/plan/active/phase158_cross_service_failure_semantics_consistency.txt
- 2026-04-14 | area: kernel | context: Phase 159 observability as a system property | decision: kv_count is the minimum gap to close — before Phase 159 a caller could observe log occupancy via log_tail but had no protocol-visible way to determine kv occupancy without prior key knowledge; kv_count via KC!! closes that gap with zero new module surface by reusing the existing payload_len=0 dispatch path already in kv_service.handle; observation is explicitly classified as non-mutating; KV_WRITE_LOG_MARKER byte value and internal endpoint IDs are classified as implementation detail rather than user contracts; no telemetry stack, logging framework, or cross-cutting debug substrate was admitted | limiter: kv_count reveals entry count but not the stored keys; the correlation between kv write count and log entry count remains broken when the log is full; that gap is the model boundary addressed in Phase 160 | revisit trigger: reopen if a later phase needs a key-enumeration surface (e.g., iterate all kv keys) that cannot be served by count plus known-key lookups | reference: docs/plan/active/phase159_observability_as_a_system_property.txt
- 2026-04-14 | area: kernel | context: Phase 160 first model boundary identification — compiler and toolchain surfaces | decision: no compiler, sema, MIR, backend, ABI, target, runtime, or hal expansion was made in Phase 160; the model boundary is identified and named but not fixed; the narrowest plausible next expansion axis (per-send delivery witnesses) is classified but not admitted; the existing effect model is sufficient to prove the boundary exists without any language change | limiter: the advisory-send gap is a runtime dispatcher policy limitation, not a missing language feature; a witness surface could be modelled within the existing effect/reply system without compiler changes | tradeoff: the boundary is named clearly before any fix is attempted, preserving the repository discipline of one honest "not yet" before larger subsystem investment | revisit trigger: admit per-send delivery witnesses if a later phase proves that the silent advisory-drop semantics causes real correctness harm in an admitted service interaction, and can show the narrowest effect-model extension that closes it without a recovery framework | reference: docs/plan/active/phase160_first_model_boundary_identification.txt
- 2026-04-14 | area: kernel | context: Phase 160 advisory-drop gap — deferred fix | decision: the advisory kv-write audit silencing is not fixed in this phase; the named fix axis is per-send delivery witnesses (a Dropped reply variant the dispatcher could surface to the kv caller when the advisory send could not be delivered); no witness implementation is admitted; the boundary is proved in the running artifact and the system test fixture but the dispatcher policy is unchanged | limiter: fixing the gap without a recovery framework requires at minimum a new EffectKind.SendWithWitness or a Dropped status code path in execute_effect, which is a deliberate two-step (name then fix) discipline | tradeoff: the kv service still returns Ok without audit when the log is full; this is acceptable for the current proof-of-model phase; a later phase may choose to fix it or to keep the advisory semantics and document them as a permanent design decision | revisit trigger: reopen when a concrete usage scenario requires the kv caller to distinguish audited writes from silent writes, and when that distinction can be expressed through the narrowest witness model without broadening into saga patterns, two-phase commit, or a supervisor | reference: docs/plan/active/phase160_first_model_boundary_identification.txt
- 2026-04-14 | area: kernel | context: Phase 166 second boundary repair — observable event codes structural home | decision: the six serial-shell dispatch event codes (EVENT_SERIAL_BUFFERED through EVENT_SERIAL_CLEARED) were duplicated verbatim between kernel_dispatch.mc and serial_shell_event_log.mc with no import relationship; a new event_codes.mc module now owns all six constants; kernel_dispatch.mc imports event_codes and drops its local copies; serial_shell_event_log.mc imports event_codes, drops its local definitions, and re-exports the constants as module-level aliases so existing call sites that access them via serial_shell_event_log.EVENT_* continue to compile without change; serial_shell_event_log.mc's slot0/slot1/slot2/slot3 separate fields were replaced with slots: [4]u32 eliminating slot_value() and simplifying with_slot() to one array copy plus one indexed write | limiter: no compiler, sema, MIR, backend, ABI, target, runtime, or hal expansion was needed; the module-level const aliasing form (const X: u32 = module.Y) works within the existing bootstrap sema constant model; the ring buffer logic, Effect model, and protocol wire format are unchanged | tradeoff: the language does not have re-export syntax, so serial_shell_event_log.mc carries explicit alias constants rather than a transparent re-export; this is honest and does not introduce a new mechanism | revisit trigger: reopen if the language later gains a re-export or public-use modifier that would eliminate the alias boilerplate; otherwise the alias pattern is stable | reference: docs/plan/active/phase166_bounded_model_expansion_from_second_boundary.txt
- 2026-04-14 | area: kernel | context: Phase 166 serial_on_receive style guide fix | decision: serial_on_receive had four explicit if-blocks guarded by obs.payload_len comparisons to conditionally copy obs.payload[0] through obs.payload[3]; replaced with a single for i in 0..obs.payload_len loop with one capacity guard inside; the behavior is identical, the code now matches the style guide's loop-over-fixed-array rule, and the loop bound is a runtime value (obs.payload_len: usize) which the existing sema range-loop model already handles | limiter: the SERIAL_BUFFER_CAPACITY is intentionally kept at 4 matching the wire payload frame size; the fix is a code-shape change, not a capacity expansion | tradeoff: none; this is a strict improvement with no behavioral change | revisit trigger: reopen only if serial protocol byte staging outgrows the 4-byte payload frame and a larger buffer capacity is needed | reference: kernel/src/serial_service.mc
- 2026-04-14 | area: kernel | context: Phase 170 static service topology first acknowledgment — topology naming decision | decision: Branch A (topology naming admitted); service_topology.mc previously held four endpoint ID constants but no record of which service or pid occupies each endpoint; that second half of the static wiring (the owner pids) lived in boot.mc as four separate BOOT_*_OWNER_PID constants; a ServiceSlot struct (endpoint: u32, pid: u32) and four slot constants (SERIAL_SLOT, SHELL_SLOT, LOG_SLOT, KV_SLOT) plus SERVICE_COUNT are added to service_topology.mc; boot.mc's four BOOT_*_OWNER_PID constants are removed and kernel_init() now reads slot.pid from the imported slots; all other service_topology consumers (kernel_dispatch.mc, shell_service.mc, scenarios.mc) are unchanged because they only needed endpoint IDs | limiter: no routing table, dynamic registration, dispatch generalisation, or indirection layer was introduced; ServiceSlot is a flat read-only record with two u32 fields and no methods; additions via new slot constant scale linearly and require no structural change | tradeoff: a reader can now find the complete static wiring of the kernel in one module without cross-referencing boot.mc; the field-access pattern (slot: ServiceSlot = module.SLOT, then slot.pid) is slightly more verbose than the former scalar constant but is straightforward; no routing or naming framework is created | revisit trigger: reopen topology structure if the service count grows past eight, if a supervising layer needs to iterate the slot table at runtime, or if a new phase requires dynamic endpoint assignment that cannot be expressed as a static constant | reference: docs/plan/active/phase170_static_service_topology_first_acknowledgment.txt, kernel/src/service_topology.mc

- 2026-04-14 | area: compiler | context: Phase 171 generic monomorphization first slice — consumer audit and deferral decision | limiter: every admitted non-extern generic function body in the repository (sync.thread_spawn<T>, testing.expect_non_nil<T>, testing.expect_buffer_non_nil<T>, testing.expect_nil<T>) uses T only behind a pointer, making all of them erased-ABI compatible; all other admitted generic functions are either extern(c) — emitted as C-ABI declarations without bodies — or special-cased MIR intrinsics (atomic_load<T>, atomic_store<T>, volatile_load<T>, volatile_store<T>, mmio_ptr<T>, hall intrinsics) that never reach the generic-call lowering path; the kernel has zero generic function calls; no second admitted consumer requiring type-specialized ABI was found | decision: take Branch B (explicit deferral) — implement no monomorphization; instead, close the one real structural gap exposed by the audit: the backend was silently skipping non-extern generic function bodies that fail erased-ABI compatibility with no diagnostic, which would produce a mystery linker error rather than a compiler error; ValidateExecutableBackendCapabilitiesImpl now emits a named error for that case before emission begins | tradeoff: the repository continues to enjoy erased-ABI generic functions that work honestly without monomorphization, and gains a compile-time diagnostic for the case that previously produced a silent link failure; full type-directed specialization remains explicitly deferred | revisit trigger: reopen when a real admitted consumer cannot be served by the erased-ABI family — for example an inline generic utility whose body requires T-sized stack allocation, T-specialized loop iteration, or T-dependent instruction selection | reference: docs/plan/active/phase171_generic_monomorphization_first_slice.txt
- 2026-04-14 | area: kernel | context: Phase 172 supervisor and restart shape probe | limiter: the system has exactly 4 boot-wired services (serial, shell, log, kv); all restart machinery (bootwith_log, bootwith_kv) was mechanically present in boot.mc but had zero callers; init.mc was named as the restart-policy owner in Phase 96–149 documentation but never created in the reset-lane tree; the supervisor concept was explicitly ruled out in Phase 149 and there is no current pressure that would reverse that ruling; the 22-step scenario exercised buffer exhaustion (Exhausted at steps 9 and 11) but had no recovery path — permanent terminal state with no named policy owner to act on it | decision: take Branch B (no supervisor framework); close the structural gap by creating kernel/src/init.mc as the named restart-policy owner; init.mc owns restart_log() and restart_kv() — two plain functions that translate an observed Exhausted reply into a clean state replacement via boot.mc's bootwith_* helpers; add run_restart_probe() to scenarios.mc to exercise the restart path: after run_main leaves the log exhausted, init.restart_log resets it and two steps verify the log accepts a new append and reports one retained entry | tradeoff: the named policy owner now exists and is exercised; no supervisor framework, health-check callback, retry counter, watchdog, or automatic policy was introduced; the restart is explicit and caller-driven, not autonomic | revisit trigger: reopen supervisor concept when service count exceeds 8, when restart-frequency pressure requires per-service retry policies, or when a phase proves that init-owned explicit restart is the narrowest blocked path for a real admitted consumer | reference: docs/plan/active/phase172_supervisor_and_restart_shape_probe.txt, kernel/src/init.mc
- 2026-04-14 | area: kernel | context: Phase 173 multi-core model boundary identification — SMP deferral decision | limiter: the kernel dispatches over a single KernelBootState pointer; all service functions are pure state machines (take state by value, return new state); service_topology.mc endpoint IDs are compile-time constants; no SMP hardware is targeted; the current 22-step scenario and restart probe are fully sequential; no phase between 152 and 172 introduced any shared-state mutation that requires concurrent access | decision: take explicit deferral — do not implement SMP, locks, barriers, or per-core data structures; instead name the two real constraints from current source: (A) unified dispatch state pointer in kernel_dispatch.mc where kernel_dispatch_step, kernel_dispatch_leaf, kernel_dispatch_kv, and execute_effect all share one *KernelBootState — the hardest to retrofit because partitioning KernelBootState requires structural redesign of the dispatch call site; (B) advisory-first compound dispatch atomicity in kernel_dispatch_kv where the kv write and advisory log append are committed to *state as a compound assignment — medium difficulty to retrofit; record also that the per-service handler functions are already SMP-compatible (pure value-in/value-out), which is the clean path for any future per-service shard approach; add tests/system/kernel_reset_lane_smp_boundary/ fixture to make constraint A and B concrete in executable code | tradeoff: SMP constraints are now named explicitly from source rather than deferred silently; future phases are aware of the two fragile points; no mechanism was added; the constraint names are grounded in actual dispatch call sites, not architecture theory | revisit trigger: reopen constraint A when a phase requires concurrent interrupt-driven dispatch from multiple hardware cores or concurrent wake on the scheduler ready queue from two cores; reopen constraint B when a phase introduces concurrent log writes from two sources sharing the same advisory commit window | reference: docs/plan/active/phase173_multi_core_model_boundary_identification.txt, kernel/src/kernel_dispatch.mc
